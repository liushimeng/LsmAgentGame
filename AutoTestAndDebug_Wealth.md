## 自动化 Web 游戏测试任务：财商流游戏 P0 专项

> 本项目按 [MIT License](LICENSE) 开源。测试任务面向维护者，参与前请阅读 [CONTRIBUTING.md](CONTRIBUTING.md)。
> 本文件为财商流游戏专用入口，**仅产出** `财商流游戏自动化测试报告_YYYYMMDD_HHMMSS.md` 前缀的报告。

- **工作目录**: `/usr/local/LsmAgentGame/LsmAgentGame`
- **入口脚本**: `AutoTestAndDebug_Wealth.sh`(随机选编程 Agent CLI → 跑测试 → 报告落盘 → **同一 Agent 会话内**自动进入修复流程)
- **产物路径(仅财商流游戏,glob 集中到 `auto_run_common.sh::GAME_GLOBS`)**:
  - 测试报告: `TestReport/财商流游戏自动化测试报告_YYYYMMDD_HHMMSS.md`(单一 glob,无兼容旧文件)
  - 进度文件: `AutoTestProgress/财商流游戏自动化测试进度_YYYYMMDD_HHMMSS.md`
  - 子工程工具报告(可选): `go-web-debug-tool/UseReport/财商流游戏测试工具使用报告_YYYYMMDD_HHMMSS.md`
  - 协议抓包分析报告(按需): `TestReport/财商流游戏协议抓包分析报告_YYYYMMDD_HHMMSS.md`
  - 文件名时间戳统一 `YYYYMMDD_HHMMSS`,精度到秒
  - **Glob 单一事实源**: 所有扫描 glob 在 `auto_run_common.sh::GAME_GLOBS` 中声明,新增游戏仅改公共库一处,本文件不再内嵌 glob 列表。
   - **测试产物一律不入库**: `TestReport/*` / `AutoTestProgress/` / `AutoTestScreenshots/` 已被 `.gitignore` 整目录忽略,所有测试生成文件(报告/进度/截图/临时文档)仅留本地,Agent **禁止**对其执行 `git add` / `git commit` / `git push`。
- **执行模式**: 主 Agent 跑核心流程;遇代码读取/协议抓包/数据查询可按需委派 SubAgent,不阻塞主流程。
- **执行策略(本版本硬约束)**:
  - **混合房间**: 财商流游戏 = 1 名人类玩家 + 11 个 Agent 混合 12 座局(2026-09-16 §12 座扩容),**不测试纯人类模式**。Agent 按 §14.2 自动分配不同模型(11 bot 尽量不同模型)。
  - **拟人节奏**: 每步操作后**等待页面完全加载**(推荐 800~1500ms)再走下一步;**操作严格模拟正常人类玩家**——不瞬时连点、不用 `eval_js` 一键改多状态。月度节拍为实时 tick(默认 8000ms/月),测试需在动作窗口内完成人类操作。
  - **月结驱动**: 单局 420 月(25→60 岁)全流程过于漫长,测试采用**小月时长**(`month_ms:3000`)+ **多房间分模块验证**策略:每个模块建独立房间,验证核心路径后即可结束,**不需要**单房间跑满 420 月。经济引擎数值正确性以**服务端单元测试**(engine/settlement/market/ledger 不变量)为事实来源,前端测试聚焦**界面交互正确性 + 状态同步**。
  - **破产/终局专项**: 破产流程、终局评分、6 种结局需安排**独立房间**验证(可通过调整 seed 或操作触发特定路径,**严禁**用 `eval_js` 直接改状态绕过游戏逻辑)。
  - **重大问题/卡住/严重不及预期 → 立即终止本轮**: 不等待超时,**立刻**生成问题报告 + 进入 §12 自动修复流程,**严禁强行绕过继续推流程**。
  - **UI/UX 合理性审计**: 测试不仅是"功能通/不通",还必须评估**布局合理性、交互合理性、可达性、可读性**——把发现的视觉/交互问题作为本轮报告的「UI 合理性审计」章节独立输出。
- **核心目标**: 验证 Web 端人类玩家可用的全部交互(房间创建、职业卡展示、12 种动作操作、月度节拍、三表面板、Ledger 流水、行情面板、聊天、观战)与 7 个 Agent 的拟人行为(月度决策、工具调用、speak 发言、记忆连贯性、破产应对、多模型驱动),并对 UI/UX 设计做合理性评估。同时通过降级路径验证服务端经济引擎 REST/状态接口。
- **代码变更限制**: 测试阶段业务代码只读;测试完成后进入「自动修复流程」时可修改代码修复缺陷(遵循 §12 约束)。

### 1. 核心测试工具(MCP)

主工具是 `go-web-debug-tool`(MCP 服务,默认 `http://localhost:28999`),接口定义以
`/usr/local/LsmAgentGame/LsmAgentGame/go-web-debug-tool/MCP_Proc_Def.md` 为唯一事实来源。
**测试 Agent 必须将其作为唯一操作入口**,**严禁**直接调业务 HTTP 接口(`curl` REST 仅允许只读查询,见 §10 降级路径)。
测试的所有交互操作——登录、房间创建、动作操作、发言、观战——**必须**通过 MCP 的 `ControlChromePage` 模拟人类操作完成,**禁止**用 `curl` / `fetch` / `axios` 等直接调业务 API 绕过 Web 界面。

#### 1.1 接口能力与典型用法

| 接口 | 用途 | 拟人场景示例 |
|------|------|--------------|
| `POST /NewChromePage` | 打开新标签页 | `{"url":"https://127.0.0.1:39001/wealth"}` |
| `POST /ControlChromePage` | 模拟人类动作 | `click` / `dblclick` / `hover` / `scroll` / `input_text` / `key_press` / `drag` |
| `POST /LookChromePageInfo` | 读取页面状态 | `screenshot` / `dom` / `console` / `network` / `accessibility`(评估可达性) |
| `POST /CloseChromePage` | 关闭标签页 | 测试结束后清理 |
| `POST /ListChromePages` | 枚举已开页面 | 会话开始 / 失联恢复 |

#### 1.2 DOM 优先、截图兜底策略(硬约束)

> 信息获取**必须**按以下优先级递降,**禁止**跳过高层直接走截图:

| 优先级 | 方式 | 适用场景 | 典型调用 |
|--------|------|---------|---------|
| 1(首选) | **DOM 文本读取** | 按钮文案/资源数值/阶段标签/发言内容/计时器/动作结果/错误提示 | `LookChromePageInfo info=dom` |
| 2 | **DOM 属性读取** | 元素尺寸/位置/可见性/禁用态/选中态/focus 序/对比度 | `LookChromePageInfo info=dom` + `eval_js`(只读) |
| 3 | **网络请求读取** | API 调用是否成功/WS 帧是否到达/接口返回值 | `LookChromePageInfo info=network` |
| 4 | **控制台日志读取** | 报错信息/警告/React 渲染日志 | `LookChromePageInfo info=console` |
| 5(兜底) | **截图** | 视觉验证(动画过渡态/布局溢出/对比度/色彩/2.5D 地图) | `LookChromePageInfo info=screenshot` |

**原则**:
- **DOM 能回答的问题不截图** — 验证「当前月份/阶段」读阶段标签文字;验证「现金/净资产」读 DOM 数值;验证「按钮是否可点击」读 `disabled` 属性;验证「Ledger 条目」读文本节点;验证「Agent 发言」读聊天 DOM。
- **截图仅用于** 视觉层面验证 — 布局溢出/动画过渡态/颜色对比度/2.5D 地图渲染/整体美观度。
- **复杂动画** (token 迁区 lerp、月结数字滚动) 必须连续截 2~3 帧(只截一帧可能错过过渡态)。
- **布局审计必须用 `accessibility` info**(role tree / contrast 提示 / focus order)评估键盘可达性。
- **优先用语义选择器**(`role` / `aria-label` / `data-testid` / 文本内容)而非 CSS class(类名易因重构失效)。

#### 1.3 拟人操作硬约束

- **每步操作后等待 800~1500ms**,**禁止**瞬时连点;`scroll` 时使用 `behavior: "smooth"` 模拟真人滚动。
- **点击必须用真实点击**(`action: "click"` / `dblclick`),**禁止**用 `eval_js` 一键完成需拟人验证的整段动作。
- **`input_text` 必须按字符节奏**:`delay_per_key_ms` ≥ 30ms(模拟真人按键)。
- **悬停 `hover`** 必须显式调用(下拉菜单展开、tooltip、城区信息卡、模态等场景)。
- **滚动 `scroll`** 必须按方向(`up` / `down` / `left` / `right`)显式调用,且目标 `selector` 优先于坐标。
- **`eval_js` 仅允许用于只读行为**:
  - 读取 `localStorage` / `sessionStorage` 状态
  - 读取 zustand store 快照
  - 读取 DOM 属性(`getBoundingClientRect` / 颜色计算 / 滚动位置)
  - 触发键盘快捷键(如 `Ctrl+Enter` 提交)
  - **禁止** 用 `eval_js` 一键创建房间、跳过月份、模拟 LLM 响应、批量改经济状态。

### 2. 财商流游戏模块与测试矩阵

#### 2.1 游戏总纲

| 维度 | 值 |
|------|-----|
| game_kind | `wealth` |
| 中文名 | 财商流游戏 |
| 房间座位 | 12 座(10–12 可玩) |
| 节奏 | 实时月度 tick(默认 8000ms/月,测试用 3000ms) |
| 人类操作 | 12 种动作 + submit_month |
| Agent 操作 | 17 工具(≤3 动作 + ≤1 speak + submit_month)/月 |
| 终局 | 主时钟 60 岁(420 月),FI + 人生满意度 + 社会贡献三维评分,6 种结局 |

#### 2.2 测试模块总览

| 模块 | 代号 | 核心验证目标 | 对应验收项 |
|------|------|-------------|-----------|
| **A 房间生命周期** | lifecycle | 大厅、建房、加入、提前开始、观战、退出、断线恢复 | F01–F08 |
| **B 经济引擎与月结** | economy | 月度节拍、月结 7 步、个税、市场漂移、失业、破产、终局 | F09–F18 / I1–I10 |
| **C 人类操作** | actions | 12 动作按钮、动作预算、买房、内联错误 | F19–F22 |
| **D Agent 驱动** | agent | 全 Agent 房、月度决策、speak、bot_contexts、混合房 | F23–F27 |
| **E 前端渲染** | render | 2.5D 地图、token、面板、GameOverModal、i18n | F28–F33 |

#### 2.3 测试矩阵(硬约束)

- **A 阶段**: 房间生命周期(1 个房间验证 F01–F08 基础流程)。
- **B 阶段**: 经济引擎(多个小房间分别验证月结、失业、破产、终局场景)。
- **C 阶段**: 人类操作(在 acting 阶段验证 12 种动作 + 动作预算 + 内联错误)。
- **D 阶段**: Agent 行为(全 Agent 房验证月度决策 / speak / 记忆 / 多模型驱动)。
- **E 阶段**: 前端渲染(2.5D 地图、三表面板、Ledger、Market、GameOverModal、i18n)。
- 每个阶段可拆分为多轮,每轮聚焦一个子目标。

#### 2.4 进度文件必含字段

- `当前阶段`(A / B / C / D / E)
- `本轮测试模块`(lifecycle / economy / actions / agent / render)
- `本轮测试焦点`(具体验证点)
- `已测组合清单` / `待测组合清单` / `下一轮建议组合`
- `未覆盖项跟踪表`(继承上轮 + 本轮新增)
- `本轮重点突破项`(1-3 项) + `突破结果`

### 3. 终止条件(立即停)

1. **重大缺陷 / 页面崩溃 / 卡死 / 规则自相矛盾** → 终止**当前用例**,出报告,**不进入下一阶段用例**。
2. 经济引擎核心逻辑错误(Ledger 不守恒、资产负债不等、税费率错误) → **整轮终止**。
3. MCP 工具严重异常无法支撑测试 → **整轮终止**,并写子工程 `UseReport/财商流游戏测试工具使用报告_*.md`。
4. Agent 调 LLM **连续 5 次失败**,协议诊断确认不可恢复 → **整轮终止**。
5. **操作逻辑严重不及预期**(Agent 月度无决策、发言为空/乱码、阶段不推进、月结数据明显错误等)→ **本轮立即终止**,不待超时,出报告。
6. **UI 严重不可达**(关键按钮无法聚焦、模态无法关闭、布局溢出导致内容不可见、对比度严重低于 WCAG AA)→ **本轮立即终止**,出 UI 合理性审计报告 + 调用修复。
7. 无论正常结束或异常终止,均须按规范出报告 + 进度文件;中断的进度必须标 `中断未完成`。

### 4. 启动前置

1. 读最近 10 次**非缺陷修复类**提交(`git log --oneline -10 -- ':!*fix' ':!*BUG'` 等过滤),梳理新增/优化项作为本轮重点。
2. 读 `AutoTestProgress/财商流游戏自动化测试进度_*.md` 最新一份,沿用其「已测 / 待测」清单与「未覆盖项跟踪表」。
3. **敲定本阶段**:`当前阶段=A / B / C / D / E`,决定测试模块与焦点,写入进度草稿。
4. 读 `go-web-debug-tool/MCP_Proc_Def.md` 确认 MCP 操作/抓包接口可用。
5. 准备 `test_account.json` 中的白名单账号(见 `docs/通用功能/测试账号凭证.md`)。
6. **服务探活**(`curl -sk https://127.0.0.1:39001/api/health` → `code: 0`)。
7. **MCP 探活**:`POST /ListChromePages` 看是否已有 page,清理残留;若无,`POST /NewChromePage {"url": "about:blank"}` 试启,确保 CDP 链路通畅。
8. **参考文档清单**(测试前 5 分钟必读清单,作为 UI 合理性审计的事实来源):
   - `docs/财商流游戏/已实现/01-产品设计/财商流游戏-P0产品与玩法骨架-v1.md`(玩法总纲、界面线框、人类操作)
   - `docs/财商流游戏/已实现/02-架构设计/财商流游戏-后端架构与经济引擎-v1.md`(月结 7 步、经济引擎、数值表)
   - `docs/财商流游戏/已实现/02-架构设计/财商流游戏-WS与HTTP协议契约-v1.md`(协议契约、错误码 350xx、动作语义表)
   - `docs/财商流游戏/已实现/02-架构设计/财商流游戏-前端架构与2.5D地图-v1.md`(前端布局、组件树、i18n 键、对比度约束)
   - `docs/财商流游戏/已实现/03-Agent设计/财商流游戏-WealthPlayer-Agent设计-v1.md`(Agent 行为预期、17 工具、月度决策循环)
   - `docs/财商流游戏/已实现/03-Agent设计/财商流游戏-职业卡与加载器设计-v1.md`(职业卡数据、精选 10 职业)
   - `docs/财商流游戏/已实现/04-验收/财商流游戏-P0验收清单-v1.md`(功能勾选表 F01–F33、经济不变量 I1–I10、前端/Agent 验收要点)
   - `docs/财商流游戏/财商流游戏规则与经济系统.md`(数值手册:五维资源、三表、收支、投资、市场周期、债务、税务)
   - `CLAUDE.md §26` 前端 UI 颜色对比度与可读性规范(暗色主题对比度硬阈值)

### 5. 大模型 API 异常与协议层诊断

- **触发**: 任一 Agent 调 LLM 失败 ≥ 2 次即启动协议诊断。
- **方式**: 调 MCP「查看 Anthropic 协议抓包数据」接口(或委派 SubAgent)。
- **重点排查**:
  - 请求侧: URL / 鉴权头 / `model` / `messages` 角色顺序 / `tools` `tool_use` `tool_result` 配对 / `max_tokens` 与上下文长度。
  - 响应侧: HTTP 状态码(400/401/429/5xx)/ `error.type|message` / `stop_reason` / SSE 是否截断 / `content` 中 `tool_use` 完整性。
  - 链路侧: TLS 握手 / 超时与连接重置 / 多模型协议适配差异(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米)。
- **结论落地**: 写入 `TestReport/财商流游戏协议抓包分析报告_*.md`;若属可修复缺陷,出具体优化建议(测试阶段**不自动改业务代码**,留待 §12 修复流程)。
- **与终止条件联动**: 2 次失败启动诊断并继续;累计 5 次失败且确认不可恢复,按 §3 终止。

### 6. 测试步骤

#### 6.1 准备

- 读 `test_account.json` 取账号;新注册账号也回写,避免重复注册。
- 启 MCP 服务,确认操作模拟 + 抓包两类能力均可用。
- **登录走真实验证码路径**(CLAUDE.md §21,2026-08-25 起 CAPTCHA 旁路已全删):
  1. `GET /api/captcha` 获取 `captcha_id` + 验证码图片
  2. 通过 MCP `LookChromePageInfo info=screenshot` 在验证码弹窗截屏 → 用 `OCR` 或人工字符识别(Agent 用 `eval_js` 读取图片像素并 OCR 是允许的只读行为,只要不绕过 captcha 校验逻辑)
  3. 携带 `captcha_id` / `captcha_answer` 正常登录
  - **严禁**尝试调用任何 `AgentBypassAccounts` / `AGENT_BYPASS_ACCOUNTS` 等已删除旁路;若 `cfg.Server.DevMode=true` 也**不**解锁任何认证旁路。
- 进度草稿记录当前阶段 + 测试模块 + 房间配置(month_ms / pool / seed)。

#### 6.2 大厅与页面遍历

- 模拟人类玩家遍历大厅与财商流游戏页所有可交互元素(按钮/链接/开关/输入框)。
- 结合最近 10 次提交判断是否有新/优功能。
- 每次点击后**等待页面完全加载**(推荐 800~1500ms)再走下一步。
- 未达预期项详细记录,写入最终报告。
- **遍历要点**:导航栏所有链接 / 左侧菜单「💰 财商流游戏」入口 / 房间列表卡片 / 筛选/排序控件 / 创建房间按钮 / 帮助/规则按钮。

#### 6.3 A 阶段:房间生命周期(核心流程)

**6.3.1 房间创建**
- 校验 `WealthCreateRoomModal` 创建弹窗各配置项渲染完整、可交互:
  - 房间名输入
  - Agent 座位配置(agent_seats 数量 / model_key 选择)
  - 游戏配置(month_ms 滑块/输入 / pool 选择 / seed 输入)
  - 创建/取消按钮
- 按 `month_ms:3000` + `pool:"curated"` + 1 人类 + 7 Agent 创建房间。
- 校验创建后房间状态为 `filling`,人类玩家占 0 号座,**职业卡展示正确**(职业名/头像/初始五维资源)。
- **agent_seats 去重随机化**: 7 bot 的 model_key 应尽量不同(§14.2 Fisher-Yates 洗牌),不一致写入报告。

**6.3.2 开局与座位**
- 房主点击「提前开始」(`game.wealth_start`),验证 `game.started` + 首个 `game.state`。
- <3 座时提前开始被拒(错误码 **35003**)。
- 验证开局聊天:Agent 通过 `SendFromBot` 发送职业卡 `opening_hook` 开场白(30–50 字)。

**6.3.3 月度节拍**
- 验证 `next_month_at` 倒计时 DOM 秒级刷新。
- 验证 `phase="acting"` 期间 12 个动作按钮可点击。
- 验证月结(`phase="settling"`)期间动作按钮 disabled。
- 验证 `game.month` 到达后 `FinancialPanel` 数字滚动、`LedgerPanel` 新增条目、`MonthTicker` 横滚更新。

**6.3.4 观战模式(独立用例)**
- 测试账号以**观战者**身份进入 Agent 对局,不占席位(`my_seat=-1`)。
- 验证观战者能实时看到 `game.month` 汇总帧 + Agent 公开发言。
- 验证观战者发送 `game.wealth_action` 被后端硬拒(错误码 **30011**)。
- 验证观战者**不能**看到其他玩家 `my.*` 私有信息(现金/贷款/资产详情)。
- 验证观战者能看到 `bot_contexts`(Agent 思考面板)。

**6.3.5 退出与断线恢复**
- 验证 `game.leave` 进行中转 bot 接管(有可用 provider)。
- 验证终局 60s 后自动 `game.removed`。
- 验证刷新 `/wealth/:roomId` → `game.state` 快照恢复全部面板,无状态丢失。

#### 6.4 B 阶段:经济引擎与月结

> **重要**: 经济引擎数值正确性以**服务端单元测试**为第一事实来源。前端测试聚焦**状态同步 + 渲染正确性**。若 `go test ./ServerGo/game/wealth/...` 全绿,则引擎数值可信;测试重点转为前端是否正确展示引擎产出。

**6.4.1 月结流程**
- 验证月结 7 步顺序(工资→被动收入→固定支出→税+社保→生活支出→债务→市场波动)。
- 验证个税计算:月薪 20000 → 社保 2100、个税 1170(《规则》§9.1 示例复算一致)——通过 `game.state.my.monthly.detail` 逐项核对。
- 验证年度调整(12 月):工资 +3%、通胀因子进位。
- 验证 Ledger 双式流水:每笔 `from/to/amount/category` 字段齐全,`from ≠ to`。

**6.4.2 经济不变量核对(通过前端 + REST)**

| 不变量 | 验证方式 |
|--------|---------|
| **I1 座位现金守恒** | `game.state.my.cash` 月变化 = Ledger `to=seat` − `from=seat` 求和 |
| **I4 资产负债恒等式** | `net_worth = cash + Σ资产市值 − Σ贷款余额`(DOM 数值复算) |
| **I6 FI 公式** | `fi_index = 被动收入/月总支出`(支出 0 → 2.0 封顶) |
| **I7 税费率** | 股票卖出的佣金 ≥5 元;房产不满五唯一 5%+2%;黄金 0.5% |

**6.4.3 市场与人生事件**
- 验证市场周期四阶段(recovery/boom/recession/depression)DOM 徽章切换。
- 验证 60 月钟声:市场周期重掷、人生事件触发(结婚💒/生育🍼/健康🏥/失业📉)。
- 验证失业:衰退 15%/萧条 25% 月度判定 → 工资归零 2–6 月。

**6.4.4 破产流程(独立房间验证)**
- 通过操作使现金连续 3 月 < 0,触发破产清算。
- 验证全屏公告「⚖️ X 号位触发破产清算」。
- 验证房产按 70% 强制变现、消费贷减 50% 分 36 期、信用分清零、停 3 月。
- 验证重组后月现金流仍负 → 出局(`alive=false`,token 移出地图)。

**6.4.5 终局与结局展示(独立房间验证)**
- 验证主时钟 60 岁 → `game.over` 三维评分到达。
- 验证 `GameOverModal`:评分条 + 结局徽章 + FI Index + 人生满意度 + 社会贡献 + 净资产曲线。
- 验证 6 种结局映射(≥85 财务自由·人生赢家 / 70–84 富足安稳 / 50–69 平淡度日 / 30–49 债务缠身 / <30 破产出局 / FI≥1.5 且满意度<40 孤独富翁)。

#### 6.5 C 阶段:人类操作

**6.5.1 12 种动作按钮验证**

逐项点击 `ActionPanel` 按钮,验证弹窗渲染 + 参数输入 + 提交 + `game.event{type:"action"}` 回执:

| 按钮 | 验证要点 |
|------|---------|
| 💵 买资产 | 类型选择 + 金额输入 + 现金扣减 Ledger |
| 📈 卖资产 | 持仓选择 + 份额 + 市值入账 − 手续费 |
| 🏠 买房 | 城区选择 + 首付比例(0.3–1.0) + 房贷生成 + 月供计算 |
| 🏦 贷款 | 种类选择(consumer/credit/business) + 金额档位校验 |
| 💳 还款 | loan_id 选择 + 提前还 ≥1 万校验 |
| 🛵 副业 | 种类选择 + 认知门槛校验 + 精力消耗 |
| 📚 学习 | 现金-2000 / 精力-1 / 认知+1 |
| 🤝 社交 | 现金-1000 / 人脉+1 |
| 😴 休息 | 精力+2 |
| ⚡ 加班 | 精力-2 / 当月工资×0.3 奖金 |
| 🚚 迁区 | 目标城区 + 搬家费 3000 + token 迁移动画 |
| 🛍 消费 | 金额 + 备注 + Ledger `consume` 条目 |

**6.5.2 动作预算与错误处理**
- 验证每月第 4 个动作 → 错误码 **35006**(ActionBudgetExhausted)。
- 验证 `phase="settling"` 期动作 → 错误码 **35004**(WrongPhase)。
- 验证停赛/出局者动作 → 错误码 **35005**(PlayerInactive)。
- 验证非法操作(如现金不足买房) → 错误码 **35007** + 弹窗内联红色错误条(§7.1 硬约束:**弹窗不关闭**,让用户就地重试)。

#### 6.6 D 阶段:Agent 驱动

**6.6.1 全 Agent 房(0 人类)**
- 创建 8 bot 房间 → 验证 8 bot 开局 → 跑完多个 tick 不卡死(watchdog 兜底)。
- 验证每月 Agent:≤3 动作 + ≤1 speak + submit_month;超时 20s 强制 submit。

**6.6.2 Agent 行为验证**
- **月度决策**:Agent 通过 17 工具执行动作,`bot_contexts.last_decision_summary` 可读(≤120 字)。
- **speak 发言**:经 `chatSvc.SendFromBot` 到达前端 `WealthGameChatPanel`,内容符合职业卡人设(保守/激进/平衡)。
- **记忆连贯性**:后续月份决策引用前期投资/事件,策略前后一致。
- **多模型驱动**:7 bot 接入不同 LLM(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米 等),调度正常;某模型异常时通过抓包定位。
- **公平性**:Agent 不得看到其他玩家 `my.*` 私有信息;验证 `bot_contexts` 仅本人座位 + 观战者可见。
- **开场白**:职业卡 `opening_hook` 开局播报。

**6.6.3 混合房**
- 人类 + Agent 同场,动作语义无分叉(同 `actions.go` 路径 + 同一套 350xx 校验)。

#### 6.7 E 阶段:前端渲染

**6.7.1 2.5D 地图**
- 验证 `WealthCityMap` r3f Canvas:8 城区楼群 + 道路 + Agent token。
- 验证 hover 城区显示房价/租金信息卡。
- 验证 `AgentToken` 圆柱 token + Billboard 头像 + 名牌。
- 验证 token 迁区 lerp 动画(连续 2~3 帧截图验证流畅)。
- 验证 `WealthMinimap` Canvas2D 小地图:城区色块 + agent 点 + 视野框。
- 验证楼群高度随 `price_index` 变化。

**6.7.2 面板栈**
- `FinancialPanel`:现金/月收支柱状/FI 仪表/五维资源条/资产负债 Tab。
- `MarketPanel`:股指/金价/债券利率/8 区房价指数。
- `LedgerPanel`:双式流水(最近 50 条)+ 月度汇总。
- `WealthGameChatPanel`:房间聊天面板(复用 5 游戏共用版 + 薄适配器)。

**6.7.3 MonthTicker 与 GameOverModal**
- `MonthTicker`:月度倒计时 + 市场数据 + 事件横滚。
- `GameOverModal`:评分条 + 结局徽章 + 净资产曲线 + 人生报告摘要。

**6.7.4 i18n**
- 验证 zh/en/ja 切换全键覆盖(缺键 tsc 报错——已通过编译门禁 G5 保证)。

#### 6.8 UI 合理性审计(强制章节)

> **本节为强制输出**。测试不仅是"功能通/不通",更要从**人类玩家的视角**评估 UI 布局合理性 + 交互设计合理性。所有审计结果独立成节写入报告,即便功能测试全部通过也要输出。

**6.8.1 布局合理性审计清单**

| 审计项 | 检查方法 | 通过标准 |
|---|---|---|
| **屏幕利用率** | 不同分辨率(1920×1080 / 1440×900 / 1280×720)截图比对 | 关键信息(当前月份 / 阶段 / 计时器 / 现金)不超出首屏 |
| **12 座位布局** | 12 人满员时各玩家信息卡是否挤压 | 玩家数增加时,卡片尺寸按比例缩放而非溢出 |
| **信息密度** | `eval_js` 取 `document.body.scrollHeight / window.innerHeight` | 单屏不出现 3 屏以上垂直滚动 |
| **响应式断点** | `viewport` 切换到 768 / 1280 / 1920 宽度 | 主面板不出现横向滚动条 / 关键按钮 ≥ 44px |
| **首屏关键信息** | 进入房间后立即 DOM 读取 | 当前月份 + 阶段 + 计时器 + 现金/净资产**全部首屏可见** |

**6.8.2 交互合理性审计清单**

| 审计项 | 检查方法 | 通过标准 |
|---|---|---|
| **点击区域大小** | 取所有按钮 `getBoundingClientRect` | 触摸目标 ≥ 44×44px(CLAUDE.md §23 token) |
| **焦点顺序** | 连续按 Tab,记录焦点元素 | 焦点流动符合视觉顺序,不出现焦点陷阱 |
| **键盘可达性** | 所有交互能否纯键盘完成 | 动作提交 / 关闭模态 / 结束本月 全部可纯键盘 |
| **悬停反馈** | hover 关键按钮 / 玩家卡片 / 城区 | 出现视觉变化(色/边框/阴影),且响应 ≤ 100ms |
| **加载状态** | 点击提交后立即 DOM 读取 | 出现 loading 占位或禁用按钮(防止重复提交) |
| **空状态** | 创建空房间 / 无对局场景 | 显示友好空态文案 + 引导按钮,不出现裸 DOM |
| **错误反馈** | 故意触发错误(如现金不足买房) | 弹窗内联红色错误条 + 顶部 toast,符合 CLAUDE.md §7.1 |
| **模态可关闭** | Esc / 点击遮罩 / 显式关闭按钮 | 三种方式至少两种可用,焦点正确回到触发元素 |

**6.8.3 可读性与对比度审计**

- **暗色主题**: 关键文本(玩家名 / 数值 / 发言 / 阶段标签)对比度 ≥ 4.5:1(正文 WCAG AA,CLAUDE.md §26.1)。
- **状态徽章**: 市场周期标签 / income-band / ending ≥ 5.0:1。
- **选中态**: 选中玩家 / 当前月份高亮 ≥ 6.0:1(选中/未选差异明显,CLAUDE.md §26.2 反模式 4)。
- **同色相陷阱**: 红底红字 / 绿底绿字 / 蓝底蓝字 一票否决(§26.2 反模式 1)。

**6.8.4 信息架构与可视化合理性**

- **2.5D 地图**: 8 城区布局是否清晰;楼群高度是否直观反映房价;token 名牌是否易读。
- **月结帧**: 数字滚动动画是否流畅;Ledger 新增条目是否醒目。
- **FinancialPanel**: 三表 Tab 切换是否直观;FI 仪表档位色是否一目了然。
- **MarketPanel**: 行情数据更新是否与月结同步;四阶段色环是否明显。
- **LedgerPanel**: 双式流水 from/to/category 是否一目了然;月度汇总是否清晰。
- **GameOverModal**: 三维评分 + 结局徽章 + 净资产曲线是否易读。

**6.8.5 UI 缺陷报告规范**

任何 UI 缺陷**必须**包含:
1. **缺陷分类**: 布局 / 交互 / 可达性 / 可读性 / 视觉一致性。
2. **复现步骤**: 具体分辨率 + 具体操作路径(可被复现)。
3. **截图证据**: `AutoTestScreenshots/财商流游戏_ui_<缺陷类型>_<时间戳>.png`。
4. **影响范围**: 单页面 / 全局 / 多分辨率。
5. **修复建议**: 具体可执行的修复方向(组件层 / CSS / 布局)。

### 7. 操作与覆盖要求

- **拟人操作(硬约束)**: 详见 §1.3。核心摘要:
  - 每步操作后**等待页面完全加载**(建议 800~1500ms)再走下一步。
  - **禁止**:`eval_js` 一键完成需拟人验证的整段动作;瞬间全点所有按钮;不读页面直接断言;绕过验证码校验逻辑。
  - **允许**:`eval_js` 只读状态快照(读取 `localStorage`、Zustand store 状态、`wealth.state` 字段、DOM 属性)。
- **DOM 优先(硬约束)**: 详见 §1.2。信息获取**必须**先尝试 DOM 读取,仅在视觉验证场景才用截图。
- **覆盖度**: 全面覆盖所有可交互元素,重点验证房间创建、12 种人类操作、月度节拍、三表面板、Ledger、Agent 行为、观战模式、**UI 合理性审计(§6.8)**七大核心模块。
- **客观性**: 严格基于 DOM 文本 / 页面截图 / 日志 / 协议抓包等可观测事实。
- **测试报告截图**: 每完成一个核心交互动作必须截图归档,供后续审计与缺陷对照。

### 8. 报告与后处理

1. 测试结束首先生成财商流游戏测试报告,完整保存(含本轮使用阶段 + 测试模块 + 测试焦点 + 测试结论 + **UI 合理性审计独立章节**)。报告必须包含 `未覆盖项继承与变化`节(对比上轮)和 `多轮收敛趋势`节。
2. 同步生成进度文件,必备字段:
   - `当前阶段`(A / B / C / D / E)+ `本轮测试模块` + `本轮测试焦点`
   - `本轮测试结论`(`通过` / `存在缺陷(附摘要)` / `中断未完成`)
   - `已测组合清单` / `待测组合清单` / `下一轮建议组合`
   - `未覆盖项跟踪表`(继承上轮 + 本轮新增,含状态与积压标注)
   - `本轮重点突破项`(1-3 项) + `突破结果`
   - **`本轮 UI 合理性审计结论`**:布局合理性 / 交互合理性 / 可达性 / 可读性,各评级为 `✅ 达标` / `⚠️ 部分达标` / `❌ 未达标`,并在报告正文展开。
3. **报告文件名前缀必须为 `财商流游戏自动化测试报告_`**(shell 接力预检 `auto_run_common.sh::GAME_GLOBS` 按此前缀扫描);报告 + 进度文件确认落盘即可——**严禁 Agent 执行任何 git 写操作**:测试产物已被 `.gitignore` 忽略,不入库、不提交。
4. **报告落盘后立即进入「自动修复流程」(§12)**——在同一 Agent 会话内完成缺陷分析、代码修复、构建验证、提交推送，无需外部脚本接力。
5. MCP 工具异常导致终止时,**额外**在 `go-web-debug-tool/UseReport/财商流游戏测试工具使用报告_*.md` 写工具报告。
6. 触发协议层诊断时,**额外**写 `TestReport/财商流游戏协议抓包分析报告_*.md`(API Key/Token/Cookie 脱敏),并在测试报告里引用。
7. 中断未完成也要写进度,标 `中断未完成`。

### 9. 注意事项

- **多 Agent 并发与 Git**: 测试期间留意其他开发 Agent;测试阶段**仅做只读 Git 查询**(`git status` / `git log` / `git diff`),**禁止** `git add` / `git commit` / `git push` 等一切写操作——测试产物按 `.gitignore` 策略仅留本地,不入库、不提交。**进入 §12 修复流程后按该章节 Git 规则执行**。
- **单轮单模块**: 硬性约束,禁止「提高效率」连测多个模块;全模块覆盖靠多轮推进。
- **长对局处理**: 单局 420 月全流程漫长,测试一律 `month_ms:3000` + 分模块验证,不跑满 420 月。破产/终局独立建房间验证。
- **经济引擎优先相信单测**: `go test ./ServerGo/game/wealth/...` 全绿则数值可信;前端聚焦状态同步与渲染。
- **重大问题立即停**(§3 已列):不待页面超时,不绕路,立刻写报告 + 进入修复流程。
- **断线重连**: 验证服务器重启 / 客户端断网 场景下重连有效性。
- **公平性与健壮性**: 校验 Agent 不得违规获取非公开信息(含其他玩家 `my.*` 私有数据);网络波动重试 / 异常降级 / 重连。
- **大模型稳定性**: Agent 多次调 LLM 失败不得直接判业务缺陷,先协议层抓包定位再下结论。
- **数据安全**: 抓包数据严禁明文输出 API Key / Token / Cookie。
- **验证码**: 严格走 `GET /api/captcha` 真实路径,**严禁**尝试调用任何已删除旁路。
- **优化建议**: 报告须给具体可执行建议(system prompt 迭代 / 工具定义补充 / 经济引擎调优 / Agent 决策策略增强 / **UI 布局与交互修复** / 前端修复 / 协议重试策略)。

### 10. 主流程阻塞时的降级测试路径

> 当 §6.3-6.7 主流程被阻塞(月度 tick 不动 / Agent 全员静默 / 创建房间失败)时,**不要立即整轮终止**,按以下降级路径继续采集可观测数据。

#### 10.1 第一层:REST API 快照

- `GET /api/games/wealth/rooms` — 开放房间列表
- `GET /api/games/wealth/rooms/:id` — 房间详情
- `GET /api/llm/models` — 验证 8 个模型注册且 key 可用(排查 Agent 静默根因)
- `GET /api/admin/llm/providers` — Provider 可用性
- **权威状态(phase/month/players)**:通过以下只读方式:
  1. 前端页面 React fiber / zustand store(MCP `eval_js`,只读);
  2. 或走 §10.2 服务端日志;
  3. 或 §10.3 DB 直查。

**判定**: 若 fiber/日志中的 `phase` 与预期不符(如一直停在 `acting` 不推进),则月度 tick 路径有问题;若 phase 正常推进但 WS 不推送,则是 WS 分发层问题。

#### 10.2 第二层:服务端日志诊断

通过 `journalctl` / 服务日志观察:
- `wealth game started` 后是否有 `agent started` × N
- `LLM call` / `stream` 相关日志
- `month` 推进日志
- `settlement` 相关日志

#### 10.3 第三层:DB 直查

```sql
-- 验证 bot user
SELECT COUNT(*) FROM t_lsm_game_user WHERE IsBot=1 AND username LIKE 'wealth_%';
-- 验证 provider 是否注册且 key 非占位
SELECT id, model, provider_type, LENGTH(api_key_enc) FROM t_lsm_game_llm_provider;
-- 验证房间 DB 状态(若落库)
SELECT id, status, current_count FROM t_lsm_game_room WHERE game_kind='wealth' ORDER BY id DESC LIMIT 5;
```

#### 10.4 降级路径产出要求

即使主流程完全阻塞,降级路径采集的数据也必须写入报告:
- **REST 快照** → 「诊断数据与证据链」
- **日志统计** → 「服务端日志统计」
- **DB 直查** → 「诊断数据与证据链」

**禁止**: 主流程阻塞后直接终止、报告只写「全部未覆盖」而无诊断数据。

### 11. 多轮迭代策略与未覆盖项滚转

> 核心理念:每轮测试不可能覆盖所有功能,但**未覆盖项必须跨轮滚转、逐轮收敛**。

#### 11.1 未覆盖项滚转规则

1. **每轮进度文件必须包含「未覆盖项跟踪表」**,格式:优先级 / 未覆盖项 / 状态 / 备注。
2. **状态三态**: `❌ 未覆盖` → `⚠️ 进行中` → `✅ 已覆盖`。
3. **上轮 `❌ 未覆盖` 自动继承到本轮**,除非有明确理由移除。
4. **连续 N 轮未覆盖的条目自动升级**:
   - 连续 2 轮 `❌` → 在备注中标注 `⬆️ 积压 2 轮`。
   - 连续 3 轮及以上 `❌` → 必须在本轮作为重点突破项。
5. **新发现的未覆盖项**以 `NEW` 标记追加到表尾。

#### 11.2 每轮重点突破项选取规则

从「未覆盖项跟踪表」中,按以下优先级选取 **1-3 项**作为本轮重点:
1. **积压最久**(连续 `❌` 轮数最多)的条目优先。
2. **与本轮模块强相关**的条目优先(如本轮测 economy → 优先突破破产流程)。
3. **依赖本轮已具备条件的条目** 次优。
4. **与最近提交变更相关的条目**最后。

#### 11.3 模块依赖型未覆盖项的处理

当未覆盖项依赖特定场景(如破产流程、终局评分)但本轮未触达时:
1. **不跳过**: 记录为 `❌ 未覆盖(场景未触达)` 并继承到下轮。
2. **下轮策略调整**: 若连续 2 轮未触达目标场景,本轮单独建房间验证该场景。

#### 11.4 进度文件新增必备字段

- `当前阶段`(A / B / C / D / E)
- `本轮测试模块`(lifecycle / economy / actions / agent / render)
- `本轮重点突破项`(1-3 项)
- `突破结果`(每项: `✅ 已突破` / `⚠️ 部分突破(附说明)` / `❌ 未突破(附原因)`)
- `积压升级标注`(连续 ≥ 2 轮未覆盖的条目标注 `⬆️ 积压 N 轮`)
- **`本轮 UI 合理性审计结论`**(各维度评级 + 缺陷计数)

#### 11.5 报告新增必备字段

- `未覆盖项继承与变化`节:对比上轮未覆盖项,列出本轮新增/已覆盖/仍继承的条目。
- `多轮收敛趋势`节:简述整体覆盖度进展(如「6 轮完成,已覆盖 X/Y 项,剩余 Z 项含 N 项积压 ≥ 2 轮」)。
- **`UI 合理性审计`独立章节**(§6.8 五维度展开:布局 / 交互 / 可达性 / 可读性 / 信息架构)。

#### 11.6 截图归档规范

- **路径**: `AutoTestScreenshots/财商流游戏_<页面或模块>_<动作或缺陷>_<YYYYMMDD_HHMMSS>.png`
- **覆盖动作**: 每个核心交互步骤至少 1 张;UI 缺陷必须含复现截图;月结 / token 迁移连续 2~3 帧。
- **报告引用**: 报告中以相对路径引用(`AutoTestScreenshots/xxx.png`),便于读者直接定位证据。

---

## 12. 自动修复流程（测试完成后执行）

> **本章节在测试报告 + 进度文件落盘后自动执行**。Agent 完成测试并写入报告后，**立即**进入以下修复流程，无需外部脚本接力。

### 12.1 硬约束（不可违反）

1. **绝对禁止修改 `CLAUDE.md`、`AGENTS.md` 这两个规则文件。**
   - 修复摘要 / 版本基线 / commit hash / 教训只写到旁路文档(如 `TestReport/<BugID>_validation.md` 或对应 `docs/` 归档),**绝不写入规则文件**。
2. **每一份被处理的报告文件都必须有明确归宿**:
   - **A. 有问题已修复并推送成功** → **删除**该报告文件。
   - **B. 经核查无问题** → **就地重命名**,追加 `_无问题` 后缀。
   - **C. 有问题但构建/重启/验证失败** → **保留原文件名不变**,等待人工介入。

### 12.2 报告检索(≤ 2 min)

- **主工程**: 在 `TestReport/` 下按**财商流游戏 glob** 检索**生成时间最早、文件名不含 `_无问题` 后缀**的报告。
  - 财商流游戏相关 glob:
    - `财商流游戏自动化测试报告_*.md`(主报告)
    - `财商流游戏协议抓包分析报告_*.md`(协议报告)
- **子工程**: 在 `go-web-debug-tool/UseReport/` 下检索:
  - `财商流游戏测试工具使用报告_*.md`(工具报告)
- 无任何待处理报告 → 静默结束修复流程。

### 12.3 缺陷分析与代码修复(≤ 12 min)

- 按报告 `Bug ID` + 堆栈 + `ERROR` 日志,在对应代码库定位问题。
- **动手改代码前必须先确认缺陷当前是否仍然存在**:`git log --oneline -10 -- <file>` 看相关文件近期提交、比对当前实现与报告中堆栈。
- **根因分析**: 从逻辑底层修复,避免仅处理表面;核心逻辑遵循 `CLAUDE.md` 架构约束(仅**读取**,绝不修改)。
- 核查后确认问题**均已不存在或非有效缺陷** → 不改任何代码,直跳 §12.6 分支 B。
- 修复后必跑 `go test ./...`(Go) / `npm run build`(前端),确保本地编译与单测通过。

### 12.4 构建重启与回归验证(≤ 8 min)

- 主工程跑 `./rebuild_restart_app.sh` 重新编译 + 重启服务。
- `go-web-debug-tool` 子工程独立编译验证。
- 回归测试统一从 `test_account.json` 读账号,执行冒烟测试,核心流程无阻塞。
- **本步骤仅在「实际改了代码」时执行**;分支 B 跳过。

### 12.5 文档同步(仅旁路,≤ 2 min)

- 修复涉及架构调整 / API 变更 / 新增常量 → **只**更新业务/技术文档(`docs/*.md`)与代码内注释。
- **禁止**追加、删除、修改 `CLAUDE.md` / `AGENTS.md`。
- 修复摘要 / commit hash / 回归结果可写 `TestReport/<BugID>_validation.md` 作为本轮验证记录。
- **本步骤仅在「实际改了代码」时执行**;分支 B 跳过。

### 12.6 Git 提交推送与报告归档(≤ 1 min)

按本轮实际结果,**三选一**:

**分支 A: 有问题已修复并验证通过**
- 主工程 + `go-web-debug-tool` 子工程分别 Git 提交,注释:
  `修复: [BugID] <问题简述> (关联报告: YYYYMMDD_HHMMSS)`
- 提交成功后自动 `git push` 同步远程。
- **推送完成后立即删除**已处理的对应报告文件。

**分支 B: 无问题(无需改代码、无新提交)**
- 报告文件**就地重命名**,追加 `_无问题` 后缀。
- **不删除文件、不代码提交推送**。

**分支 C: 有问题但构建/重启/验证失败**
- **保留原报告文件名不变**,不得删除、不得重命名。
- 记录错误日志,等待人工介入。

### 12.7 约束与监控

- **总时长硬上限 25 分钟**: 超时强制终止,报告保留原名。
- **子阶段时间盒**: 检索 ≤ 2 min / 修复 ≤ 12 min / 构建 ≤ 8 min / 文档 ≤ 2 min / 归档 ≤ 1 min。
- **错误处理**: 构建/重启/验证阶段失败 → 保留报告原名,等人工排查。
- **规则文件守门**: 任何「更新 `CLAUDE.md`」冲动,先停下来确认——本流程明确禁止。
- **Git 推送权限**: 默认 `git push` 同步远程;若远程不可达,记录错误,本轮走分支 C(报告保留),等人工介入。
