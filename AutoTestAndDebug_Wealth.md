## 自动化 Web 游戏测试任务：虚拟城市 —— 全 Agent 真实经济模拟专项

> 本项目按 [MIT License](LICENSE) 开源。测试任务面向维护者，参与前请阅读 [CONTRIBUTING.md](CONTRIBUTING.md)。
> 本文件为虚拟城市专用入口，**仅产出** `虚拟城市自动化测试报告_YYYYMMDD_HHMMSS.md` 前缀的报告。

- **工作目录**: `/usr/local/LsmAgentGame/LsmAgentGame`
- **入口脚本**: `AutoTestAndDebug_Wealth.sh`(随机选编程 Agent CLI → 跑测试 → 报告落盘 → **同一 Agent 会话内**自动进入修复流程)
- **产物路径(仅虚拟城市,glob 集中到 `auto_run_common.sh::GAME_GLOBS`)**:
  - 测试报告: `TestReport/虚拟城市自动化测试报告_YYYYMMDD_HHMMSS.md`(单一 glob,无兼容旧文件)
  - 进度文件: `AutoTestProgress/虚拟城市自动化测试进度_YYYYMMDD_HHMMSS.md`
  - 子工程工具报告(可选): `go-web-debug-tool/UseReport/虚拟城市测试工具使用报告_YYYYMMDD_HHMMSS.md`
  - 协议抓包分析报告(按需): `TestReport/虚拟城市协议抓包分析报告_YYYYMMDD_HHMMSS.md`
  - 文件名时间戳统一 `YYYYMMDD_HHMMSS`,精度到秒
  - **Glob 单一事实源**: 所有扫描 glob 在 `auto_run_common.sh::GAME_GLOBS` 中声明,新增游戏仅改公共库一处,本文件不再内嵌 glob 列表。
   - **测试产物一律不入库**: `TestReport/*` / `AutoTestProgress/` / `AutoTestScreenshots/` 已被 `.gitignore` 整目录忽略,所有测试生成文件(报告/进度/截图/临时文档)仅留本地,Agent **禁止**对其执行 `git add` / `git commit` / `git push`。
- **执行模式**: 主 Agent 跑核心流程;遇代码读取/协议抓包/数据查询可按需委派 SubAgent,不阻塞主流程。
- **执行策略(本版本硬约束)**:
  - **全 Agent 房间**: 虚拟城市 = **12 个 Agent 全自动、0 人类玩家** 的 12 座经济社会。本测试**完全不涉及人类玩家**——无人类登录、无人类操作、无人类验证码。12 名 Agent 各自代表一个"社会个体"，构成完整闭环的微型经济社会。
  - **完全模拟真实世界的财富流动**: 测试核心目标是验证 12 Agent 构成的微型经济社会能否**涌现出现实世界的经济现象**——财富的生产/分配/交换/消费循环、阶层流动、贫富分化、经济周期、泡沫与崩溃、宏观调控传导等。经济引擎数值正确性以**服务端单元测试**为事实来源，本测试聚焦**宏观涌现行为 + 经济系统闭环正确性**。
  - **模拟真实世界的经济行为（三个层次）**:
    1. **个体经济行为（某一个人）**: 单个 Agent 的消费/储蓄/投资/借贷/创业/学习/副业/加班/休息/社交/迁区等月度决策是否符合理性经济人假设；是否体现真实人类的行为偏差（损失厌恶、过度自信、锚定效应、羊群效应）；是否根据自身职业/资产/生命周期阶段调整策略。
    2. **群体博弈行为（一群人）**: 多名 Agent 之间是否涌现出市场博弈行为——竞价买房推高房价、恐慌性抛售资产压低市价、合谋坐庄、信息不对称下的逆向选择；是否出现真实市场的"看不见的手"自发调节。
    3. **社会群体行为（社会层面）**: 12 Agent 整体是否涌现出宏观经济现象——财富分布服从幂律（少数人掌握多数财富）、经济周期四阶段自然轮动、失业率随周期波动、房地产市场泡沫与崩盘、信贷扩张与收缩、贫富差距随时间拉大、社会流动性（穷人翻身/富人跌落）。
  - **月结驱动**: 单局 420 月(25→60 岁)全流程过于漫长,测试采用**小月时长**(`month_ms:3000`)+ **多房间分模块验证**策略:每个模块建独立房间,验证核心路径后即可结束,**不需要**单房间跑满 420 月。宏观涌现验证需**连续跑足够多月**(≥60 月)以观察趋势，可用 `month_ms:1000` 加速。
  - **破产/终局专项**: 破产流程、终局评分、6 种结局需安排**独立房间**验证(可通过调整 seed 或操作触发特定路径,**严禁**用 `eval_js` 直接改状态绕过游戏逻辑)。
  - **重大问题/卡住/严重不及预期 → 立即终止本轮**: 不等待超时,**立刻**生成问题报告 + 进入 §12 自动修复流程,**严禁强行绕过继续推流程**。
  - **UI/UX 合理性审计**: 测试不仅是"功能通/不通",还必须评估**布局合理性、交互合理性、可达性、可读性**——把发现的视觉/交互问题作为本轮报告的「UI 合理性审计」章节独立输出。
- **核心目标**: 验证 12 Agent 构成的微型经济社会是否能够**完全模拟真实世界的财富流动**——(1) 个体层：单 Agent 经济决策是否拟人且理性；(2) 群体层：Agent 间是否涌现市场博弈与自发秩序；(3) 社会层：宏观经济现象（财富分布、经济周期、阶层流动、泡沫崩溃）是否自然涌现；(4) 系统层：Ledger 双式守恒、资产负债恒等、税费率正确性等经济不变量始终成立。并对 UI/UX 设计做合理性评估（观战者视角观察 12 Agent 社会运行）。
- **代码变更限制**: 测试阶段业务代码只读;测试完成后进入「自动修复流程」时可修改代码修复缺陷(遵循 §12 约束)。

### 1. 核心测试工具(MCP)

主工具是 `go-web-debug-tool`(MCP 服务,默认 `http://localhost:28999`),接口定义以
`/usr/local/LsmAgentGame/LsmAgentGame/go-web-debug-tool/MCP_Proc_Def.md` 为唯一事实来源。
**测试 Agent 必须将其作为唯一操作入口**,**严禁**直接调业务 HTTP 接口(`curl` REST 仅允许只读查询,见 §10 降级路径)。
测试的所有操作——创建 Agent 房间、以观战者视角观察 12 Agent 社会运行、抓取宏观数据——**必须**通过 MCP 的 `ControlChromePage` 模拟完成,**禁止**用 `curl` / `fetch` / `axios` 等直接调业务 API 绕过 Web 界面。

#### 1.1 接口能力与典型用法

| 接口 | 用途 | 全 Agent 模拟场景示例 |
|------|------|--------------|
| `POST /NewChromePage` | 打开新标签页 | `{"url":"https://127.0.0.1:39001/wealth"}` |
| `POST /ControlChromePage` | 模拟观战者动作 | `click`(创建全 Agent 房间) / `scroll`(浏览面板) / `hover`(查看 Agent 信息) |
| `POST /LookChromePageInfo` | 读取页面状态 | `screenshot`(宏观概览) / `dom`(经济数据) / `console`(Agent 决策日志) / `network`(WS 帧) / `accessibility`(可达性) |
| `POST /CloseChromePage` | 关闭标签页 | 测试结束后清理 |
| `POST /ListChromePages` | 枚举已开页面 | 会话开始 / 失联恢复 |

#### 1.2 DOM 优先、截图兜底策略(硬约束)

> 信息获取**必须**按以下优先级递降,**禁止**跳过高层直接走截图:

| 优先级 | 方式 | 适用场景 | 典型调用 |
|--------|------|---------|---------|
| 1(首选) | **DOM 文本读取** | 宏观财富分布/阶段标签/Agent 发言/Ledger 条目/市场周期/失业率/股价 | `LookChromePageInfo info=dom` |
| 2 | **DOM 属性读取** | 元素尺寸/位置/可见性/禁用态/对比度 | `LookChromePageInfo info=dom` + `eval_js`(只读) |
| 3 | **网络请求读取** | API 调用是否成功/WS 帧是否到达/接口返回值 | `LookChromePageInfo info=network` |
| 4 | **控制台日志读取** | 报错信息/Agent 决策日志/LLM 调用统计 | `LookChromePageInfo info=console` |
| 5(兜底) | **截图** | 视觉验证(2.5D 地图 token 分布/宏观面板/动画过渡态) | `LookChromePageInfo info=screenshot` |

**原则**:
- **DOM 能回答的问题不截图** — 验证「当前月份/阶段」读阶段标签文字;验证「现金/净资产」读 DOM 数值;验证「市场周期」读徽章;验证「Ledger 条目」读文本节点;验证「Agent 发言」读聊天 DOM;验证「财富分布」读 12 座位资产柱。
- **截图仅用于** 视觉层面验证 — 2.5D 地图 token 分布/宏观面板布局/动画过渡态/颜色对比度/整体美观度。
- **复杂动画** (token 迁区 lerp、月结数字滚动、资产曲线) 必须连续截 2~3 帧(只截一帧可能错过过渡态)。
- **布局审计必须用 `accessibility` info**(role tree / contrast 提示 / focus order)评估键盘可达性。
- **优先用语义选择器**(`role` / `aria-label` / `data-testid` / 文本内容)而非 CSS class(类名易因重构失效)。

#### 1.3 操作硬约束

- **观战者视角操作**: 测试以**观战者 spectator** 身份进入全 Agent 房间观察 12 Agent 社会运行,不占席位(`my_seat=-1`)。创建房间时配置 `agent_seats: 12` + 0 人类。
- **宏观数据采集节奏**: 每月结(`settling`)期间采集一次宏观快照(12 Agent 的资产/现金/负债/职业/发言),**禁止**在 `acting` 期高频轮询干扰 Agent 决策。
- **`scroll` 使用 `behavior: "smooth"`** 模拟真人滚动。
- **`eval_js` 仅允许用于只读行为**:
  - 读取 `localStorage` / `sessionStorage` 状态
  - 读取 zustand store 快照
  - 读取 DOM 属性(`getBoundingClientRect` / 颜色计算 / 滚动位置)
  - 读取 `game.state` / `bot_contexts` 宏观数据
  - **禁止** 用 `eval_js` 一键创建房间、跳过月份、模拟 LLM 响应、批量改经济状态。

### 2. 虚拟城市模块与测试矩阵

#### 2.1 游戏总纲

| 维度 | 值 |
|------|-----|
| game_kind | `wealth` |
| 中文名 | 虚拟城市 |
| 房间座位 | 12 座(10–12 可玩) |
| 节奏 | 实时月度 tick(默认 8000ms/月,宏观涌现测试用 1000ms,快速功能测试用 3000ms) |
| 人类操作 | **无**(本测试为 0 人类全自动) |
| Agent 操作 | 17 工具(≤3 动作 + ≤1 speak + submit_month)/月 × 12 Agent |
| 终局 | 主时钟 60 岁(420 月),FI + 人生满意度 + 社会贡献三维评分,6 种结局 |
| 经济闭环 | 工资→被动收入→固定支出→税+社保→生活支出→债务→市场波动(月结 7 步) |

#### 2.2 测试模块总览

| 模块 | 代号 | 核心验证目标 | 对应验收项 |
|------|------|-------------|-----------|
| **A 全 Agent 房间生命周期** | lifecycle | 全 Agent 建房(12 bot / 0 人类)、开局、月度 tick、观战者观察、断线恢复、终局 | F01–F08 |
| **B 经济引擎与月结** | economy | 月结 7 步、个税、市场漂移、失业、破产、Ledger 双式守恒、资产负债恒等 | F09–F18 / I1–I10 |
| **C 个体经济行为模拟** | individual | 单 Agent 月度决策合理性、行为偏差(损失厌恶/过度自信/羊群效应)、职业卡一致性、生命周期策略调整 | F19–F22 / E1–E5 |
| **D 群体博弈与社会涌现** | emergence | 多 Agent 市场博弈(竞价/抛售/坐庄)、自发秩序、宏观经济涌现(财富幂律/经济周期/阶层流动/泡沫崩溃) | F23–F27 / M1–M8 |
| **E 前端渲染(观战者视角)** | render | 2.5D 地图 12 token、三表面板、Ledger、Market、GameOverModal、i18n、Agent 思考面板 | F28–F33 |

#### 2.3 测试矩阵(硬约束)

- **A 阶段**: 全 Agent 房间生命周期(1 个房间验证 F01–F08 基础流程,观战者视角)。
- **B 阶段**: 经济引擎(多个小房间分别验证月结、失业、破产、终局场景,核对经济不变量)。
- **C 阶段**: 个体经济行为(观察单个 Agent ≥30 月的决策序列,验证拟人度与行为偏差)。
- **D 阶段**: 群体博弈与社会涌现(≥60 月长跑,采集宏观时间序列,验证经济现象涌现)。
- **E 阶段**: 前端渲染(2.5D 地图 12 token 分布、三表面板、Ledger、Market、GameOverModal、i18n)。
- 每个阶段可拆分为多轮,每轮聚焦一个子目标。

#### 2.4 进度文件必含字段

- `当前阶段`(A / B / C / D / E)
- `本轮测试模块`(lifecycle / economy / individual / emergence / render)
- `本轮测试焦点`(具体验证点)
- `已测组合清单` / `待测组合清单` / `下一轮建议组合`
- `未覆盖项跟踪表`(继承上轮 + 本轮新增)
- `本轮重点突破项`(1-3 项) + `突破结果`
- **`本轮宏观涌现评估`**(仅 D 阶段):财富分布基尼系数趋势、经济周期轮动、阶层流动性评级

### 3. 终止条件(立即停)

1. **重大缺陷 / 页面崩溃 / 卡死 / 规则自相矛盾** → 终止**当前用例**,出报告,**不进入下一阶段用例**。
2. 经济引擎核心逻辑错误(Ledger 不守恒、资产负债不等、税费率错误) → **整轮终止**。
3. MCP 工具严重异常无法支撑测试 → **整轮终止**,并写子工程 `UseReport/虚拟城市测试工具使用报告_*.md`。
4. Agent 调 LLM **连续 5 次失败**,协议诊断确认不可恢复 → **整轮终止**。
5. **宏观涌现严重不及预期**(12 Agent 决策完全同质化、市场周期不轮动、财富分布无分化、Agent 全员无发言/乱码)→ **本轮立即终止**,不待超时,出报告。
6. **经济不变量破坏**(Ledger 双式不等、资产负债恒等式不成立、税费率偏离规则)→ **本轮立即终止**,出经济引擎审计报告。
7. **UI 严重不可达**(关键按钮无法聚焦、模态无法关闭、布局溢出导致内容不可见、对比度严重低于 WCAG AA)→ **本轮立即终止**,出 UI 合理性审计报告 + 调用修复。
8. 无论正常结束或异常终止,均须按规范出报告 + 进度文件;中断的进度必须标 `中断未完成`。

### 4. 启动前置

1. 读最近 10 次**非缺陷修复类**提交(`git log --oneline -10 -- ':!*fix' ':!*BUG'` 等过滤),梳理新增/优化项作为本轮重点。
2. 读 `AutoTestProgress/虚拟城市自动化测试进度_*.md` 最新一份,沿用其「已测 / 待测」清单与「未覆盖项跟踪表」。
3. **敲定本阶段**:`当前阶段=A / B / C / D / E`,决定测试模块与焦点,写入进度草稿。
4. 读 `go-web-debug-tool/MCP_Proc_Def.md` 确认 MCP 操作/抓包接口可用。
5. **服务探活**(`curl -sk https://127.0.0.1:39001/api/health` → `code: 0`)。
6. **MCP 探活**:`POST /ListChromePages` 看是否已有 page,清理残留;若无,`POST /NewChromePage {"url": "about:blank"}` 试启,确保 CDP 链路通畅。
7. **LLM Provider 可用性探测**:`GET /api/llm/models` 验证 ≥8 个模型注册且 key 可用(12 Agent 需 8+ 不同模型,按 §14.2 去重随机分配)。
8. **参考文档清单**(测试前 5 分钟必读清单,作为 UI 合理性审计 + 经济模拟的事实来源):
   - `lag_docs/虚拟城市/已实现/01-产品设计/虚拟城市-P0产品与玩法骨架-v1.md`(玩法总纲、界面线框)
   - `lag_docs/虚拟城市/已实现/02-架构设计/虚拟城市-后端架构与经济引擎-v1.md`(月结 7 步、经济引擎、数值表)
   - `lag_docs/虚拟城市/已实现/02-架构设计/虚拟城市-WS与HTTP协议契约-v1.md`(协议契约、错误码 350xx、动作语义表)
   - `lag_docs/虚拟城市/已实现/02-架构设计/虚拟城市-前端架构与2.5D地图-v1.md`(前端布局、组件树、i18n 键、对比度约束)
   - `lag_docs/虚拟城市/已实现/03-Agent设计/虚拟城市-WealthPlayer-Agent设计-v1.md`(Agent 行为预期、17 工具、月度决策循环)
   - `lag_docs/虚拟城市/已实现/03-Agent设计/虚拟城市-职业卡与加载器设计-v1.md`(职业卡数据、精选 10 职业)
   - `lag_docs/虚拟城市/已实现/04-验收/虚拟城市-P0验收清单-v1.md`(功能勾选表 F01–F33、经济不变量 I1–I10、前端/Agent 验收要点)
   - `lag_docs/虚拟城市/虚拟城市规则与经济系统.md`(数值手册:五维资源、三表、收支、投资、市场周期、债务、税务)
   - `CLAUDE.md §26` 前端 UI 颜色对比度与可读性规范(暗色主题对比度硬阈值)

### 5. 大模型 API 异常与协议层诊断

- **触发**: 任一 Agent 调 LLM 失败 ≥ 2 次即启动协议诊断。
- **方式**: 调 MCP「查看 Anthropic 协议抓包数据」接口(或委派 SubAgent)。
- **重点排查**:
  - 请求侧: URL / 鉴权头 / `model` / `messages` 角色顺序 / `tools` `tool_use` `tool_result` 配对 / `max_tokens` 与上下文长度。
  - 响应侧: HTTP 状态码(400/401/429/5xx)/ `error.type|message` / `stop_reason` / SSE 是否截断 / `content` 中 `tool_use` 完整性。
  - 链路侧: TLS 握手 / 超时与连接重置 / 多模型协议适配差异(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米)。
- **结论落地**: 写入 `TestReport/虚拟城市协议抓包分析报告_*.md`;若属可修复缺陷,出具体优化建议(测试阶段**不自动改业务代码**,留待 §12 修复流程)。
- **与终止条件联动**: 2 次失败启动诊断并继续;累计 5 次失败且确认不可恢复,按 §3 终止。

### 6. 测试步骤

#### 6.1 准备

- 启 MCP 服务,确认操作模拟 + 抓包两类能力均可用。
- 进度草稿记录当前阶段 + 测试模块 + 房间配置(month_ms / pool / seed / agent_seats 数量)。
- **无需人类登录/验证码**: 本测试为全 Agent 房,创建房间后即以**观战者 spectator**身份观察,无需人类账号登录。

#### 6.2 大厅与页面遍历(观战者视角)

- 模拟观战者遍历大厅与虚拟城市页所有可交互元素(按钮/链接/开关/输入框)。
- 结合最近 10 次提交判断是否有新/优功能。
- 每次点击后**等待页面完全加载**(推荐 800~1500ms)再走下一步。
- 未达预期项详细记录,写入最终报告。
- **遍历要点**:导航栏所有链接 / 左侧菜单「💰 虚拟城市」入口 / 房间列表卡片 / 筛选/排序控件 / 创建房间按钮 / 帮助/规则按钮。

#### 6.3 A 阶段:全 Agent 房间生命周期(观战者视角)

**6.3.1 全 Agent 房间创建(12 bot / 0 人类)**
- 校验 `WealthCreateRoomModal` 创建弹窗各配置项渲染完整、可交互:
  - 房间名输入
  - Agent 座位配置(`agent_seats: 12`, model_key 列表)
  - 游戏配置(month_ms 滑块/输入 / pool 选择 / seed 输入)
  - 创建/取消按钮
- 按 `month_ms:3000` + `pool:"curated"` + `agent_seats: 12`(0 人类)创建全 Agent 房间。
- 校验创建后房间状态为 `filling` → 12 bot 自动入座。
- **agent_seats 去重随机化**: 12 bot 的 model_key 应尽量不同(§14.2 Fisher-Yates 洗牌),不一致写入报告。验证 ≥8 个不同模型被使用。
- 以**观战者 spectator**身份进入房间(`my_seat=-1`),验证可观战 12 Agent 对局。

**6.3.2 开局与座位**
- 12 bot 到齐后自动开局(或房主点击「提前开始」`game.wealth_start`),验证 `game.started` + 首个 `game.state`。
- 验证开局聊天:12 Agent 通过 `SendFromBot` 发送职业卡 `opening_hook` 开场白(30–50 字)。
- 验证 12 个座位职业卡展示正确(职业名/头像/初始五维资源),**职业分布多样化**(不应 12 人同一职业)。

**6.3.3 月度节拍(全自动)**
- 验证 `next_month_at` 倒计时 DOM 秒级刷新。
- 验证 `phase="acting"` 期间 12 个 Agent 同时/依次执行月度决策(无需人类操作)。
- 验证月结(`phase="settling"`)期间 12 人同时 submit_month。
- 验证 `game.month` 到达后 `FinancialPanel` 数字滚动、`LedgerPanel` 新增 12 条(每人一条)流水、`MonthTicker` 横滚更新。

**6.3.4 观战模式(本测试主视角)**
- 验证观战者能实时看到 `game.month` 汇总帧 + 12 Agent 公开发言 + 市场数据。
- 验证观战者发送 `game.wealth_action` 被后端硬拒(错误码 **30011**)。
- 验证观战者**不能**看到其他玩家 `my.*` 私有信息(现金/贷款/资产详情),但能看到脱敏后的资产柱/排名。
- 验证观战者能看到 `bot_contexts`(12 Agent 思考面板)——这是宏观涌现观察的关键入口。

**6.3.5 退出与断线恢复**
- 验证 Agent 掉线后转 bot 接管(有可用 provider)。
- 验证终局 60s 后自动 `game.removed`。
- 验证刷新 `/wealth/:roomId` → `game.state` 快照恢复全部面板,无状态丢失。

#### 6.4 B 阶段:经济引擎与月结

> **重要**: 经济引擎数值正确性以**服务端单元测试**为第一事实来源。前端测试聚焦**状态同步 + 渲染正确性 + 宏观数据一致性**。若 `go test ./ServerGo/game/wealth/...` 全绿,则引擎数值可信;测试重点转为前端是否正确展示引擎产出。

**6.4.1 月结流程**
- 验证月结 7 步顺序(工资→被动收入→固定支出→税+社保→生活支出→债务→市场波动)。
- 验证个税计算:月薪 20000 → 社保 2100、个税 1170(《规则》§9.1 示例复算一致)——通过 `game.state` 中各 Agent 的 `monthly.detail` 逐项核对。
- 验证年度调整(12 月):工资 +3%、通胀因子进位。
- 验证 Ledger 双式流水:每笔 `from/to/amount/category` 字段齐全,`from ≠ to`。**12 Agent 一月应产生 ≥12 条 Ledger 条目**(每人至少工资+税+社保+消费)。

**6.4.2 经济不变量核对(宏观 + 微观)**

| 不变量 | 验证方式 |
|--------|---------|
| **I1 座位现金守恒** | 每个 Agent 的 `cash` 月变化 = Ledger `to=seat` − `from=seat` 求和 |
| **I4 资产负债恒等式** | 每个 Agent `net_worth = cash + Σ资产市值 − Σ贷款余额`(DOM 数值复算) |
| **I6 FI 公式** | `fi_index = 被动收入/月总支出`(支出 0 → 2.0 封顶) |
| **I7 税费率** | 股票卖出的佣金 ≥5 元;房产不满五唯一 5%+2%;黄金 0.5% |
| **I8 系统总财富守恒** | 12 Agent `Σ net_worth` + 系统税收总额 = 初始总财富 + 系统新增注入(工资)- 系统扣减(税) |

**6.4.3 市场与人生事件**
- 验证市场周期四阶段(recovery/boom/recession/depression)DOM 徽章切换,**阶段应随时间轮动**(非固定不变)。
- 验证 60 月钟声:市场周期重掷、人生事件触发(结婚💒/生育🍼/健康🏥/失业📉)。
- 验证失业:衰退 15%/萧条 25% 月度判定 → 工资归零 2–6 月。**宏观验证**:≥60 月长跑中应至少出现 N 次失业事件(按概率估算)。

**6.4.4 破产流程(独立房间验证)**
- 通过 seed 或操作使某 Agent 现金连续 3 月 < 0,触发破产清算。
- 验证全屏公告「⚖️ X 号位触发破产清算」。
- 验证房产按 70% 强制变现、消费贷减 50% 分 36 期、信用分清零、停 3 月。
- 验证重组后月现金流仍负 → 出局(`alive=false`,token 移出地图)。
- **宏观验证**:≥60 月长跑中,12 Agent 应出现 ≥1 次破产事件(模拟真实社会的破产率)。

**6.4.5 终局与结局展示(独立房间验证)**
- 验证主时钟 60 岁 → `game.over` 三维评分到达。
- 验证 `GameOverModal`:评分条 + 结局徽章 + FI Index + 人生满意度 + 社会贡献 + 净资产曲线。
- 验证 6 种结局映射(≥85 财务自由·人生赢家 / 70–84 富足安稳 / 50–69 平淡度日 / 30–49 债务缠身 / <30 破产出局 / FI≥1.5 且满意度<40 孤独富翁)。
- **宏观验证**:12 Agent 终局结局应**多样化**(不应全部同一结局),体现个体差异。

#### 6.5 C 阶段:个体经济行为模拟(某一个人)

> **本阶段验证单个 Agent 的经济决策是否符合真实人类的经济行为特征。**

**6.5.1 月度决策合理性**
- 观察单个 Agent 连续 ≥30 月的决策序列(通过 `bot_contexts.last_decision_summary` 采集)。
- 验证决策是否符合其**职业卡人设**(保守型偏储蓄/理财,激进型偏投资/创业,平衡型偏多元配置)。
- 验证决策是否**响应市场状态**:衰退期减少投资/增加储蓄,繁荣期增加投资/适度加杠杆。
- 验证**动作预算**遵守:每月 ≤3 动作,第 4 个被拒(错误码 **35006**)。

**6.5.2 行为经济学偏差验证**
- **损失厌恶**:Agent 在资产亏损时是否更倾向于持有(不愿止损)而非理性止损。
- **过度自信**:Agent 是否在高收益投资上加仓过快(未充分分散风险)。
- **锚定效应**:Agent 买房出价是否过度参考历史成交价而非当前市场价值。
- **羊群效应**:当多个 Agent 都在买某类资产时,其他 Agent 是否跟风买入。
- **心理账户**:Agent 是否区分"辛苦钱"(工资)与"意外之财"(投资收益),对后者的消费更慷慨。

**6.5.3 生命周期策略调整**
- 验证青年期(25–35 岁):Agent 是否偏进取(加杠杆买房/投资/学习提升)。
- 验证中年期(35–50 岁):Agent 是否偏稳健(减少杠杆/增加被动收入/子女教育支出)。
- 验证临退休期(50–60 岁):Agent 是否偏保守(清债/减少高风险投资/锁定收益)。

**6.5.4 speak 发言一致性**
- 验证 Agent 发言(`SendFromBot`)是否符合其职业卡人设与当前经济状态。
- 验证发言内容是否**引用真实经济事件**("刚买了 XX 区房子,首付 30 万"、"上个月失业了,正在找新工作"、"股市崩了,亏了 5 万")。
- 验证发言**非空/非乱码/非重复模板**——应为情境化自然语言。

#### 6.6 D 阶段:群体博弈与社会涌现(一群人 / 社会层面)

> **本阶段验证 12 Agent 构成的微型经济社会是否涌现出现实世界的宏观经济现象。这是本测试的核心特色。**

**6.6.1 长跑实验设计**
- 创建 1 个全 Agent 房间,配置 `month_ms:1000`(加速),连续跑 **≥60 月**(理想 420 月至终局,但 60 月已可观察趋势)。
- 每月在 `settling` 期采集**宏观快照**:12 Agent 的现金/资产/负债/净资产/职业/投资/发言/决策摘要。
- 存储宏观时间序列数据,用于后续分析。

**6.6.2 财富分布涌现(M1: 幂律分布)**
- 验证 12 Agent 的净资产分布是否随时间从"均匀"走向"分化"。
- 计算**基尼系数**时间序列:初始应接近 0(人人平等),60 月后应显著 > 0.3(模拟真实社会贫富差距)。
- 验证是否出现**幂律尾部**:top 20% Agent 持有 > 50% 的社会总财富。
- **判定标准**:基尼系数单调递增(前 30 月)→趋于稳定(30–60 月)即为涌现成功。

**6.6.3 经济周期涌现(M2: 周期轮动)**
- 验证市场周期四阶段(recovery → boom → recession → depression → recovery)是否**自然轮动**,非固定不变。
- 验证 60 月内至少完成 **≥2 个完整周期**。
- 验证各阶段**宏观指标**是否符合现实:
  - Boom:房价上涨、Agent 普遍加杠杆、投资活跃。
  - Recession:失业率上升、Agent 减少投资、消费降级。
  - Depression:破产事件频发、资产价格暴跌、Agent 普遍"现金为王"。
  - Recovery:经济触底反弹、Agent 试探性加仓。

**6.6.4 群体博弈行为(M3: 竞价/抛售/坐庄)**
- **竞价买房**:验证当多名 Agent 同时想买同一城区房产时,是否价高者得(推高房价)。
- **恐慌性抛售**:验证市场崩盘时,是否出现 Agent 集中卖出资产 → 价格踩踏。
- **信息不对称**:验证 Agent 是否利用私有信息(如预知市场转向)进行套利。
- **自发秩序**:验证无中心调控下,Agent 是否通过供需博弈形成均衡价格。

**6.6.5 社会流动性涌现(M4: 阶层流动)**
- 验证是否存在**穷人翻身**:初始资产低的 Agent 通过学习/副业/投资实现净资产排名上升。
- 验证是否存在**富人跌落**:初始资产高的 Agent 因投资失败/破产/失业排名下降。
- 计算**社会流动性指数**:60 月前后净资产排名的 Spearman 相关系数。完全固化 → 1.0,完全流动 → 0.0;合理区间 0.4–0.7(模拟真实社会)。
- **判定标准**:社会流动性指数 < 0.9 即为涌现成功(存在阶层流动)。

**6.6.6 泡沫与崩溃涌现(M5: 资产泡沫)**
- 验证 ≥60 月长跑中是否出现 **≥1 次资产泡沫**:某类资产(房产/股票)价格持续上涨远超基本面 → 随后崩盘。
- 泡沫期特征:Agent 普遍看涨 → 加杠杆追涨 → 价格脱离价值。
- 崩盘期特征:先涨后跌 → Agent 恐慌抛售 → 价格暴跌 → 杠杆 Agent 爆仓/破产。
- **判定标准**:观测到至少一次"快速上涨 → 快速下跌"的价格尖峰即为涌现成功。

**6.6.7 信贷周期涌现(M6: 杠杆与去杠杆)**
- 验证 Agent 是否在**繁荣期主动加杠杆**(贷款买房/融资炒股),在**衰退期被动去杠杆**(抛售资产还贷/断供)。
- 计算**系统总贷款余额**时间序列:应与经济周期正相关(boom 期高、depression 期低)。
- 验证**断供/法拍**:depression 期是否出现 Agent 房贷违约 → 房产被法拍。

**6.6.8 宏观涌现验收清单(M1–M8)**

| 编号 | 涌现现象 | 验收标准 | 采集方式 |
|------|---------|---------|---------|
| M1 | 财富幂律分布 | 60 月基尼系数 > 0.3 | 每月净资产快照 |
| M2 | 经济周期轮动 | 60 月 ≥2 个完整周期 | 市场周期徽章 DOM |
| M3 | 群体博弈行为 | 竞价/抛售/坐庄至少出现一种 | 发言 + Ledger 分析 |
| M4 | 社会流动性 | 排名 Spearman 相关 < 0.9 | 月净资产排名序列 |
| M5 | 资产泡沫与崩溃 | ≥1 次价格尖峰 | 房价/股价时间序列 |
| M6 | 信贷周期 | 总贷款余额与经济周期正相关 | 月贷款余额加总 |
| M7 | 失业率周期 | depression 期失业事件频次 > boom 期 | 月失业事件计数 |
| M8 | 破产事件 | 60 月 ≥1 次破产 | 破产公告 DOM |

#### 6.7 E 阶段:前端渲染(观战者视角)

**6.7.1 2.5D 地图(12 token 分布)**
- 验证 `WealthCityMap` r3f Canvas:8 城区楼群 + 道路 + **12 个 Agent token**。
- 验证 hover 城区显示房价/租金信息卡。
- 验证 `AgentToken` 圆柱 token + Billboard 头像 + 名牌,**12 token 颜色/名牌可区分**。
- 验证 token 迁区 lerp 动画(连续 2~3 帧截图流畅)。
- 验证 `WealthMinimap` Canvas2D 小地图:城区色块 + **12 agent 点** + 视野框。
- 验证楼群高度随 `price_index` 变化。

**6.7.2 面板栈**
- `FinancialPanel`:现金/月收支柱状/FI 仪表/五维资源条/资产负债 Tab。
- `MarketPanel`:股指/金价/债券利率/8 区房价指数。
- `LedgerPanel`:双式流水(最近 50 条)+ 月度汇总。
- `WealthGameChatPanel`:房间聊天面板(12 Agent 发言)。
- `AgentThoughtPanel`:12 Agent 思考面板(`bot_contexts`),可切换查看各 Agent 决策摘要。

**6.7.3 MonthTicker 与 GameOverModal**
- `MonthTicker`:月度倒计时 + 市场数据 + 事件横滚。
- `GameOverModal`:评分条 + 结局徽章 + 净资产曲线 + 人生报告摘要。

**6.7.4 i18n**
- 验证 zh/en/ja 切换全键覆盖(缺键 tsc 报错——已通过编译门禁 G5 保证)。

#### 6.8 UI 合理性审计(强制章节)

> **本节为强制输出**。测试不仅是"功能通/不通",更要从**观战者(观察 12 Agent 社会)的视角**评估 UI 布局合理性 + 交互设计合理性。所有审计结果独立成节写入报告,即便功能测试全部也要输出。

**6.8.1 布局合理性审计清单**

| 审计项 | 检查方法 | 通过标准 |
|---|---|---|
| **屏幕利用率** | 不同分辨率(1920×1080 / 1440×900 / 1280×720)截图比对 | 关键信息(当前月份 / 阶段 / 计时器 / 12 座位资产)不溢出首屏 |
| **12 座位布局** | 12 人满员时各玩家信息卡是否挤压 | 玩家数增加时,卡片尺寸按比例缩放而非溢出 |
| **宏观面板可读性** | 12 Agent 数据密度是否过高 | 财富分布 / 市场周期 / 经济指标一目了然 |
| **响应式断点** | `viewport` 切换到 768 / 1280 / 1920 宽度 | 主面板不出现横向滚动条 / 关键按钮 ≥ 44px |
| **首屏关键信息** | 进入房间后立即 DOM 读取 | 当前月份 + 阶段 + 计时器 + 市场周期 + 12 座位概览**全部首屏可见** |

**6.8.2 交互合理性审计清单**

| 审计项 | 检查方法 | 通过标准 |
|---|---|---|
| **点击区域大小** | 取所有按钮 `getBoundingClientRect` | 触摸目标 ≥ 44×44px(CLAUDE.md §23 token) |
| **焦点顺序** | 连续按 Tab,记录焦点元素 | 焦点流动符合视觉顺序,不出现焦点陷阱 |
| **键盘可达性** | 所有交互能否纯键盘完成 | 切换 Agent 面板 / 关闭模态 / 滚动聊天 全部可纯键盘 |
| **悬停反馈** | hover 关键按钮 / Agent 卡片 / 城区 | 出现视觉变化(色/边框/阴影),且响应 ≤ 100ms |
| **加载状态** | 创建全 Agent 房间后立即 DOM 读取 | 出现 loading 占位(12 bot 入座需时) |
| **空状态** | 无对局场景 | 显示友好空态文案 + 引导按钮,不出现裸 DOM |
| **错误反馈** | 故意触发错误(如观战者发 action) | 顶部 toast 报 30011,符合 CLAUDE.md §7.1 |
| **模态可关闭** | Esc / 点击遮罩 / 显式关闭按钮 | 三种方式至少两种可用,焦点正确回到触发元素 |

**6.8.3 可读性与对比度审计**

- **暗色主题**: 关键文本(Agent 名 / 数值 / 发言 / 阶段标签)对比度 ≥ 4.5:1(正文 WCAG AA,CLAUDE.md §26.1)。
- **状态徽章**: 市场周期标签 / income-band / ending ≥ 5.0:1。
- **选中态**: 选中 Agent / 当前月份高亮 ≥ 6.0:1(选中/未选差异明显,CLAUDE.md §26.2 反模式 4)。
- **同色相陷阱**: 红底红字 / 绿底绿字 / 蓝底蓝字 一票否决(§26.2 反模式 1)。
- **12 token 区分度**: 12 Agent 的 token 颜色 + 名牌应可快速区分,不依赖颜色单一通道(色盲友好)。

**6.8.4 信息架构与可视化合理性**

- **2.5D 地图**: 12 token 在 8 城区的分布是否清晰反映经济行为(高薪 Agent 迁高档区、失业 Agent 迁郊区)。
- **月结帧**: 12 人月结数据更新是否流畅;Ledger 新增条目是否醒目。
- **FinancialPanel**: 观战者视角展示的汇总数据是否直观(12 人平均/最高/最低)。
- **MarketPanel**: 行情数据更新是否与月结同步;四阶段色环是否明显。
- **LedgerPanel**: 双式流水 from/to/category 是否一目了然;月度汇总是否清晰。
- **AgentThoughtPanel**:12 Agent 决策摘要是否可读、可切换、可按时间回溯。
- **GameOverModal**: 12 Agent 终局对比(三维评分 + 结局徽章 + 净资产曲线)是否易读。

**6.8.5 UI 缺陷报告规范**

任何 UI 缺陷**必须**包含:
1. **缺陷分类**: 布局 / 交互 / 可达性 / 可读性 / 视觉一致性。
2. **复现步骤**: 具体分辨率 + 具体操作路径(可被复现)。
3. **截图证据**: `AutoTestScreenshots/虚拟城市_ui_<缺陷类型>_<时间戳>.png`。
4. **影响范围**: 单页面 / 全局 / 多分辨率。
5. **修复建议**: 具体可执行的修复方向(组件层 / CSS / 布局)。

### 7. 操作与覆盖要求

- **观战者视角操作(硬约束)**: 以 spectator 身份观察 12 Agent 对局,**禁止**以玩家身份操作(无人类玩家)。详见 §1.3。
- **宏观数据采集(硬约束)**: 每月结(`settling`)期间采集一次 12 Agent 宏观快照;长跑(≥60 月)期间**禁止**在 `acting` 期轮询干扰 Agent 决策;宏观分析基于**事后离线采集**的时间序列,非实时干预。
- **DOM 优先(硬约束)**: 详见 §1.2。信息获取**必须**先尝试 DOM 读取,仅在视觉验证场景才用截图。
- **覆盖度**: 全面覆盖所有可交互元素,重点验证:(1) 全 Agent 房间创建与开局;(2) 月结 7 步与经济不变量;(3) 个体 Agent 经济决策拟人度;(4) 群体博弈与社会涌现(M1–M8);(5) 观战者视角前端渲染;(6) UI 合理性审计(§6.8)六大核心模块。
- **客观性**: 严格基于 DOM 文本 / 页面截图 / 日志 / 协议抓包 / 宏观时间序列数据等可观测事实。
- **测试报告截图**: 每完成一个核心交互动作必须截图归档,供后续审计与缺陷对照。

### 8. 报告与后处理

1. 测试结束首先生成虚拟城市测试报告,完整保存(含本轮使用阶段 + 测试模块 + 测试焦点 + 测试结论 + **宏观涌现评估** + **UI 合理性审计独立章节**)。报告必须包含 `未覆盖项继承与变化`节(对比上轮)和 `多轮收敛趋势`节。
2. 同步生成进度文件,必备字段:
   - `当前阶段`(A / B / C / D / E)+ `本轮测试模块` + `本轮测试焦点`
   - `本轮测试结论`(`通过` / `存在缺陷(附摘要)` / `中断未完成`)
   - `已测组合清单` / `待测组合清单` / `下一轮建议组合`
   - `未覆盖项跟踪表`(继承上轮 + 本轮新增,含状态与积压标注)
   - `本轮重点突破项`(1-3 项) + `突破结果`
   - **`本轮宏观涌现评估`**(仅 D 阶段):M1–M8 各项 `✅ 已涌现` / `⚠️ 部分涌现` / `❌ 未涌现`,附基尼系数/流动性指数等量化指标
   - **`本轮 UI 合理性审计结论`**:布局合理性 / 交互合理性 / 可达性 / 可读性,各评级为 `✅ 达标` / `⚠️ 部分达标` / `❌ 未达标`,并在报告正文展开。
3. **报告文件名前缀必须为 `虚拟城市自动化测试报告_`**(shell 接力预检 `auto_run_common.sh::GAME_GLOBS` 按此前缀扫描);报告 + 进度文件确认落盘即可——**严禁 Agent 执行任何 git 写操作**:测试产物已被 `.gitignore` 忽略,不入库、不提交。
4. **报告落盘后立即进入「自动修复流程」(§12)**——在同一 Agent 会话内完成缺陷分析、代码修复、构建验证、提交推送，无需外部脚本接力。
5. MCP 工具异常导致终止时,**额外**在 `go-web-debug-tool/UseReport/虚拟城市测试工具使用报告_*.md` 写工具报告。
6. 触发协议层诊断时,**额外**写 `TestReport/虚拟城市协议抓包分析报告_*.md`(API Key/Token/Cookie 脱敏),并在测试报告里引用。
7. 中断未完成也要写进度,标 `中断未完成`。

### 9. 注意事项

- **全 Agent 房间(硬约束)**: 本测试**不涉及任何人类玩家操作**——无人类登录、无人类验证码、无人类动作操作。12 名 Agent 构成完整经济社会,测试者以观战者(spectator)视角观察。
- **宏观涌现评估(核心特色)**: 测试不仅是"功能通/不通",更要评估 12 Agent 社会是否涌现出真实经济现象。D 阶段为必保阶段,每轮宏观涌现验收清单(M1–M8)必须完整输出。
- **多 Agent 并发与 Git**: 测试期间留意其他开发 Agent;测试阶段**仅做只读 Git 查询**(`git status` / `git log` / `git diff`),**禁止** `git add` / `git commit` / `git push` 等一切写操作——测试产物按 `.gitignore` 策略仅留本地,不入库、不提交。**进入 §12 修复流程后按该章节 Git 规则执行**。
- **单轮单模块**: 硬性约束,禁止「提高效率」连测多个模块;全模块覆盖靠多轮推进。
- **长对局处理**: D 阶段宏观涌现验证需连续跑 ≥60 月,使用 `month_ms:1000` 加速(60 月 ≈ 60 秒)。B 阶段功能测试可用 `month_ms:3000`。破产/终局独立建房间验证。
- **经济引擎优先相信单测**: `go test ./ServerGo/game/wealth/...` 全绿则数值可信;前端聚焦状态同步与渲染。
- **重大问题立即停**(§3 已列):不待页面超时,不绕路,立刻写报告 + 进入修复流程。
- **经济不变量硬约束**: Ledger 双式守恒 / 资产负债恒等 / 税费率正确性为**不可破坏**的硬约束,任何修复不得破坏这些不变量。
- **宏观数据采集伦理**: 采集 12 Agent 宏观数据用于测试分析是允许的,但**禁止**通过 eval_ws/eval_js 向 Agent 注入指令影响其决策(保持 Agent 自主性)。
- **公平性与健壮性**: 校验 Agent 不得违规获取非公开信息(含其他 Agent `my.*` 私有数据);网络波动重试 / 异常降级 / 重连。
- **大模型稳定性**: Agent 多次调 LLM 失败不得直接判业务缺陷,先协议层抓包定位再下结论。
- **数据安全**: 抓包数据严禁明文输出 API Key / Token / Cookie。
- **优化建议**: 报告须给具体可执行建议(经济引擎调优 / Agent 决策策略增强 / 宏观涌现机制优化 / **UI 布局与交互修复** / 前端修复 / 协议重试策略)。

### 10. 主流程阻塞时的降级测试路径

> 当 §6.3-6.7 主流程被阻塞(月度 tick 不动 / 12 Agent 全员静默 / 创建房间失败)时,**不要立即整轮终止**,按以下降级路径继续采集可观测数据。

#### 10.1 第一层:REST API 快照

- `GET /api/games/wealth/rooms` — 开放房间列表
- `GET /api/games/wealth/rooms/:id` — 房间详情
- `GET /api/llm/models` — 验证 ≥8 个模型注册且 key 可用(排查 Agent 静默根因)
- `GET /api/admin/llm/providers` — Provider 可用性
- **权威状态(phase/month/players/bot_contexts)**:通过以下只读方式:
  1. 前端页面 React fiber / zustand store(MCP `eval_js`,只读);
  2. 或走 §10.2 服务端日志;
  3. 或 §10.3 DB 直查。

**判定**: 若 fiber/日志中的 `phase` 与预期不符(如一直停在 `acting` 不推进),则月度 tick 路径有问题;若 phase 正常推进但 WS 不推送,则是 WS 分发层问题;若 12 Agent 无 LLM 调用日志,则是 Provider/模型配置问题。

#### 10.2 第二层:服务端日志诊断

通过 `journalctl` / 服务日志观察:
- `wealth game started` 后是否有 `agent started` × 12
- `LLM call` / `stream` 相关日志(12 Agent 每月应产生 ≥12 次 LLM 调用)
- `month` 推进日志
- `settlement` 相关日志
- `bot_contexts` 更新日志

#### 10.3 第三层:DB 直查

```sql
-- 验证 12 bot user
SELECT COUNT(*) FROM t_lsm_game_user WHERE IsBot=1 AND username LIKE 'wealth_%';
-- 验证 provider 是否注册且 key 非占位(≥8 个不同模型)
SELECT id, model, provider_type, LENGTH(api_key_enc) FROM t_lsm_game_llm_provider;
-- 验证房间 DB 状态
SELECT id, status, current_count FROM t_lsm_game_room WHERE game_kind='wealth' ORDER BY id DESC LIMIT 5;
```

#### 10.4 降级路径产出要求

即使主流程完全阻塞,降级路径采集的数据也必须写入报告:
- **REST 快照** → 「诊断数据与证据链」
- **日志统计** → 「服务端日志统计」
- **DB 直查** → 「诊断数据与证据链」

**禁止**: 主流程阻塞后直接终止、报告只写「全部未覆盖」而无诊断数据。

### 11. 多轮迭代策略与未覆盖项滚转

> 核心理念:每轮测试不可能覆盖所有功能,但**未覆盖项必须跨轮滚转、逐轮收敛**。宏观涌现验收项(M1–M8)同理。

#### 11.1 未覆盖项滚转规则

1. **每轮进度文件必须包含「未覆盖项跟踪表」**,格式:优先级 / 未覆盖项 / 状态 / 备注。
2. **状态三态**: `❌ 未覆盖` → `⚠️ 进行中` → `✅ 已覆盖`。
3. **上轮 `❌ 未覆盖` 自动继承到本轮**,除非有明确理由移除。
4. **连续 N 轮未覆盖的条目自动升级**:
   - 连续 2 轮 `❌` → 在备注中标注 `⬆️ 积压 2 轮`。
   - 连续 3 轮及以上 `❌` → 必须在本轮作为重点突破项。
5. **新发现的未覆盖项**以 `NEW` 标记追加到表尾。

#### 11.2 每轮重点突破项选取规则

从「未覆盖项跟踪表」+「宏观涌现验收清单(M1–M8)」中,按以下优先级选取 **1-3 项**作为本轮重点:
1. **积压最久**(连续 `❌` 轮数最多)的条目优先。
2. **宏观涌现未实现**的 M 项优先(如 M1 财富分布未分化、M5 无泡沫崩溃)。
3. **与本轮模块强相关**的条目优先(如本轮测 emergence → 优先突破 M 项)。
4. **依赖本轮已具备条件的条目** 次优。
5. **与最近提交变更相关的条目**最后。

#### 11.3 模块依赖型未覆盖项的处理

当未覆盖项依赖特定场景(如破产流程、终局评分、宏观泡沫)但本轮未触达时:
1. **不跳过**: 记录为 `❌ 未覆盖(场景未触达)` 并继承到下轮。
2. **下轮策略调整**: 若连续 2 轮未触达目标场景,本轮单独建房间验证该场景。

#### 11.4 进度文件新增必备字段

- `当前阶段`(A / B / C / D / E)
- `本轮测试模块`(lifecycle / economy / individual / emergence / render)
- `本轮重点突破项`(1-3 项)
- `突破结果`(每项: `✅ 已突破` / `⚠️ 部分突破(附说明)` / `❌ 未突破(附原因)`)
- `积压升级标注`(连续 ≥ 2 轮未覆盖的条目标注 `⬆️ 积压 N 轮`)
- **`本轮宏观涌现评估`**(M1–M8 各状态 + 量化指标)
- **`本轮 UI 合理性审计结论`**(各维度评级 + 缺陷计数)

#### 11.5 报告新增必备字段

- `未覆盖项继承与变化`节:对比上轮未覆盖项,列出本轮新增/已覆盖/仍继承的条目。
- `多轮收敛趋势`节:简述整体覆盖度进展(如「6 轮完成,已覆盖 X/Y 项,剩余 Z 项含 N 项积压 ≥ 2 轮;宏观涌现 M1–M8 已涌现 5/8」)。
- **`宏观涌现评估`独立章节**(D 阶段必含):M1–M8 各项验收结果 + 量化数据(基尼系数/流动性指数/周期轮动次数/泡沫事件数等)。
- **`UI 合理性审计`独立章节**(§6.8 五维度展开:布局 / 交互 / 可达性 / 可读性 / 信息架构)。

#### 11.6 截图归档规范

- **路径**: `AutoTestScreenshots/虚拟城市_<页面或模块>_<动作或缺陷>_<YYYYMMDD_HHMMSS>.png`
- **覆盖动作**: 每个核心交互步骤至少 1 张;宏观面板 / 12 token 分布 / UI 缺陷必须含截图;月结 / token 迁移连续 2~3 帧。
- **报告引用**: 报告中以相对路径引用(`AutoTestScreenshots/xxx.png`),便于读者直接定位证据。

---

## 12. 自动修复流程（测试完成后执行）

> **本章节在测试报告 + 进度文件落盘后自动执行**。Agent 完成测试并写入报告后，**立即**进入以下修复流程，无需外部脚本接力。

### 12.1 硬约束（不可违反）

1. **绝对禁止修改 `CLAUDE.md`、`AGENTS.md` 这两个规则文件。**
   - 修复摘要 / 版本基线 / commit hash / 教训只写到旁路文档(如 `TestReport/<BugID>_validation.md` 或对应 `lag_docs/` 归档),**绝不写入规则文件**。
2. **每一份被处理的报告文件都必须有明确归宿**:
   - **A. 有问题已修复并推送成功** → **删除**该报告文件。
   - **B. 经核查无问题** → **就地重命名**,追加 `_无问题` 后缀。
   - **C. 有问题但构建/重启/验证失败** → **保留原文件名不变**,等待人工介入。

### 12.2 报告检索(≤ 2 min)

- **主工程**: 在 `TestReport/` 下按**虚拟城市 glob** 检索**生成时间最早、文件名不含 `_无问题` 后缀**的报告。
  - 虚拟城市相关 glob:
    - `虚拟城市自动化测试报告_*.md`(主报告)
    - `虚拟城市协议抓包分析报告_*.md`(协议报告)
- **子工程**: 在 `go-web-debug-tool/UseReport/` 下检索:
  - `虚拟城市测试工具使用报告_*.md`(工具报告)
- 无任何待处理报告 → 静默结束修复流程。

### 12.3 缺陷分析与代码修复(≤ 12 min)

- 按报告 `Bug ID` + 堆栈 + `ERROR` 日志,在对应代码库定位问题。
- **动手改代码前必须先确认缺陷当前是否仍然存在**:`git log --oneline -10 -- <file>` 看相关文件近期提交、比对当前实现与报告中堆栈。
- **根因分析**: 从逻辑底层修复,避免仅处理表面;核心逻辑遵循 `CLAUDE.md` 架构约束(仅**读取**,绝不修改)。
- 核查后确认问题**均已不存在或非有效缺陷** → 不改任何代码,直跳 §12.6 分支 B。
- 修复后必跑 `go test ./...`(Go) / `npm run build`(前端),确保本地编译与单测通过。

### 12.4 构建重启与回归验证(≤ 8 min)

- 主工程跑 `./rebuild_restart_app.sh` 重新编译 + 重启服务。
- `go-web-debug-tool` 子工程独立编译验证。
- 回归测试:创建全 Agent 房间(12 bot / 0 人类),以观战者身份观察 ≥10 月,核心流程无阻塞,经济不变量不破坏。
- **本步骤仅在「实际改了代码」时执行**;分支 B 跳过。

### 12.5 文档同步(仅旁路,≤ 2 min)

- 修复涉及架构调整 / API 变更 / 新增常量 → **只**更新业务/技术文档(`lag_docs/*.md`)与代码内注释。
- **禁止**追加、删除、修改 `CLAUDE.md` / `AGENTS.md`。
- 修复摘要 / commit hash / 回归结果可写 `TestReport/<BugID>_validation.md` 作为本轮验证记录。
- **本步骤仅在「实际改了代码」时执行**;分支 B 跳过。

### 12.6 Git 提交推送与报告归档(≤ 1 min)

按本轮实际结果,**三选一**:

**分支 A: 有问题已修复并验证通过**
- 主工程 + `go-web-debug-tool` 子工程分别 Git 提交,注释:
  `修复: [BugID] <问题简述> (关联报告: YYYYMMDD_HHMMSS)`
- 提交成功后自动 `git push` 同步远程。
- **推送完成后立即删除**已处理的对应报告文件。

**分支 B: 无问题(无需改代码、无新提交)**
- 报告文件**就地重命名**,追加 `_无问题` 后缀。
- **不删除文件、不代码提交推送**。

**分支 C: 有问题但构建/重启/验证失败**
- **保留原报告文件名不变**,不得删除、不得重命名。
- 记录错误日志,等待人工介入。

### 12.7 约束与监控

- **总时长硬上限 25 分钟**: 超时强制终止,报告保留原名。
- **子阶段时间盒**: 检索 ≤ 2 min / 修复 ≤ 12 min / 构建 ≤ 8 min / 文档 ≤ 2 min / 归档 ≤ 1 min。
- **错误处理**: 构建/重启/验证阶段失败 → 保留报告原名,等人工排查。
- **规则文件守门**: 任何「更新 `CLAUDE.md`」冲动,先停下来确认——本流程明确禁止。
- **Git 推送权限**: 默认 `git push` 同步远程;若远程不可达,记录错误,本轮走分支 C(报告保留),等人工介入。
