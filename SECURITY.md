# 安全策略

> 感谢你帮助我们保护 `LsmAgentGame` 及其用户的安全。

---

## 1. 支持版本

| 版本/分支 | 是否接收安全更新 |
|----------|------------------|
| `main`   | ✅ 接收 |
| 其他历史分支 | ❌ 不接收 |

## 2. 上报漏洞

**请勿** 在 GitHub / Gitee / GitCode 公开 Issue 中报告安全漏洞。

请通过以下方式私下联系维护者：

- 📧 邮箱：见仓库首页 `README.md` 的「联系方式」一节
- 📨 或在 GitHub 仓库的 **Security** 标签下点击「Report a vulnerability」

收到报告后，维护者将在 **72 小时内**确认并启动调查。

## 3. 上报内容建议

为加速处理，请在邮件中包含：

- 漏洞类型（XSS / SQL 注入 / 越权 / 密钥泄漏 / 依赖链漏洞 / …）
- 受影响文件 / 接口 / 路径
- 复现步骤 / PoC（最小可行）
- 潜在影响范围（数据量 / 用户量 / 严重程度）
- 你的修复建议（可选）

## 4. 响应时间表

| 阶段 | 时间 |
|------|------|
| 首次确认 | ≤ 72 小时 |
| 初步评估 | ≤ 7 天 |
| 修复与版本发布 | 根据严重程度，详见下表 |

| 严重程度 | 修复 SLA |
|----------|----------|
| Critical（远程代码执行 / 密钥硬编码泄漏） | ≤ 24 小时 |
| High（认证绕过 / 越权） | ≤ 7 天 |
| Medium（信息泄漏 / DoS） | ≤ 30 天 |
| Low（最佳实践违反） | 下一个常规版本 |

## 5. 协调披露原则

- 维护者收到报告后会先确认并私下修复，对外披露前会与上报者协调一个合理的发布日期。
- 修复发布后，维护者会在 `CHANGELOG.md` / GitHub Security Advisory 中致谢上报者（除非上报者明确要求匿名）。
- 公开漏洞细节前，**确保** 对应版本的修复已 `git tag` 发布。

## 6. 已知安全设计

本仓库已遵循以下安全规范（详见 `CLAUDE.md` §5）：

- 密钥全部从 `LsmAgentGame.conf` 读取，禁止硬编码到代码 / 文档
- 强制 HTTPS（端口 39001）/ WSS（端口 39002）
- bcrypt 密码哈希
- JWT HS256 签名（密钥在配置中）
- CORS 白名单配置化
- 严禁生产证书 / `LsmAgentGame.conf` 提交到仓库

## 7. 安全联系方式

- 仓库首页 `README.md` → 「联系方式」一节
- SECURITY 邮件标题建议：`[SECURITY] <一句话描述>`
