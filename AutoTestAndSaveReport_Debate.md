## 自动化 Web 游戏测试任务：辩论比赛 Agent 专项

> 本项目按 [MIT License](LICENSE) 开源。测试任务面向维护者，参与前请阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 与 [AutoDebugTestReport.md](AutoDebugTestReport.md)。
> 本文件为辩论比赛专用入口，**仅产出** `辩论比赛自动化测试报告_YYYYMMDD_HHMMSS.md` 前缀的报告。

- **工作目录**: `/usr/local/LsmAgentGame/LsmAgentGame`
- **入口脚本**: `AutoTestAndSaveReport_Debate.sh`(随机选编程 Agent CLI → 跑测试 → 报告落盘 → shell 层确定性接力 `AutoDebugTestReport.sh` 自动修复)
- **产物路径(仅辩论比赛,glob 集中到 `auto_run_common.sh::GAME_GLOBS`)**:
  - 测试报告: `TestReport/辩论比赛自动化测试报告_YYYYMMDD_HHMMSS.md`(单一 glob,无兼容旧文件)
  - 进度文件: `AutoTestProgress/辩论比赛自动化测试进度_YYYYMMDD_HHMMSS.md`
  - 子工程工具报告(可选): `go-web-debug-tool/UseReport/辩论比赛测试工具使用报告_YYYYMMDD_HHMMSS.md`
  - 协议抓包分析报告(按需): `TestReport/辩论比赛协议抓包分析报告_YYYYMMDD_HHMMSS.md`
  - 文件名时间戳统一 `YYYYMMDD_HHMMSS`,精度到秒
  - **Glob 单一事实源**: 所有扫描 glob 在 `auto_run_common.sh::GAME_GLOBS` 中声明,新增游戏仅改公共库一处,本文件不再内嵌 glob 列表。
   - **测试产物一律不入库**: `TestReport/*` / `AutoTestProgress/` / `AutoTestScreenshots/` 已被 `.gitignore` 整目录忽略,所有测试生成文件(报告/进度/截图/临时文档)仅留本地,Agent **禁止**对其执行 `git add` / `git commit` / `git push`。
- **执行模式**: 主 Agent 跑核心流程;遇代码读取/协议抓包/数据查询可按需委派 SubAgent,不阻塞主流程。
- **执行策略(本版本硬约束)**:
  - **纯 Agent 参赛**: 辩论比赛全部参赛角色均由 Agent 扮演,人类玩家以「观众」身份观看比赛、在聊天室插话。测试以观战者视角验证全部功能。
  - **模式覆盖**: 辩论比赛支持多种模式(双队对抗/三队辩论/四队混战/五队发散),测试需覆盖不同模式下的 Agent 行为差异。
  - **拟人节奏**: 每步操作后**等待页面完全加载**(推荐 800~1500ms)再走下一步;**操作严格模拟正常人类观众**——不瞬时连点、不用 `eval_js` 一键改多状态。
  - **重大问题/卡住/严重不及预期 → 立即终止本轮**: 不等待超时,**立刻**生成问题报告 + 调用 `AutoDebugTestReport.sh` 启动修复,**严禁强行绕过继续推流程**。
  - **UI/UX 合理性审计**: 测试不仅是"功能通/不通",还必须评估**布局合理性、交互合理性、可达性、可读性**——把发现的视觉/交互问题作为本轮报告的「UI 合理性审计」章节独立输出。
- **核心目标**: 验证 Web 端观众可用的全部交互(房间创建、观战、聊天、点赞、提问)与 Agent 的辩论行为(立论/驳论/质询/自由辩论/总结、评审打分、多模型驱动、记忆与策略连贯性),并对 UI/UX 设计做合理性评估。
- **代码变更限制**: 业务代码只读;若发现 Agent 提示词/工具定义/业务逻辑/UI 设计缺陷,**只出报告与建议**,严禁自动改业务代码。

### 1. 核心测试工具(MCP)

主工具是 `go-web-debug-tool`(MCP 服务,默认 `http://localhost:28999`),接口定义以
`/usr/local/LsmAgentGame/LsmAgentGame/go-web-debug-tool/MCP_Proc_Def.md` 为唯一事实来源。
**测试 Agent 必须将其作为唯一操作入口**,**严禁**直接调业务 HTTP 接口(`curl` REST 仅允许只读查询,见 §10 降级路径)。

#### 1.1 接口能力与典型用法

| 接口 | 用途 | 拟人场景示例 |
|------|------|--------------|
| `POST /NewChromePage` | 打开新标签页 | `{"url":"https://127.0.0.1:39001/debate"}` |
| `POST /ControlChromePage` | 模拟人类动作 | `click` / `dblclick` / `hover` / `scroll` / `input_text` / `key_press` / `drag` |
| `POST /LookChromePageInfo` | 读取页面状态 | `screenshot` / `dom` / `console` / `network` / `accessibility`(评估可达性) |
| `POST /CloseChromePage` | 关闭标签页 | 测试结束后清理 |
| `POST /ListChromePages` | 枚举已开页面 | 会话开始 / 失联恢复 |

#### 1.2 拟人操作硬约束(2026-08-31 强化版)

- **每步操作后等待 800~1500ms**,**禁止**瞬时连点;`scroll` 时使用 `behavior: "smooth"` 模拟真人滚动。
- **点击必须用真实点击**(`action: "click"` / `dblclick`),**禁止**用 `eval_js` 一键完成需拟人验证的整段动作。
- **`input_text` 必须按字符节奏**:`delay_per_key_ms` ≥ 30ms(模拟真人按键)。
- **悬停 `hover`** 必须显式调用(下拉菜单展开、tooltip、模态等场景)。
- **滚动 `scroll`** 必须按方向(`up` / `down` / `left` / `right`)显式调用,且目标 `selector` 优先于坐标。
- **拖拽 `drag`**(自由辩论区卡牌拖拽、评分条等)使用 `source_selector` + `target_selector`,**禁止** `eval_js` 改 zustand 状态。
- **`eval_js` 仅允许用于只读行为**:
  - 读取 `localStorage` / `sessionStorage` 状态
  - 读取 zustand store 快照(`window.__DEBATE_STORE__` 等全局句柄)
  - 读取 DOM 属性(`getBoundingClientRect` / 颜色计算 / 滚动位置)
  - 触发键盘快捷键(如 `Ctrl+Enter` 提交)
  - **禁止** 用 `eval_js` 一键创建房间、跳过阶段、模拟 LLM 响应、批量改状态。

#### 1.3 DOM / 截图 / 元素定位规范

- **每完成一个核心交互动作,必须截图**(`info: "screenshot"`)并归档到 `AutoTestScreenshots/辩论比赛_<阶段>_<动作>_<时间戳>.png`,报告按需引用。
- **优先用语义选择器**(`role` / `aria-label` / `data-testid` / 文本内容)而非 CSS class(类名易因重构失效)。
- **复杂组件**(雷达图、阶段时间轴、评分柱状图)**先取 DOM 结构再读 computed style**,避免只截一张图就下结论。
- **布局审计必须用 `accessibility` info**(role tree / contrast 提示 / focus order)评估键盘可达性。
- **MCP 返回值是只读快照**:复杂动画(阶段切换 / 评分揭晓)必须连续截 2~3 帧,只截一帧可能错过过渡态。

### 2. 辩论比赛模式与测试矩阵

#### 2.1 辩论模式总览

| 模式 | 队伍数 | 每队人数 | 总 Agent 数 | 立场分配 |
|------|--------|----------|-------------|----------|
| **双队对抗** | 2 队 | 3-4 人 | 6-8 | 正方 + 反方 |
| **三队辩论** | 3 队 | 3 人 | 9 | 正方 + 反方 + 中立 |
| **四队混战** | 4 队 | 2 人 | 8 | BP 制议会辩论 |
| **五队发散** | 5 队 | 2 人 | 10 | 5 个不同角度 |

#### 2.2 辩论阶段流程

| 阶段 | 代号 | 执行者 | 核心任务 |
|------|------|--------|----------|
| 赛前准备 | `preparation` | 全体 Agent | 审题构思(不调 LLM) |
| 开篇立论 | `opening_argument` | 各方一辩 | 定义权、判准、论点框架 |
| 驳论 | `rebuttal` | 各方二辩 | 反驳对方立论漏洞 |
| 质询 | `cross_examination` | 各方三辩 | 交叉盘问交锋 |
| 质询小结 | `cross_exam_summary` | 一/二辩 | 梳理质询战果 |
| 自由辩论 | `free_debate` | 全体 Agent | 交替发言交锋 |
| 总结陈词 | `closing_argument` | 各方四辩 | 梳理攻防、升华价值 |
| 评审 | `judging` | 3 裁判 Agent | 独立打分投票 |
| 公布结果 | `result` | 系统 | 公布胜方、最佳辩手 |

#### 2.3 测试矩阵(硬约束)

- **A 阶段**: 双队对抗模式(2 队 × 4 人 + 3 裁判),验证完整辩论流程。
- **B 阶段**: 多队模式验证(三队/四队/五队),验证多队规则差异与评审机制。
- **C 阶段**: 观众交互专项(观战、聊天、点赞、提问、Agent 思考面板)。
- 每个阶段可拆分为多轮,每轮聚焦一个子目标。

#### 2.4 进度文件必含字段

- `当前阶段`(A / B / C)
- `本轮测试模式`(双队/三队/四队/五队)
- `本轮测试焦点`(流程/Agent 行为/评审/观众交互/异常处理/**UI 合理性**)
- `已测组合清单` / `待测组合清单` / `下一轮建议组合`
- `未覆盖项跟踪表`(继承上轮 + 本轮新增)
- `本轮重点突破项`(1-3 项) + `突破结果`

### 3. 终止条件(立即停)

1. **重大缺陷 / 页面崩溃 / 卡死 / 规则自相矛盾** → 终止**当前用例**,出报告,**不进入下一阶段用例**。
2. 游戏规则(发言顺序/时间控制/评分计算/胜负判定)核心逻辑错误 → **整轮终止**。
3. MCP 工具严重异常无法支撑测试 → **整轮终止**,并写子工程 `UseReport/辩论比赛测试工具使用报告_*.md`。
4. Agent 调 LLM **连续 5 次失败**,协议诊断确认不可恢复 → **整轮终止**。
5. **操作逻辑严重不及预期**(Agent 发言为空/乱码、阶段不推进、评审不出结果、多 Agent 发言完全重复等)→ **本轮立即终止**,不待超时,出报告。
6. **UI 严重不可达**(关键按钮无法聚焦、模态无法关闭、布局溢出导致内容不可见、对比度严重低于 WCAG AA)→ **本轮立即终止**,出 UI 合理性审计报告 + 调用修复。
7. 无论正常结束或异常终止,均须按规范出报告 + 进度文件;中断的进度必须标 `中断未完成`。

### 4. 启动前置

1. 读最近 10 次**非缺陷修复类**提交(`git log --oneline -10 -- ':!*fix' ':!*BUG'` 等过滤),梳理新增/优化项作为本轮重点。
2. 读 `AutoTestProgress/辩论比赛自动化测试进度_*.md` 最新一份,沿用其「已测 / 待测」清单与「未覆盖项跟踪表」。
3. **敲定本阶段**:`当前阶段=A / B / C`,决定测试模式与焦点,写入进度草稿。
4. 读 `go-web-debug-tool/MCP_Proc_Def.md` 确认 MCP 操作/抓包接口可用;读 `MCP_ControlChromePage_Def.md` / `MCP_LookChromePageInfo_Def.md` 复核具体参数(避免凭记忆调用错误字段)。
5. 准备 `test_account.json` 中的白名单账号(见 `docs/通用功能/测试账号凭证.md`)。
6. **服务探活**(`curl -sk https://127.0.0.1:39001/api/health` → `code: 0`)。
7. **MCP 探活**:`POST /ListChromePages` 看是否已有 page,清理残留;若无,`POST /NewChromePage {"url": "about:blank"}` 试启,确保 CDP 链路通畅。
8. **参考文档清单**(测试前 5 分钟必读清单,作为 UI 合理性审计的事实来源):
   - `docs/辩论比赛/00-辩论比赛总体架构设计.md`(顶层架构)
   - `docs/辩论比赛/01-辩论比赛游戏流程设计.md`(阶段流程与阶段转换)
   - `docs/辩论比赛/02-辩论比赛Agent设计.md`(Agent 行为预期)
   - `docs/辩论比赛/03-辩论比赛房间创建与配置设计.md`(创建弹窗 UI 事实来源)
   - `docs/辩论比赛/04-辩论比赛界面与交互设计.md`(**布局/交互/可达性事实来源**)
   - `docs/辩论比赛/06-辩论比赛公平性与评审系统设计.md`(评审规则事实来源)
   - `CLAUDE.md §26` 前端 UI 颜色对比度与可读性规范(暗色主题对比度硬阈值)

### 5. 大模型 API 异常与协议层诊断

- **触发**: 任一 Agent 调 LLM 失败 ≥ 2 次即启动协议诊断。
- **方式**: 调 MCP「查看 Anthropic 协议抓包数据」接口(或委派 SubAgent)。
- **重点排查**:
  - 请求侧: URL / 鉴权头 / `model` / `messages` 角色顺序 / `tools` `tool_use` `tool_result` 配对 / `max_tokens` 与上下文长度。
  - 响应侧: HTTP 状态码(400/401/429/5xx)/ `error.type|message` / `stop_reason` / SSE 是否截断 / `content` 中 `tool_use` 完整性。
  - 链路侧: TLS 握手 / 超时与连接重置 / 多模型协议适配差异(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米)。
- **结论落地**: 写入 `TestReport/辩论比赛协议抓包分析报告_*.md`;若属可修复缺陷,出具体优化建议(**不自动改业务代码**)。
- **与终止条件联动**: 2 次失败启动诊断并继续;累计 5 次失败且确认不可恢复,按 §3 终止。

### 6. 测试步骤

#### 6.1 准备

- 读 `test_account.json` 取账号;新注册账号也回写,避免重复注册。
- 启 MCP 服务,确认操作模拟 + 抓包两类能力均可用。
- **登录走真实验证码路径**(CLAUDE.md §21,2026-08-25 起 CAPTCHA 旁路已全删):
  1. `GET /api/captcha` 获取 `captcha_id` + 验证码图片
  2. 通过 MCP `LookChromePageInfo info=screenshot` 在验证码弹窗截屏 → 用 `OCR` 或人工字符识别(Agent 用 `eval_js` 读取图片像素并 OCR 是允许的只读行为,只要不绕过 captcha 校验逻辑)
  3. 携带 `captcha_id` / `captcha_answer` 正常登录
  - **严禁**尝试调用任何 `AgentBypassAccounts` / `AGENT_BYPASS_ACCOUNTS` 等已删除旁路;若 `cfg.Server.DevMode=true` 也**不**解锁任何认证旁路。
- 进度草稿记录当前阶段 + 测试模式 + 房间创建时间。

#### 6.2 大厅与页面遍历

- 模拟人类玩家遍历大厅与辩论比赛页所有可交互元素(按钮/链接/开关/输入框)。
- 结合最近 10 次提交判断是否有新/优功能。
- 每次点击后**等待页面完全加载**(推荐 800~1500ms)再走下一步。
- 未达预期项详细记录,写入最终报告。
- **遍历要点**:导航栏所有链接 / 房间列表卡片 / 筛选/排序控件 / 分页器 / 帮助/规则按钮。

#### 6.3 房间创建与配置

**6.3.1 创建房间弹窗验证**
- 校验创建房间弹窗各配置项渲染完整、可交互:
  - 辩题选择(分类筛选/搜索/自定义输入)
  - 辩论模式选择(双队/三队/四队/五队)
  - 阶段参数配置(各阶段时长、字数限制、预设模板)
  - 模型分配(自动/手动)
  - 观众设置(聊天/思考展示/提问)
- **辩题池验证**: 辩题列表加载、分类筛选、辩题详情展示。
- **AI 配置块 UI 验证**: 模型下拉、不同模型重复检测、自动 Fisher-Yates 重排。

**6.3.2 房间创建与启动**
- 按本轮测试模式创建房间(含 Agent 队伍配置 + 3 裁判)。
- 校验创建后房间状态为 `filling`,房主自动进入观战。
- 校验队伍信息/裁判信息/阶段配置与创建请求一致。
- 房主点击「开始比赛」,验证比赛启动。

#### 6.4 辩论流程验证(核心)

**6.4.1 阶段推进验证**
- 验证阶段自动转换: `preparation` → `opening_argument` → `rebuttal` → `cross_examination` → `cross_exam_summary` → `free_debate` → `closing_argument` → `judging` → `result` → `game_over`。
- 验证每个阶段的计时器正常倒计时、超时自动推进(watchdog)。
- 验证阶段切换动画/通知正常展示。

**6.4.2 发言功能验证**
- **立论**: 各方一辩按顺序发言,字数 ≤ 配置上限,内容紧扣辩题立场。
- **驳论**: 各方二辩针对对方立论反驳,必须引用对方原文片段。
- **质询**: 三辩交叉盘问,质询方只能提问、被质询方只能回答。
- **自由辩论**: 双方/多队交替发言,发言权严格交替,每次字数 ≤ 配置上限。
- **总结陈词**: 四辩梳理全场攻防、升华价值。
- 验证发言字数超限自动截断 + 警告。

**6.4.3 Agent 智能行为(核心)**
- **多模型驱动**: 各 Agent 接入不同 LLM(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米 等),**Fisher-Yates 重排**:同一房间 N 个 Agent 尽量使用不同模型。
- **论证质量**: 论点清晰、论据充分、论证有力;不编造事实/数据/研究。
- **逻辑严谨**: 逻辑链条完整、无明显漏洞;反驳精准、指出对方错误 + 说明原因 + 给出正确观点。
- **团队配合**: 队友论点一致互补、不自我矛盾;总结陈词不重复基础论点。
- **角色差异化**: 一辩重立论框架、二辩重反驳、三辩重质询、四辩重总结升华。
- **记忆连贯性**: 后续阶段发言引用前期论点/交锋点,策略前后一致。
- **内部思考**: Agent 通过 `internal_thought` 提交思考过程,观众可见于思考面板。
- **公平性**: Agent 不得看到对方未公开信息(内部思考/未发言内容);验证 WS 帧中信息隔离。

**6.4.4 评审系统验证**
- **3 裁判独立评审**: 3 个裁判同时开始、独立打分、互不可见。
- **多维度评分**: 5 个维度(论证质量/逻辑严谨/语言表达/团队配合/反驳效力)各 1-10 分。
- **评分计算**: 取中位数(3 取中),胜方判定(多数决)。
- **最佳辩手**: 3 裁判提名投票选出。
- **评语要求**: 每个评分附带具体理由,整体评语 ≤ 300 字。
- **异常处理**: 裁判超时/失败时使用 fallback 评分(均 5 分)。

**6.4.5 多队模式差异验证**
- **三队模式**: 正方/反方/中立三队轮流,质询每队质询其他两队。
- **四队模式(BP 制)**: 政府上/下院 + 反对上/下院,按 BP 制顺序发言,POI 插话。
- **五队模式**: 5 个不同角度,综合评分排名。
- 验证多队模式下发言顺序/时间分配/评审规则的正确性。

#### 6.5 观众交互验证

**6.5.1 观战功能**
- 观众进入房间观战,实时看到所有发言、Agent 思考过程。
- 验证观众**不能**参与比赛(不能发言/投票/打断流程)。
- 验证观众**不能**看到 Agent 私有信息(内部策略/未公开内容)。

**6.5.2 聊天室**
- 观众可在聊天室发言,不影响比赛流程。
- 聊天内容实时广播给所有观众。
- 验证观众聊天**不能**被辩方 Agent 看到(辩方 Agent 不可见观众消息)。

**6.5.3 点赞与提问**
- 观众可为精彩发言点赞(仅展示用,不影响评分)。
- 观众可向裁判 Agent 提问(裁判可选择性回应)。

**6.5.4 Agent 思考面板**
- 展示 Agent 内部思考过程(可折叠)。
- 验证思考内容不包含违规信息(如对方私有信息)。

#### 6.6 界面与可视化验证

- **队伍信息面板**: 各队 Agent 列表、当前发言者高亮、辩位/模型展示。
- **裁判面板**: 3 裁判状态、评分进度、评分公布后展示。
- **发言历史面板**: 按阶段分组的发言列表、展开/收起。
- **评分结果面板**: 总分对比(柱状图)、各维度雷达图、最佳辩手、裁判点评。
- **阶段指示器**: 当前阶段、剩余时间、阶段切换动画。
- **响应式布局**: 桌面端/平板端/手机端布局适配。

#### 6.7 UI 合理性审计(2026-08-31 新增强制章节)

> **本节为 2026-08-31 提示词重构的核心增量**。测试不仅是"功能通/不通",更要从**人类观众的视角**评估 UI 布局合理性 + 交互设计合理性。所有审计结果独立成节写入报告,即便功能测试全部通过也要输出。

**6.7.1 布局合理性审计清单**

| 审计项 | 检查方法 | 通过标准 |
|---|---|---|
| **屏幕利用率** | 不同分辨率(1920×1080 / 1440×900 / 1280×720)截图比对 | 关键信息(当前发言 / 阶段计时)不超出首屏 |
| **队伍/裁判布局** | 多队模式下(3/4/5 队)各队伍卡片是否挤压 | 队伍数增加时,卡片尺寸按比例缩放而非溢出 |
| **信息密度** | `eval_js` 取 `document.body.scrollHeight / window.innerHeight` | 单屏不出现 3 屏以上垂直滚动 |
| **响应式断点** | `viewport` 切换到 768 / 1280 / 1920 宽度 | 主面板不出现横向滚动条 / 关键按钮 ≥ 44px |
| **首屏关键信息** | 进入房间后立即截图 | 当前阶段 + 当前发言者 + 计时器**全部首屏可见** |

**6.7.2 交互合理性审计清单**

| 审计项 | 检查方法 | 通过标准 |
|---|---|---|
| **点击区域大小** | 取所有按钮 `getBoundingClientRect` | 触摸目标 ≥ 44×44px(CLAUDE.md §23 token) |
| **焦点顺序** | 连续按 Tab,记录焦点元素 | 焦点流动符合视觉顺序,不出现焦点陷阱 |
| **键盘可达性** | 所有交互能否纯键盘完成 | 创建房间 / 提交发言 / 关闭模态 全部可纯键盘 |
| **悬停反馈** | hover 关键按钮 / 卡片 | 出现视觉变化(色/边框/阴影),且响应 ≤ 100ms |
| **加载状态** | 点击提交后立即截图 | 出现 loading 占位或禁用按钮(防止重复提交) |
| **空状态** | 创建空房间 / 加载失败场景 | 显示友好空态文案 + 引导按钮,不出现裸 DOM |
| **错误反馈** | 故意触发错误(如输入超长辩题) | 弹窗内联红色错误条 + 顶部 toast,符合 CLAUDE.md §7.1 |
| **模态可关闭** | Esc / 点击遮罩 / 显式关闭按钮 | 三种方式至少两种可用,焦点正确回到触发元素 |

**6.7.3 可读性与对比度审计**

- **暗色主题**: 关键文本(辩题 / 发言 / 评分)对比度 ≥ 4.5:1(正文 WCAG AA,CLAUDE.md §26.1)。
- **状态徽章**: 阶段标签 / 队伍标签 / 评分等级 ≥ 5.0:1。
- **选中态**: 选中队伍 / 当前发言者高亮 ≥ 6.0:1(选中/未选差异明显,CLAUDE.md §26.2 反模式 4)。
- **同色相陷阱**: 红底红字 / 绿底绿字 / 蓝底蓝字 一票否决(§26.2 反模式 1)。

**6.7.4 信息架构与可视化合理性**

- **雷达图**: 5 维度雷达图坐标轴是否均匀,数值范围是否合理,选中队伍高亮是否明显。
- **柱状图**: 队伍总分柱状图是否包含数值标签 / 队伍名称 / 排名顺序。
- **时间轴**: 阶段时间轴当前阶段高亮 + 剩余时间是否易读(秒级刷新)。
- **思考面板**: 折叠 / 展开是否保留上下文 / 滚动是否独立于主区域。

**6.7.5 UI 缺陷报告规范**

任何 UI 缺陷**必须**包含:
1. **缺陷分类**: 布局 / 交互 / 可达性 / 可读性 / 视觉一致性。
2. **复现步骤**: 具体分辨率 + 具体操作路径(可被复现)。
3. **截图证据**: `AutoTestScreenshots/辩论比赛_ui_<缺陷类型>_<时间戳>.png`。
4. **影响范围**: 单页面 / 全局 / 多分辨率。
5. **修复建议**: 具体可执行的修复方向(组件层 / CSS / 布局)。

#### 6.8 异常处理验证

- **Agent LLM 调用超时**: 使用 fallback 文本(模板化发言)。
- **Agent 连续失败 3 次**: 隔离该 Agent(quarantine),该队剩余 Agent 继续。
- **Agent 发言为空**: 视为放弃本轮发言,自动切换下一位。
- **裁判评分超时**: 使用默认中位数评分。
- **网络断线**: 重连后恢复状态(复用 WS 重连机制)。

### 7. 操作与覆盖要求

- **拟人操作(硬约束)**:
  - 每步操作后**等待页面完全加载**(建议 800~1500ms)再走下一步。
  - **禁止**:`eval_js` 一键完成需拟人验证的整段动作;瞬间全点所有按钮;不读页面直接断言;绕过验证码校验逻辑。
  - **允许**:`eval_js` 只读状态快照(读取 `localStorage`、Zustand store 状态、`debate.state` 字段、DOM 属性)。
- **覆盖度**: 全面覆盖所有可交互元素,重点验证房间创建、阶段推进、发言功能、评审系统、观众交互、**UI 合理性审计**六大核心模块。
- **客观性**: 严格基于页面截图 / 日志 / 协议抓包等可观测事实。
- **测试报告截图**: 每完成一个核心交互动作必须截图归档,供后续审计与缺陷对照。

### 8. 报告与后处理

1. 测试结束首先生成辩论比赛测试报告,完整保存(含本轮使用阶段 + 测试模式 + 测试焦点 + 测试结论 + **UI 合理性审计独立章节**)。报告必须包含 `未覆盖项继承与变化`节(对比上轮)和 `多轮收敛趋势`节。
2. 同步生成进度文件,必备字段:
   - `当前阶段`(A / B / C)+ `本轮测试模式` + `本轮测试焦点`
   - `本轮测试结论`(`通过` / `存在缺陷(附摘要)` / `中断未完成`)
   - `已测组合清单` / `待测组合清单` / `下一轮建议组合`
   - `未覆盖项跟踪表`(继承上轮 + 本轮新增,含状态与积压标注)
   - `本轮重点突破项`(1-3 项) + `突破结果`
   - **`本轮 UI 合理性审计结论`**:布局合理性 / 交互合理性 / 可达性 / 可读性,各评级为 `✅ 达标` / `⚠️ 部分达标` / `❌ 未达标`,并在报告正文展开。
3. **报告文件名前缀必须为 `辩论比赛自动化测试报告_`**(shell 接力预检 `auto_run_common.sh::GAME_GLOBS` 按此前缀扫描);报告 + 进度文件确认落盘即可——**严禁 Agent 执行任何 git 写操作**:测试产物已被 `.gitignore` 忽略,不入库、不提交。
4. **接力逻辑(报告生成后自动 debug)**: 报告与进度文件落盘 → Agent 退出 → 入口脚本 `AutoTestAndSaveReport_Debate.sh` 在 shell 层**确定性接力**启动 `AutoDebugTestReport.sh` 进入自动修复(含待处理报告预检 + flock 防重入),避免「声明了却从不接线」式断链。Agent 只需确保报告与进度文件落盘;**严禁 Agent 自行启动 `AutoDebugTestReport.sh`**(与 shell 接力双重启动)。修复流程会提交/推送代码修复并删除或归档已处理报告,因此报告内容必须自包含、结论写全。
5. MCP 工具异常导致终止时,**额外**在 `go-web-debug-tool/UseReport/辩论比赛测试工具使用报告_*.md` 写工具报告。
6. 触发协议层诊断时,**额外**写 `TestReport/辩论比赛协议抓包分析报告_*.md`(API Key/Token/Cookie 脱敏),并在测试报告里引用。
7. 中断未完成也要写进度,标 `中断未完成`。

### 9. 注意事项

- **多 Agent 并发与 Git**: 测试期间留意其他开发 Agent;**仅做只读 Git 查询**(`git status` / `git log` / `git diff`),**禁止** `git add` / `git commit` / `git push` 等一切写操作——测试产物按 `.gitignore` 策略仅留本地,不入库、不提交。
- **单轮单组合**: 硬性约束,禁止「提高效率」连测多个模式;全模式覆盖靠多轮推进。
- **重大问题立即停**(§3 已列):不待页面超时,不绕路,立刻写报告 + 接力修复。
- **断线重连**: 验证服务器重启 / 客户端断网 场景下重连有效性。
- **公平性与健壮性**: 校验 Agent 不得违规获取非公开信息(含对方内部思考);网络波动重试 / 异常降级 / 重连。
- **大模型稳定性**: Agent 多次调 LLM 失败不得直接判业务缺陷,先协议层抓包定位再下结论。
- **数据安全**: 抓包数据严禁明文输出 API Key / Token / Cookie。
- **验证码**: 严格走 `GET /api/captcha` 真实路径,**严禁**尝试调用任何已删除旁路。
- **优化建议**: 报告须给具体可执行建议(system prompt 迭代 / 工具定义补充 / 辩论策略增强 / 评审维度优化 / **UI 布局与交互修复** / 前端修复 / 协议重试策略)。

### 10. 主流程阻塞时的降级测试路径

> 当 §6.3-6.6 主流程被阻塞(阶段不推进 / Agent 全员静默 / 创建房间失败)时,**不要立即整轮终止**,按以下降级路径继续采集可观测数据。

#### 10.1 第一层:REST API 快照

- `GET /api/games/debate/rooms` — 开放房间列表
- `GET /api/games/debate/rooms/:id` — 房间详情(含队伍/裁判/阶段配置)
- `GET /api/games/debate/topics` — 辩题池(验证辩题加载)
- `GET /api/llm/models` — 验证 8 个模型注册且 key 可用
- `GET /api/admin/llm/providers` — Provider 可用性
- **辩论权威状态(phase/current_speaker/scores)**:REST 不暴露内部状态,改用以下只读方式:
  1. 前端页面 React fiber 中提取 `debateState`(go-web-debug-tool `eval_js`,只读);
  2. 或走 §10.2 服务端日志;
  3. 或 §10.3 DB 直查。

**判定**: 若 fiber/日志中的 `phase` 与预期不符(如一直停在 `preparation`),则 Agent 唤醒路径有问题;若 phase 正常推进但 WS 不推送,则是 WS 分发层问题。

#### 10.2 第二层:服务端日志诊断

通过 `journalctl` / 服务日志观察:
- `debate game started` 后是否有 `agent started` × N
- `LLM call` / `stream` 相关日志
- `phase` 推进日志(`preparation` → `opening_argument` → ... → `result`)
- `judge` goroutine 启动日志

#### 10.3 第三层:DB 直查

```sql
-- 验证 bot user
SELECT COUNT(*) FROM t_lsm_game_user WHERE IsBot=1 AND username LIKE 'debate_%';
-- 验证房间 DB 状态
SELECT id, status, current_phase, mode, current_count FROM t_lsm_game_debate_room WHERE id=:room_id;
-- 验证发言记录
SELECT COUNT(*) FROM t_lsm_game_debate_speech WHERE room_id=:room_id;
-- 验证评审记录
SELECT COUNT(*) FROM t_lsm_game_debate_score WHERE room_id=:room_id;
```

#### 10.4 降级路径产出要求

即使主流程完全阻塞,降级路径采集的数据也必须写入报告:
- **REST 快照** → 「诊断数据与证据链」
- **日志统计** → 「服务端日志统计」
- **DB 直查** → 「诊断数据与证据链」

**禁止**: 主流程阻塞后直接终止、报告只写「全部未覆盖」而无诊断数据。

### 11. 多轮迭代策略与未覆盖项滚转

> 核心理念:每轮测试不可能覆盖所有功能,但**未覆盖项必须跨轮滚转、逐轮收敛**。

#### 11.1 未覆盖项滚转规则

1. **每轮进度文件必须包含「未覆盖项跟踪表」**,格式:优先级 / 未覆盖项 / 状态 / 备注。
2. **状态三态**: `❌ 未覆盖` → `⚠️ 进行中` → `✅ 已覆盖`。
3. **上轮 `❌ 未覆盖` 自动继承到本轮**,除非有明确理由移除。
4. **连续 N 轮未覆盖的条目自动升级**:
   - 连续 2 轮 `❌` → 在备注中标注 `⬆️ 积压 2 轮`。
   - 连续 3 轮及以上 `❌` → 必须在本轮作为重点突破项。
5. **新发现的未覆盖项**以 `NEW` 标记追加到表尾。

#### 11.2 每轮重点突破项选取规则

从「未覆盖项跟踪表」中,按以下优先级选取 **1-3 项**作为本轮重点:
1. **积压最久**(连续 `❌` 轮数最多)的条目优先。
2. **与本轮模式强相关**的条目优先(如本轮测四队模式 → 优先突破 BP 制顺序)。
3. **依赖本轮已具备条件的条目** 次优。
4. **与最近提交变更相关的条目**最后。

#### 11.3 模式依赖型未覆盖项的处理

当未覆盖项依赖特定模式(如四队 BP 制、五队发散)但本轮未测该模式时:
1. **不跳过**: 记录为 `❌ 未覆盖(模式未到)` 并继承到下轮。
2. **下轮策略调整**: 若连续 2 轮未到目标模式,本轮改打目标模式。

#### 11.4 观众交互专属未覆盖项的处理

观众交互专属功能(如点赞/提问/Agent 思考面板)在纯流程测试中无法覆盖:
1. 累积至 2 项及以上时,**必须在下一轮安排观众交互专项用例**(§6.5)。
2. 若本轮时间不足,在进度文件中注明「下轮优先安排观众交互专项」。

#### 11.5 进度文件新增必备字段

- `当前阶段`(A / B / C)
- `本轮测试模式`(双队/三队/四队/五队)
- `本轮重点突破项`(1-3 项)
- `突破结果`(每项: `✅ 已突破` / `⚠️ 部分突破(附说明)` / `❌ 未突破(附原因)`)
- `积压升级标注`(连续 ≥ 2 轮未覆盖的条目标注 `⬆️ 积压 N 轮`)
- **`本轮 UI 合理性审计结论`**(各维度评级 + 缺陷计数)

#### 11.6 报告新增必备字段

- `未覆盖项继承与变化`节:对比上轮未覆盖项,列出本轮新增/已覆盖/仍继承的条目。
- `多轮收敛趋势`节:简述整体覆盖度进展(如「6 轮完成,已覆盖 X/Y 项,剩余 Z 项含 N 项积压 ≥ 2 轮」)。
- **`UI 合理性审计`独立章节**(§6.7 五维度展开:布局 / 交互 / 可达性 / 可读性 / 信息架构)。

#### 11.7 截图归档规范

- **路径**: `AutoTestScreenshots/辩论比赛_<页面或阶段>_<动作或缺陷>_<YYYYMMDD_HHMMSS>.png`
- **覆盖动作**: 每个核心交互步骤至少 1 张;UI 缺陷必须含复现截图;阶段切换 / 评分揭晓连续 2~3 帧。
- **报告引用**: 报告中以相对路径引用(`AutoTestScreenshots/xxx.png`),便于读者直接定位证据。