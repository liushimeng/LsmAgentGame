## 自动化 Web 游戏测试任务：德州扑克 2-6 人 Agent 专项

> 本项目按 [MIT License](LICENSE) 开源。测试任务面向维护者，参与前请阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 与 [AutoDebugTestReport.md](AutoDebugTestReport.md)。
> 本文件为德州扑克专用入口，**仅产出** `德州扑克自动化测试报告_YYYYMMDD_HHMMSS.md` 前缀的报告。

- **工作目录**: `/usr/local/LsmAgentGame/LsmAgentGame`
- **入口脚本**: `AutoTestAndSaveReport_TexasPoker.sh`(随机选编程 Agent CLI → 跑测试 → 报告落盘 → shell 层确定性接力 `AutoDebugTestReport.sh` 自动修复)
- **产物路径(仅德扑,glob 集中到 `auto_run_common.sh::GAME_GLOBS`)**:
  - 测试报告: `TestReport/德州扑克自动化测试报告_YYYYMMDD_HHMMSS.md`(单一 glob,无兼容旧文件)
  - 进度文件: `AutoTestProgress/德州扑克自动化测试进度_YYYYMMDD_HHMMSS.md`
  - 子工程工具报告(可选): `go-web-debug-tool/UseReport/德州扑克测试工具使用报告_YYYYMMDD_HHMMSS.md`
  - 协议抓包分析报告(按需): `TestReport/德州扑克协议抓包分析报告_YYYYMMDD_HHMMSS.md`
  - 文件名时间戳统一 `YYYYMMDD_HHMMSS`,精度到秒
  - **Glob 单一事实源**: 所有扫描 glob 在 `auto_run_common.sh::GAME_GLOBS` 中声明,新增游戏仅改公共库一处,本文件不再内嵌 glob 列表。
   - **测试产物一律不入库**: `TestReport/*` / `AutoTestProgress/` / `AutoTestScreenshots/` 已被 `.gitignore` 整目录忽略,所有测试生成文件(报告/进度/截图/临时文档)仅留本地,Agent **禁止**对其执行 `git add` / `git commit` / `git push`。
- **执行模式**: 主 Agent 跑核心流程;遇代码读取/协议抓包/数据查询可按需委派 SubAgent,不阻塞主流程。
- **执行策略(本版本硬约束)**:
  - **阶段 A**:先以 **1 个真人类 + N 个 Agent** 跑完两轮(初测 + 回归),均通过后再进入阶段 B。
  - **阶段 B**:再以 **2 个真人类 + N 个 Agent** 跑同主路径,验证多人类视角/座位抢占/盲注职责分配。
  - **N 优先 5**(`1+5` 撞满 6 人上限桌),缺 Agent 资源时允许 `N=3`(`1+3` 4 人桌)或 `N=1`(`1+1` 2 人桌);**N 不允许 < 1**(低于 2 人无法成局)。
  - **拟人节奏**:每步操作后**等待页面完全加载**(推荐 800~1500ms)再走下一步;**操作严格模拟正常人类玩家**——不瞬时连点、不用 `eval_js` 一键改多状态、不省略决策思考时长。Bot 思考阶段**保留 0.8~1.5s 间隔**,不要触发服务端「过快操作」频率熔断。
   - **重大问题/卡住/严重不及预期 → 立即终止本轮**:不等待超时,**立刻**生成问题报告 + 调用 `AutoDebugTestReport.sh` 启动修复,**严禁强行绕过继续推流程**。
- **核心目标**: 验证 Web 端人类玩家可用全部交互(房间创建、Buy-in、座位抢占、Dealer 按钮、盲注、4 个下注轮、All-in、Showdown、手牌复盘)与 Bot 的拟人行为(牌力推断、底池赔率、bluff/fold 决策、错开思考时序)。
- **代码变更限制**: 业务代码只读;若发现 Agent 提示词/工具定义/业务逻辑缺陷,**只出报告与建议**,严禁自动改业务代码。

### 1. 核心测试工具(MCP)

主工具是 `go-web-debug-tool`(MCP 服务,默认 `http://localhost:28999`),接口定义以
`/usr/local/LsmAgentGame/LsmAgentGame/go-web-debug-tool/MCP_Proc_Def.md` 为唯一事实来源。提供两类能力:

1. **Web 端操作模拟**: 模拟真实人类点击/滚动/截图,**拟人节奏**——**禁止 `eval_js` 一键完成需拟人验证的整段动作**(只允许用于读取快照/状态、脱敏检查等只读行为)。
2. **协议层诊断**: Agent 调 LLM 失败 ≥ 2 次时,**必须**走该接口下探协议抓包,再下结论。

### 2. 分阶段测试流程(硬约束)

#### 2.1 阶段 A:1 个真人类 + N 个 Agent

- **目的**: 在最常见、对抗最完整的「1+N」对局下验证全部德扑功能(房间、Buy-in、Dealer 按钮、4 下注轮、Showdown、Bluff/Fold/All-in、Bot 模型多样性、思考错开)。
- **A-1 初测**:`N=5`(满 6 人桌)。
- **A-2 回归**:Agent 资源不足时降配 `N=3` 或 `N=1`,但至少要在 2 个不同 N 取值下完成 Showdown。
- A 阶段任意一轮出现 **§3 终止条件** 列出的 P0/P1 项 → **本轮立即终止**,出报告,不进入 B 阶段。
- A 阶段两轮全部 Showdown 通过 → 进入 B 阶段。

#### 2.2 阶段 B:2 个真人类 + N 个 Agent(只有 A 全过才进)

- **目的**: 验证多人类玩家场景下的**座位抢占合法性**、**平盲注职责分配**、**筹码独立性**、**同房间 2 人各自看到的状态视图一致**。
- **B-1**:固定 `N=3`(桌 5 人)— 撞满绝大多数下注场景但不超 6 人桌上限。
- **B-2**:固定 `N=1`(桌 3 人)。覆盖小桌下的 Dealer 按钮旋转 + 三人盲注职责。
- B 阶段任意一轮出现 P0 项 → **本轮立即终止**,出报告。
- B 阶段两轮全部通过 → 全测试 PASS。

#### 2.3 进度文件必含字段

- `当前阶段`(A / B)
- `本轮人类玩家数`(1 / 2)
- `本轮 Agent 数量 N`
- `已测组合清单` / `待测组合清单` / `下一轮建议组合`
- `未覆盖项跟踪表`(继承上轮 + 本轮新增)
- `本轮重点突破项`(1-3 项) + `突破结果`

### 3. 终止条件(立即停)

1. **重大缺陷 / 页面崩溃 / 卡死 / 规则自相矛盾** → 终止**当前用例**(`A-1`/`A-2`/`B-1`/`B-2`),出报告,**不进入下一阶段用例**。
2. 游戏规则(盲注/All-in/Showdown/筹码结算、座位分布错误等)核心逻辑错误 → **整轮终止**。
3. MCP 工具严重异常无法支撑测试 → **整轮终止**,并写子工程 `UseReport/德州扑克测试工具使用报告_*.md`。
4. Agent 调 LLM **连续 5 次失败**,协议诊断确认不可恢复 → **整轮终止**。
5. **操作逻辑严重不及预期**(Bot 1 秒内全部 All-in、Dealer 按钮不旋转、盲注位置错、Showdown 不翻开底牌等)→ **本轮立即终止**,不待超时,出报告。
6. 无论正常结束或异常终止,均须按规范出报告 + 进度文件;中断的进度必须标 `中断未完成`。

### 4. 启动前置

1. 读最近 10 次**非缺陷修复类**提交(`git log --oneline -10 -- ':!*fix' ':!*BUG'` 等过滤),梳理新增/优化项作为本轮重点。
2. 读 `AutoTestProgress/德州扑克自动化测试进度_*.md` 最新一份,沿用其「已测 / 待测」清单与「未覆盖项跟踪表」。
3. **敲定本阶段**:`当前阶段=A` 还是 `B`,决定人类玩家数与 N 取值,写入进度草稿。
4. 读 `go-web-debug-tool/MCP_Proc_Def.md` 确认 MCP 操作/抓包接口可用。
5. 准备 `test_account.json` 中的白名单账号(验证码自动旁路,见 `docs/通用功能/测试账号凭证.md`),阶段 B 需 2 套不同账号。
6. **服务探活**(`curl -sk https://127.0.0.1:39001/api/health` → `code: 0`)。

### 5. 大模型 API 异常与协议层诊断

- **触发**: 任一 Agent 调 LLM 失败 ≥ 2 次即启动协议诊断。
- **方式**: 调 MCP「查看 Anthropic 协议抓包数据」接口(或委派 SubAgent)。
- **重点排查**:
  - 请求侧: URL / 鉴权头 / `model` / `messages` 角色顺序 / `tools` `tool_use` `tool_result` 配对 / `max_tokens` 与上下文长度。
  - 响应侧: HTTP 状态码(400/401/429/5xx)/ `error.type|message` / `stop_reason` / SSE 是否截断 / `content` 中 `tool_use` 完整性。
  - 链路侧: TLS 握手 / 超时与连接重置 / 多模型协议适配差异(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米)。
- **结论落地**: 写入 `TestReport/德州扑克协议抓包分析报告_*.md`;若属可修复缺陷,出具体优化建议(**不自动改业务代码**)。
- **与终止条件联动**: 2 次失败启动诊断并继续;累计 5 次失败且确认不可恢复,按 §3 终止。

### 6. 测试步骤

#### 6.1 准备
- 读 `test_account.json` 取账号;阶段 B 取 2 套,新注册账号也回写。
- 启 MCP 服务,确认操作模拟 + 抓包两类能力均可用。
- 验证码走 `test_01~04` 白名单旁路。
- 进度草稿记录当前阶段 + 人类玩家数 + Agent 数 + 房间创建时间。

#### 6.2 大厅与页面遍历
- 模拟人类玩家遍历大厅与德扑页所有可交互元素(进入按键等待 1~1.5s,再点击下一步)。
- 结合最近 10 次提交判断是否有新/优功能。
- 每次点击后**等待页面完全加载**(推荐 800~1500ms)再走下一步。
- 未达预期项详细记录,写入最终报告。

#### 6.3 对局功能与 Agent 专项

##### 6.3.1 房间创建与角色(座位)选择
- 校验德扑房间创建弹窗各配置项渲染完整、可交互(房间名/小额盲注/大额盲注/最低 Buy-in/最大 Buy-in/最大玩家数 2-6)。
- **AI 配置块 UI 验证**: AI 数量 slider 0~6、模型下拉、不同模型重复检测、自动 Fisher-Yates 重排(沿用狼人杀同款实现)。
- 按本轮阶段 + Agent 数创建房间;**最低必须 2 Bot**(本测试硬约束)才有显著 Agent 调度意义。
- 校验 Dealer 按钮 / 盲注位置 / 玩家座位与加入顺序严格一致。
- **阶段 B**:2 个人类玩家抢占座位(谁先点击谁坐),验证先点者先得 + 旁观者不能挤占已占座位 + 两人分别下的盲注职责正确(如 dealer=1 时玩家 2 付小盲、玩家 3 付大盲)。

##### 6.3.2 人类玩家操作与界面功能
- **按钮完整性**: Fold / Check / Call / Raise / All-in / 弃牌 等所有按钮,验证点击响应、金额 slider、状态切换。
- **下注轮**: Preflop → Flop → Turn → River → Showdown,**4 个下注轮**的窗口显示、计时器、轮次切换、Dealer 按钮旋转、底池金额实时变化。
- **手牌面板**: 玩家手牌 2 张底牌 + 0~5 张公共牌;Showdown 时 5 张公共牌全部翻开。
- **下注窗口**: Raise 金额 + slider + 快捷(1/2 pot / pot / 2×pot / All-in),目标金额正确。
- **All-in**: 桌面筹码 ≥ 0 后自动变 All-in;All-in 旁注池(side pot)正确拆分。
- **观战模式**: 主对局结束后另起一局,测试账号以观战者身份进入 Bot 对局,验证隐藏底牌 + 公开牌面 + 行动流可见。

##### 6.3.3 Agent 发言活跃度(本游戏发言占比较低,降权)
- 德扑是「沉默纸牌游戏」—— 玩家常规情况不发言,只有 Showdown 附近或异常(如牌桌口水战)才有发言。
- 重点验证 Bot **决策时序错开**(不要 6 Bot 同时发牌同时行动);遇长时间沉默 **不视为缺陷**(沉默=正常德扑基线)。

##### 6.3.4 Agent 智能行为(核心)
- **多模型驱动**: 接入不同 LLM(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米 等),**Fisher-Yates 重排**:同一房间 N 个 Bot 尽量使用不同模型。
- **决策工具**: 校验 `hand_strength`、`pot_odds`、`win_rate_pot_odds_compare` 三个 LLM 工具的合理性(LLM 给出的金额 vs 工具返回数值);**不允许 LLM 直接决定行动**——必须 ToolUse 取值后由规则引擎比对后输出(同公平性原则)。
- **牌力推断**: 蒙特卡洛模拟下,AA vs KK preflop 胜率应在 [77%, 83%](权威 80.2%);验证工具运行结束 ≥ 800 次抽样且结果在该区间。
- **底池赔率**:
  - 听牌(pot = 100, call = 20)底池赔率 5:1,听牌成功率需 ≥ 16.7% 才是 +EV call。
  - 验证 Bot 听到听牌时调用 pot_odds 后是否合理决策(call 还是 fold)。
- **bluff 频率**: 验证 Bot 会**偶尔** 选择 raise bluff(手里是垃圾牌但金额合理);但不要被庄家抓到连续 5 局 bluff 同花听牌 ≥ 3 次的「不可解释 bluff」模式。
- **fold discipline**: 拿到 [2][7] offsuit + 高 raise 入场,Bot **必须** fold(权威策略);**不允许** +AI 角色直接 call 这种烂牌(违规策略会被反向利用)。
- **all-in 节奏**: Bot 在持有 Nuts(最佳牌型)时**逐步 build pot**,而不是直接 All-in;Showdown 才 All-in。
- **公平性**: Bot 不得能读到未公开底牌(其他玩家手牌);验证方式:WS `game.state` 帧中他人座位仅暴露 `hole_count` 而无 `hole` 牌面(从 React fiber 的 `gameState` 或 WS 抓包确认视图脱敏)。

##### 6.3.5 聊天(公聊 + 私聊)
- 德扑聊天频率低,但仍要验证: 公聊界面、消息收发、输入框自动清空;私聊可形成差异化策略配合(本游戏弱化)。

##### 6.3.6 完整对局(含 Showdown)
- 以本轮阶段 + Agent 数的人类玩家跑完整对局至 Showdown + 筹码结算,**覆盖全部 4 个下注轮 + Dealer 按钮旋转至少 1 次**。
- 复盘 Bot 策略连贯性(bluff 合理性、fold discipline、听牌 pot odds 应用)、与真实人类的反应差异。

#### 6.4 观战模式(独立用例)
- 主对局结束后另起一局,测试账号以观战者身份进入 Bot 对局。
- **权限隔离**: 观战者**看不到** Bot 底牌,只能看到公共牌 + 玩家行动 + 公开决策理由(若 Agent 选择 speak)。
- **公平性**: 验证观战模式下 Bot 不会因为「有人类观战」而泄露底牌行为(如异常 raise);若可疑,报告中给提示词优化方案。

### 7. 操作与覆盖要求

- **拟人操作(硬约束)**:
  - 每步操作后**等待页面完全加载**(建议 800~1500ms)再走下一步。
  - **禁止**:`eval_js` 一键 fold/call/raise 全部 Bot 同步;瞬间全点所有按钮;不读页面直接断言。
  - **允许**:`eval_js` 只读状态快照(如读取 `localStorage`、Zustand store 状态、`game.state` 字段)。
  - 桌面布局:全程模拟人类视角滚动 / 拖动 slider / 等待动画,不要绕过 UI 事件。
- **覆盖度**: 全面覆盖所有可交互元素,重点验证 Raise/All-in slider、Dealer 按钮、Showdown 摊牌界面。
- **客观性**: 严格基于页面截图 / 日志 / 协议抓包等可观测事实。

### 8. 报告与后处理

1. 测试结束首先生成德扑测试报告,完整保存(含本轮使用阶段 + 人类玩家数 + Agent 数 + 测试结论)。报告必须包含 `未覆盖项继承与变化`节(对比上轮)和 `多轮收敛趋势`节。
2. 同步生成进度文件,必备字段:
   - `当前阶段`(A / B)+ `本轮人类玩家数` + `本轮 Agent 数量 N`
   - `本轮测试结论`(`通过` / `存在缺陷(附摘要)` / `中断未完成`)
   - `已测组合清单` / `待测组合清单` / `下一轮建议组合`
   - `未覆盖项跟踪表`(继承上轮 + 本轮新增,含状态与积压标注)
   - `本轮重点突破项`(1-3 项) + `突破结果`
3. **报告文件名前缀必须为 `德州扑克自动化测试报告_`**(shell 接力预检 `auto_run_common.sh::GAME_GLOBS` 按此前缀扫描);报告 + 进度文件确认落盘即可——**严禁 Agent 执行任何 git 写操作**:测试产物已被 `.gitignore` 忽略,不入库、不提交。
4. **接力逻辑(报告生成后自动 debug)**: 报告与进度文件落盘 → Agent 退出 → 入口脚本 `AutoTestAndSaveReport_TexasPoker.sh` 在 shell 层**确定性接力**启动 `AutoDebugTestReport.sh` 进入自动修复(含待处理报告预检 + flock 防重入),避免「声明了却从不接线」式断链。Agent 只需确保报告与进度文件落盘;**严禁 Agent 自行启动 `AutoDebugTestReport.sh`**(与 shell 接力双重启动)。修复流程会提交/推送代码修复并删除或归档已处理报告,因此报告内容必须自包含、结论写全。
5. MCP 工具异常导致终止时,**额外**在 `go-web-debug-tool/UseReport/德州扑克测试工具使用报告_*.md` 写工具报告。
6. 触发协议层诊断时,**额外**写 `TestReport/德州扑克协议抓包分析报告_*.md`(API Key/Token/Cookie 脱敏),并在测试报告里引用。
7. 中断未完成也要写进度,标 `中断未完成`。

### 9. 注意事项

- **多 Agent 并发与 Git**: 测试期间留意其他开发 Agent;**仅做只读 Git 查询**(`git status` / `git log` / `git diff`),**禁止** `git add` / `git commit` / `git push` 等一切写操作——测试产物按 `.gitignore` 策略仅留本地,不入库、不提交。
- **单轮单组合**: 硬性约束,禁止「提高效率」连测多个组合;全组合覆盖靠多轮推进。
- **重大问题立即停**(§3 已列):不待页面超时,不绕路,立刻写报告 + 接力修复。
- **断线重连**: 验证服务器重启 / 客户端断网 场景下重连有效性。
- **公平性与健壮性**: 校验 Bot 不得违规获取非公开信息(含其他玩家手牌);网络波动重试 / 异常降级 / 重连。
- **大模型稳定性**: Bot 多次调 LLM 失败不得直接判业务缺陷,先协议层抓包定位再下结论。
- **数据安全**: 抓包数据严禁明文输出 API Key / Token / Cookie。
- **优化建议**: 报告须给具体可执行建议(system prompt 迭代 / 工具定义补充 / bluff 频率阈值 / showdown 行为校正 / 前端修复 / 协议重试策略)。

### 10. 主流程阻塞时的降级测试路径

> 当 §6.3 主流程被阻塞(下注无响应 / 公共牌未翻开 / Showdown 卡死)时,**不要立即整轮终止**,按以下降级路径继续采集可观测数据。

#### 10.1 第一层:REST API 快照

> 修订说明:德扑**没有**专用的 `GET /api/games/texasholdem/rooms/:id` /
> `GET /api/games/texasholdem/state` 端点(均 404),以下为本项目**实际存在**的取数方式。

- `GET /api/games/texasholdem/rooms` — 开放房间列表(通用路由 `GET /api/games/:kind/rooms`)
- `GET /api/rooms/:id` — 房间详情(通用路由,含玩家列表/状态;**不含**牌局内部状态如 phase/pot)
- `GET /api/llm/models` — 验证 10 个模型注册且 key 可用
- `GET /api/admin/llm/providers` — Provider 可用性
- **牌局权威状态(phase/pot/current_bet/turn)**:REST 不暴露,改用以下只读方式:
  1. 前端页面 React fiber 中提取 `gameState`(go-web-debug-tool `eval_js`,只读);
  2. 或走 §10.2 服务端日志(`texasholdem hand started` / street 推进日志);
  3. 或 §10.3 DB 直查 `t_lsm_game_room` 的 `status`/`phase`。

**判定**: 若 fiber/日志中的 `phase` 与预期不符(如一直停在 `preflop`),则 Action_ 系列接入或 Bot 驱动链路有问题;若 phase 正常推进但 WS 不推送,则是 WS 分发层问题。

#### 10.2 第二层:服务端日志诊断

通过 `journalctl` / 服务日志观察:
- `texasholdem game started` 后是否有 `agent started` × N
- `LLM call` / `stream` 相关日志
- `phase` 推进日志(`preflop` → `flop` → `turn` → `river` → `showdown`)

#### 10.3 第三层:DB 直查

```sql
-- 验证 bot user
SELECT COUNT(*) FROM t_lsm_game_user WHERE IsBot=1 AND username LIKE 'texasbot_%';
-- 验证房间 DB 状态
SELECT id, status, phase, current_count FROM t_lsm_game_room WHERE id=:room_id;
```

#### 10.4 降级路径产出要求

即使主流程完全阻塞,降级路径采集的数据也必须写入报告:
- **REST 快照** → 「诊断数据与证据链」
- **日志统计** → 「服务端日志统计」
- **DB 直查** → 「诊断数据与证据链」

**禁止**: 主流程阻塞后直接终止、报告只写「全部未覆盖」而无诊断数据。

### 11. 多轮迭代策略与未覆盖项滚转

> 核心理念:每轮测试不可能覆盖所有功能,但**未覆盖项必须跨轮滚转、逐轮收敛**。

#### 11.1 未覆盖项滚转规则

1. **每轮进度文件必须包含「未覆盖项跟踪表」**,格式:优先级 / 未覆盖项 / 状态 / 备注。
2. **状态三态**: `❌ 未覆盖` → `⚠️ 进行中` → `✅ 已覆盖`。
3. **上轮 `❌ 未覆盖` 自动继承到本轮**,除非有明确理由移除。
4. **连续 N 轮未覆盖的条目自动升级**:
   - 连续 2 轮 `❌` → 在备注中标注 `⬆️ 积压 2 轮`。
   - 连续 3 轮及以上 `❌` → 必须在本轮作为重点突破项。
5. **新发现的未覆盖项**以 `NEW` 标记追加到表尾。

#### 11.2 每轮重点突破项选取规则

从「未覆盖项跟踪表」中,按以下优先级选取 **1-3 项**作为本轮重点:
1. **积压最久**(连续 `❌` 轮数最多)的条目优先。
2. **与本轮模式强相关**的条目优先(如本轮测 N=5 满桌 → 优先突破 Dealer 旋转 × 5 次)。
3. **依赖本轮已具备条件的条目** 次优。
4. **与最近提交变更相关的条目**最后。

#### 11.3 模式依赖型未覆盖项的处理

当未覆盖项依赖特定模式(如 side pot 拆分要求 ≥ 3 人,全 raise + 全 call 要求 6 人)但本轮组合未到:
1. **不跳过**: 记录为 `❌ 未覆盖(组合未到)` 并继承到下轮。
2. **下轮策略调整**: 若连续 2 轮未到目标组合,本轮改打目标组合(不优先突破其他项)。

#### 11.4 观战专属未覆盖项的处理

观战者专属功能(如:私下查看牌谱、Bot 思考中停留时间)在玩家对局中无法覆盖:
1. 累积至 2 项及以上时,**必须在下一轮安排独立观战用例**。
2. 若本轮时间不足,在进度文件中注明「下轮优先安排观战用例」。

#### 11.5 进度文件新增必备字段

- `当前阶段`(A / B)
- `本轮重点突破项`(1-3 项)
- `突破结果`(每项: `✅ 已突破` / `⚠️ 部分突破(附说明)` / `❌ 未突破(附原因)`)
- `积压升级标注`(连续 ≥ 2 轮未覆盖的条目标注 `⬆️ 积压 N 轮`)

#### 11.6 报告新增必备字段

- `未覆盖项继承与变化`节:对比上轮未覆盖项,列出本轮新增/已覆盖/仍继承的条目。
- `多轮收敛趋势`节:简述整体覆盖度进展(如「6 轮完成,已覆盖 X/Y 项,剩余 Z 项含 N 项积压 ≥ 2 轮」)。
