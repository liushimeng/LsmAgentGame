# 虚拟城市（wealth → virtual-city）URL + Kind 全栈重命名方案

> **方案编号**：`tmpPlan/虚拟城市-重命名方案与执行记录-20260924.md`
> **方案版本**：v1.0（实操级口径 — 经用户决策升级）
> **生成时间**：2026-09-24
> **决策确认**：用户已确认决策 C（全栈重命名）；i18n key 命名空间一并改为 `virtualCity.*`；规则 markdown 改名；`python-generate-image-tool/` 子模块一并改名（新建 art-designer SubAgent 派遣）
> **关联前置**：`tmpPlan/虚拟城市-重命名现状扫描报告-20260924.md`（现状盘点 1123 行）
> **目标**：把 `/wealth` URL、`game_kind="wealth"` 字面量、`lobby-wealth` slug、`components/wealth/` 目录、`game/wealth/` 包、`agent/wealthplayer/` 包、`wealthplayer` 类型命名空间，全部统一改为 `virtual-city` / `virtual_city` / `VirtualCity*`，**不触碰 i18n key 命名空间**（仅翻译文案）

---

## 0. 决策摘要

| 维度 | 当前值 | 目标值 | 决策 |
|---|---|---|---|
| URL 前缀 | `/wealth` | `/virtual-city` | ✅ 改 |
| 后端 `game_kind` 字面量 | `"wealth"` | `"virtual_city"` | ✅ 改 |
| 后端 lobby id slug | `"lobby-wealth"` | `"lobby-virtual-city"` | ✅ 改 |
| 后端 HTTP group | `/api/games/wealth` | `/api/games/virtual_city` | ✅ 改 |
| 后端 WS 帧名 | `game.wealth_{action,start,pause,paused}` | `game.virtual_city_*` | ✅ 改 |
| 后端 Go 包 | `game/wealth`、`agent/wealthplayer`、`agent/wealthtypes`、`api/wealth_*` | `game/virtual_city`、`agent/vcplayer`、`agent/vctypes`、`api/virtual_city_*` | ✅ 改 |
| 后端配置 JSON key | `"wealth":` | `"virtual_city":` | ✅ 改 |
| 后端 `WealthConfig` 类型 | `config.WealthConfig` | `config.VirtualCityConfig` | ✅ 改 |
| 后端 Agent ClassName UA | `LsmAgentGame-City-Human` | `LsmAgentGame-City-Human` | ⏸ 保持（已是城市向，§27 不动） |
| 前端路由 | `pages/WealthLobbyPage.tsx` / `WealthGamePage.tsx` | `pages/VirtualCityLobbyPage.tsx` / `VirtualCityGamePage.tsx` | ✅ 改 |
| 前端组件目录 | `components/wealth/` | `components/virtualCity/` | ✅ 改 |
| 前端 hooks/store/types/api | `useWealth.ts` / `wealth.store.ts` / `wealth.ts` / `wealth.ts` | `useVirtualCity.ts` / `virtualCity.store.ts` / `virtualCity.ts` / `virtualCity.ts` | ✅ 改 |
| 前端类型命名空间 | `WealthGameState` / `WealthPlayer` 等 ~30 个接口 | `VirtualCityGameState` / `VirtualCityPlayer` 等 | ✅ 改 |
| 前端样式 | `styles/wealth*.css`（6 件） | `styles/virtualCity*.css`（6 件） | ✅ 改（保持 globals.css `@import` 顺序） |
| 前端资源 | `assets/images/wealth/` | `assets/images/virtualCity/` | ✅ 改 |
| 前端规则 markdown | `static/rules/wealth.md` | `static/rules/virtual-city.md` | ✅ 改 |
| 前端 i18n key 命名空间 | `wealth.*`（1000+ keys） | `virtualCity.*` | ✅ **改为 `virtualCity.*`**（用户决策，1000+ key 批量重命名 + 文案同步翻译） |
| 前端 `GameKind` union | `'wealth'` | `'virtual_city'` | ✅ 改 |
| 创建弹窗默认值 | `RESIDENT_DEFAULT = 10000` | `RESIDENT_DEFAULT = 10` | ✅ 改（用户明确要求） |
| 创建弹窗 `resident_count` 字段名 | `resident_count` | `resident_count` | ⏸ **保持不变**（前后端协议字段稳定） |
| 数据库 `t_lsm_game_room.game_kind` | `'wealth'` | `'virtual_city'` | ✅ 改（带兼容 alias） |
| 后端错误码 enum 名称 | `ErrCodeRoomKindWealth` 等（推测） | `ErrCodeRoomKindVirtualCity` 等 | ✅ 改（错误码 35010/35041 数字不变） |
| `AutoTestAndDebug_Wealth.{md,sh}` | 仓库根文件名 | `AutoTestAndDebug_VirtualCity.{md,sh}` | ✅ 改 |
| `lag_docs/虚拟城市/虚拟城市定位声明-全Agent真实城市模拟器.md` | v1.0 §5.4 保留 `wealth` | 升版 v2.0 §5.4 解禁 | ✅ **必做前置**：新增 §5.4 v2.0 解禁条目 |
| `lag_docs/虚拟城市/` 子目录 | 已含 `财商流/已实现/` 等历史命名 | **保持不变**（仅内文审阅） | ⏸ 不动（按定位声明 §4 禁改区） |
| `python-generate-image-tool/` 内 `generate_wealth_*.py` | `generate_wealth_v2_props.py` 等 3 文件 + `ImageOutput/wealth{,/_city}` | `generate_virtual_city_*.py` + `ImageOutput/virtual_city/` | ✅ **改**（用户决策；需派遣 art-designer SubAgent） |

---

## 1. 命名规约映射表（实操级）

### 1.1 URL / 路由
| 当前 | 目标 |
|---|---|
| `/wealth` | `/virtual-city` |
| `/wealth/:roomId` | `/virtual-city/:roomId` |
| `/wealth/spectate/:roomId` | `/virtual-city/spectate/:roomId` |
| `/api/games/wealth/rooms/:id/survey` | `/api/games/virtual_city/rooms/:id/survey` |
| `/api/games/wealth/rooms/:id/surveys` | `/api/games/virtual_city/rooms/:id/surveys` |
| `/api/games/wealth/rooms/:id/city/residents` | `/api/games/virtual_city/rooms/:id/city/residents` |
| `/api/games/wealth/rooms/:id/city/residents/:cardId` | `/api/games/virtual_city/rooms/:id/city/residents/:cardId` |
| `/rules/wealth.md` | `/rules/virtual-city.md` |

### 1.2 kind 字面量 / slug
| 当前 | 目标 |
|---|---|
| `"wealth"`（game_kind 协议值） | `"virtual_city"` |
| `"lobby-wealth"` | `"lobby-virtual-city"` |
| `'wealth'`（前端 TS union） | `'virtual_city'` |

### 1.3 Go 包 / 文件 / 类型 / 变量
| 当前 | 目标 |
|---|---|
| `package wealth` | `package virtual_city` |
| `package wealthplayer` | `package vcplayer` |
| `package wealthtypes` | `package vctypes` |
| `api/wealth_city_api.go` | `api/virtual_city_api.go` |
| `api/wealth_survey_api.go` | `api/virtual_city_survey_api.go` |
| `api/wealth_city_api_test.go` | `api/virtual_city_api_test.go` |
| `*api.WealthCityAPI` | `*api.VirtualCityAPI` |
| `*api.WealthSurveyAPI` | `*api.VirtualCitySurveyAPI` |
| `service.WealthRoomOptions` | `service.VirtualCityRoomOptions` |
| `service.WealthFullAgent` | `service.VirtualCityFullAgent` |
| `config.WealthConfig` | `config.VirtualCityConfig` |
| `config.Wealth` | `config.VirtualCity` |
| `applyWealthDefaults()` | `applyVirtualCityDefaults()` |
| `EngineWealth` / `EngineWealth*` 等常量 | `EngineVirtualCity` / `EngineVirtualCity*` |
| `ErrCodeRoomKindWealth`（推测） | `ErrCodeRoomKindVirtualCity` |
| `ErrWealthSurvey*` / `ErrWealthCity*` 等 | `ErrVirtualCitySurvey*` / `ErrVirtualCityCity*` |

### 1.4 前端 TS 类型 / 文件 / hook / store / api / 资源
| 当前 | 目标 |
|---|---|
| `pages/WealthLobbyPage.tsx` | `pages/VirtualCityLobbyPage.tsx` |
| `pages/WealthGamePage.tsx` | `pages/VirtualCityGamePage.tsx` |
| `hooks/useWealth.ts` | `hooks/useVirtualCity.ts` |
| `store/wealth.store.ts` | `store/virtualCity.store.ts` |
| `types/wealth.ts` | `types/virtualCity.ts`（文件可能拆分子文件，见 §5） |
| `api/wealth.ts` | `api/virtualCity.ts` |
| `components/wealth/` | `components/virtualCity/` |
| `components/wealth/WealthCreateRoomModal.tsx` | `components/virtualCity/VirtualCityCreateRoomModal.tsx` |
| `components/wealth/WealthGameChatPanel.tsx` | `components/virtualCity/VirtualCityGameChatPanel.tsx` |
| `components/wealth/WealthCityMap.tsx` | `components/virtualCity/VirtualCityCityMap.tsx` |
| `components/wealth/WealthBotPanel.tsx` | `components/virtualCity/VirtualCityBotPanel.tsx` |
| `components/wealth/WealthMinimap.tsx` | `components/virtualCity/VirtualCityMinimap.tsx` |
| `components/wealth/WealthPyramidPanel.tsx` | `components/virtualCity/VirtualCityPyramidPanel.tsx` |
| `components/wealth/wealth-batch20.css` | `components/virtualCity/virtualCity-batch20.css`（样式归属组件就近） |
| `styles/wealth*.css`（6 件） | `styles/virtualCity*.css`（6 件） |
| `assets/images/wealth/` | `assets/images/virtualCity/` |
| `assets/images/wealth/index.ts` | `assets/images/virtualCity/index.ts` |
| `WEALTH_BANNER` | `VIRTUAL_CITY_BANNER` |
| `useWealth` | `useVirtualCity` |
| `useWealthStore` | `useVirtualCityStore` |
| `WealthGameState` | `VirtualCityGameState` |
| `WealthPlayer` | `VirtualCityPlayer` |
| `WealthBotContext` | `VirtualCityBotContext` |
| `WealthSenseResult` | `VirtualCitySenseResult` |
| `WealthCityProfileProgress` | `VirtualCityCityProfileProgress` |
| `WealthCityResidentProfile` | `VirtualCityCityResidentProfile` |
| `WealthSurvey` | `VirtualCitySurvey` |
| `WealthSideTierMeta` | `VirtualCitySideTierMeta` |
| `WealthFlowStat/Node/Link` | `VirtualCityFlowStat/Node/Link` |
| `WealthSociety/MyState` | `VirtualCitySociety/MyState` |
| `WealthGame/WealthRoomOptions` | `VirtualCityGame/VirtualCityRoomOptions` |
| `WealthDistrictDef/WealthDistrictId` | `VirtualCityDistrictDef/VirtualCityDistrictId` |
| `WealthEventFrame/WealthMonthFrame` | `VirtualCityEventFrame/VirtualCityMonthFrame` |
| `WealthTradeAction` | `VirtualCityTradeAction` |
| `WEALTH_MAX_SEATS` / `WEALTH_MIN_SEATS` / `WEALTH_DISTRICTS` | `VIRTUAL_CITY_MAX_SEATS` / `VIRTUAL_CITY_MIN_SEATS` / `VIRTUAL_CITY_DISTRICTS` |
| `wealthSideTierMeta()` 等 utils | `virtualCitySideTierMeta()` |
| `wealthStockBreakerActive()` | `virtualCityStockBreakerActive()` |
| `wealthSideExpectedIncome()` | `virtualCitySideExpectedIncome()` |
| `wealthElectionTermElapsed()` | `virtualCityElectionTermElapsed()` |

### 1.5 WS 帧名
| 当前 | 目标 |
|---|---|
| `game.wealth_action` | `game.virtual_city_action` |
| `game.wealth_start` | `game.virtual_city_start` |
| `game.wealth_pause` | `game.virtual_city_pause` |
| `game.wealth_paused` | `game.virtual_city_paused` |

### 1.6 i18n（按用户决策：全部改为 `virtualCity.*`）
| 当前 | 目标 | 说明 |
|---|---|---|
| `nav.wealth` | `nav.virtualCity` | 改名 + 文案翻译（zh-CN:'虚拟城市'/en:'Virtual City'/ja:'仮想都市'）保持 |
| `wealth.*` 1000+ i18n keys | `virtualCity.*` | **批量改名**（用户决策） |
| `wealth.title` / `wealth.subtitle` / `wealth.createRoom` 等 | `virtualCity.title` / `virtualCity.subtitle` / `virtualCity.createRoom` 等 | 同上 |
| `wealthKeys.ts` | `virtualCityKeys.ts` | **文件名一并改** |
| `locales/wealth*.ts`（4 组 × 3 语言 = 12 件） | `locales/virtualCity*.ts` | **文件名一并改** |

### 1.7 配置
| 当前 | 目标 |
|---|---|
| `LsmAgentGame.conf.example` 中 `"wealth": {...}` 段 | `"virtual_city": {...}` |
| `cfg.Wealth.*` 字段访问 | `cfg.VirtualCity.*` |
| `cfg.Wealth.MonthMs` | `cfg.VirtualCity.MonthMs` |
| `cfg.Wealth.MaxResidents` | `cfg.VirtualCity.MaxResidents` |
| `cfg.Wealth.CityDriverEnabled` | `cfg.VirtualCity.CityDriverEnabled` |
| `cfg.Wealth.CityDriverWorkers` | `cfg.VirtualCity.CityDriverWorkers` |

### 1.8 AgentClassName（UA）— **保持不变**
| 当前 | 目标 |
|---|---|
| `LsmAgentGame-City-Human` | `LsmAgentGame-City-Human`（已是城市向，2026-08-06 §24 注册） |

### 1.9 测试入口
| 当前 | 目标 |
|---|---|
| `AutoTestAndDebug_Wealth.md` | `AutoTestAndDebug_VirtualCity.md` |
| `AutoTestAndDebug_Wealth.sh` | `AutoTestAndDebug_VirtualCity.sh` |

---

## 2. 实施阶段（共 5 阶段 + 验收）

### 阶段 1：定位声明档升版（前置 — 必做）
**目标**：解除 §5.4 的"代码层保留 wealth 命名空间"约束。
**操作**：
- 在 `lag_docs/虚拟城市/虚拟城市定位声明-全Agent真实城市模拟器.md` 升版 v2.0（87 行 → ~95 行）
- 追加 §5.5 新条目："2026-09-24 解禁 §5.4：本项目正式启用 `virtual-city` 命名空间，对应前端 URL `/virtual-city`、后端 `game_kind="virtual_city"`、lobby slug `lobby-virtual-city`，定位声明 v1.0 §5.4 同时废止。"
- 该 .md 文件在 `lag_docs/` 子模块内；按 MEMORY 规约 `lag_docs/` 子模块内先 commit+push，主仓库再提交指针

**验收**：`grep "§20260924-01" lag_docs/虚拟城市/虚拟城市定位声明-全Agent真实城市模拟器.md` 命中

### 阶段 2：后端 kind 字面量 + lobby slug + 配置 JSON key（最小后端改造）
**目标**：改后端所有 `game_kind="wealth"` / `lobby-wealth` / `"wealth":` 字面量；不重构包/类型。
**文件清单**：
- `ServerGo/service/game_service.go:46` —— `ID: "lobby-virtual-city", Kind: "virtual_city", Name: "虚拟城市"`
- `ServerGo/api/room_api.go` —— 3 处 `kind == "wealth"` → `kind == "virtual_city"`
- `ServerGo/ws/game_service.go` —— 30+ 处 `case "wealth":` → `case "virtual_city":`、`gameKind == "wealth"` → `gameKind == "virtual_city"`
- `ServerGo/ws/game_service_xiangqi.go` —— 4 处 `case "wealth":` 同步改
- `ServerGo/ws/game_service_wealth.go` —— 6+ 处 `"game_kind": "wealth"` log 字段
- `ServerGo/service/room_service_crud.go` —— 16+ 处 `"wealth"`
- `ServerGo/service/room_service_wealth_fill.go` / `_test.go` / `_full_agent_test.go` —— 全文替换
- `ServerGo/agent/wealthplayer/agent.go` —— `GameKind: "wealth"` 2 处
- `ServerGo/agent/wealthtypes/context.go` —— `GameKind: "wealth"` 1 处
- `ServerGo/config/config.go:33` —— `Wealth WealthConfig \`json:"wealth"\`` → `VirtualCity VirtualCityConfig \`json:"virtual_city"\`` + 顶层字段
- `ServerGo/config/config.go` —— `applyWealthDefaults()` → `applyVirtualCityDefaults()` + 所有 `cfg.Wealth.*` 访问点
- `ServerGo/LsmAgentGame.conf.example` L228 —— `"wealth": {...}` → `"virtual_city": {...}`
- `ServerGo/llm/defaults.go:55` —— 注释 `cfg.Wealth.AgentConcurrency` → `cfg.VirtualCity.AgentConcurrency`
- `ServerGo/llm/registry.go:1017` —— 同上

**验收**：`ServerGo/` 内 grep `case "wealth"\|game_kind.*wealth\|kind == "wealth"\|lobby-wealth\|"wealth":` 零命中（除子模块 `lag_docs/` 引用）

### 阶段 3：后端包/类型/文件名重构
**目标**：`game/wealth/` → `game/virtual_city/`、`agent/wealthplayer/` → `agent/vcplayer/`、`agent/wealthtypes/` → `agent/vctypes/`、`api/wealth_*.go` → `api/virtual_city_*.go`，同步 package name。
**关键操作**：
- `mkdir game/virtual_city && mv game/wealth/*.go game/virtual_city/`
- 批量 `sed -i 's/^package wealth$/package virtual_city/' game/virtual_city/*.go`
- `mv api/wealth_city_api.go api/virtual_city_api.go` + 同步改类型 `WealthCityAPI` → `VirtualCityAPI`
- `mv api/wealth_survey_api.go api/virtual_city_survey_api.go` + 同步改类型 `WealthSurveyAPI` → `VirtualCitySurveyAPI`
- `mv agent/wealthplayer agent/vcplayer` + `mv agent/wealthtypes agent/vctypes`
- 批量改 package 名 + 内部 import 路径
- `router/router.go:38-43` 构造签名同步：`wealthSurveyAPI *api.WealthSurveyAPI` → `vcSurveyAPI *api.VirtualCitySurveyAPI`
- `router/router.go:286-296` `r.Group("/api/games/wealth")` → `r.Group("/api/games/virtual_city")`
- `router/router.go` 4 个 endpoint path 同步
- `main.go:457, 994, 1158, 1173` 注册点改 import 路径

**§4 单文件 ≤1800 行硬上限拆分**（前置必做）：
- `ServerGo/game/wealth/view.go`（>1800 行）拆为 `view_state.go` + `view_lobby.go` + `view_action.go` + `view_residents.go`（按 BuildClientState 子函数分组）
- `ServerGo/game/wealth/engine.go` / `manager.go` / `distribution.go` —— 跑 `wc -l` 评估，按主题拆分
- `ServerGo/agent/vcplayer/prompt.go` / `tools.go` —— 同上

**WS 帧名同步**（`ServerGo/ws/game_service.go:945-950`）：
- 4 个 case 同步改：`case "game.virtual_city_action":` 等
- 同时改 `ServerGo/ws/game_service_wealth.go` → `ServerGo/ws/game_service_virtual_city.go`（包内仍走 `package ws` 不变，但文件名 + 函数名）

**错误码同步**：
- `ServerGo/errcode/errcode.go` 41 处 `wealth` 命中
- 错误码数字（35010/35041）**不变**
- 仅改错误消息字面量（如 `"wealth 房间不存在"` → `"virtual_city 房间不存在"`）和 enum 常量名（如 `ErrCodeRoomKindWealth` → `ErrCodeRoomKindVirtualCity`）

**验收**：`ServerGo/` 内 grep `WealthRoomOptions\|WealthConfig\|WealthCityAPI\|WealthSurveyAPI\|wealthplayer\|wealthtypes\|game/wealth` 零命中（除 `agent/class_names.go` 中 `AgentClassCityHuman` 已是不含 wealth 的好命名）

### 阶段 4：前端 URL / 路由 / 弹窗默认值（最关键 — 用户直接见到的部分）
**目标**：把前端 `/wealth` 全部改为 `/virtual-city`，把 `WealthCreateRoomModal` 的 `RESIDENT_DEFAULT` 从 10000 改为 10。
**文件清单**：
- `ClientWeb/src/App.tsx:66-68` —— 三条 Route path 改 `/virtual-city*`
- `ClientWeb/src/components/layout/AppSidebar.tsx:60` —— `to: '/wealth'` → `to: '/virtual-city'`，`labelKey: 'nav.wealth'` 保持
- `ClientWeb/src/components/lobby/GameCard.tsx:19-37` —— `game.kind === 'virtual_city'` 分支映射 `/virtual-city`
- `ClientWeb/src/pages/WealthLobbyPage.tsx` → 重命名为 `pages/VirtualCityLobbyPage.tsx`，全部 11+ 处 `nav('/wealth/...')` 替换
- `ClientWeb/src/pages/WealthGamePage.tsx` → 重命名为 `pages/VirtualCityGamePage.tsx`，内部 `game_kind: 'wealth'` → `game_kind: 'virtual_city'`
- `ClientWeb/src/hooks/useWealth.ts` → `hooks/useVirtualCity.ts`，所有 `navigate('/wealth')` 和 `game_kind: 'wealth'` 替换
- `ClientWeb/src/rules/gameRules.ts:10` `GameKind` union 改 `'virtual_city'`；L27-29 `rulesUrl(kind)` 函数同步；L13 `'wealth'` → `'virtual-city'`
- `ClientWeb/src/hooks/useSessionRestore.ts` —— L10/L12/L14/L29-30 四处 `'wealth'` → `'virtual-city'`（路径正则同步）
- `ClientWeb/src/services/auth.service.ts:135` —— `roomService.create/list` 的 `gameKind` 参数透传保持 `string` 类型（接 `'virtual_city'`）
- `ClientWeb/src/api/wealth.ts` → `api/virtualCity.ts`，4 个 URL 改 `/api/games/virtual_city/rooms/...`
- `ClientWeb/src/types/wealth.ts` → `types/virtualCity.ts`，`game_kind: 'wealth'` → `game_kind: 'virtual_city'`
- `ClientWeb/src/types/api.ts:3` —— import 路径同步
- `ClientWeb/src/store/wealth.store.ts` → `store/virtualCity.store.ts`

**§4 单文件 ≤1800 行硬上限拆分**（前置必做）：
- `ClientWeb/src/types/wealth.ts`（**~1213 行 → ~1800 行 max**，先 `wc -l` 确认）
  - 如超过 1800 行：拆 `types/virtualCity/{state,player,actions,society,markets,events,utils}.ts`
- `ClientWeb/src/styles/wealth.css`（**1738 行**确认超限）
  - 必先拆：按主题拆为 `styles/virtualCity.css`（核心 ~800 行）+ `styles/virtualCity-extras.css`（抽屉/弹层/扩展 ~400 行）+ `styles/virtualCity-economy.css`（经济图表 ~400 行）
  - `globals.css` 的 `@import` 顺序**保持不变**（CLAUDE.md §2.1 第 4 条硬约束）

**创建弹窗 `RESIDENT_DEFAULT` 调整**：
- `ClientWeb/src/components/wealth/WealthCreateRoomModal.tsx` → 重命名 `components/virtualCity/VirtualCityCreateRoomModal.tsx`
- L56：`RESIDENT_DEFAULT = 10000` → `RESIDENT_DEFAULT = 10`（**用户明确要求**）
- L47-52 `RESIDENT_PRESETS = [{10,'10'}, {1000,'1千'}, {10000,'1万'}, {100000,'10万'}]` —— **保留**（10/1千/1万/10万 是合法可选档位，弹窗默认 = 10，但用户仍可在弹窗里手动选其他档位或自由输入 10-100000）

**前后端联调验证**：
- POST `/api/games/virtual_city/rooms` body 字段 `resident_count=10` 应被后端接受并 `cfg.VirtualCity.MaxResidents=100000` clamp
- 后端 `service.room_service_crud.go:288-300` 读取 `ResidentCount=10` 创建房间并下发 `room.ResidentCount=10`
- 前端大厅列表 `RoomListTable.tsx:256-262` 的 🏙 badge 显示 `10`

**资源目录**：
- `assets/images/wealth/` → `assets/images/virtualCity/`
- `assets/images/wealth/index.ts` → `assets/images/virtualCity/index.ts`，同步 `WEALTH_BANNER` → `VIRTUAL_CITY_BANNER`
- `assets/images/wealth/{agents,districts,facades,goods,ground,pbr,props,roofs,sky,streets}/` —— 目录同步搬移，URL barrel 导出路径自动跟随

**静态规则 markdown**：
- `ServerGo/static/rules/wealth.md` → `static/rules/virtual-city.md`
- `ClientWeb/dist/rules/wealth.md`（构建产物）—— 自动跟随
- `rebuild_restart_app.sh` 同步检查 copy step（如有 hardcode）

**i18n 范围控制**（按用户决策：全量改 `virtualCity.*`）：
- `i18n/wealthKeys.ts` → `i18n/virtualCityKeys.ts`，内文 1000+ key 全部 `wealth.*` → `virtualCity.*`
- `i18n/locales/{zh-CN,en,ja}.ts` 中所有 `wealth.*` 引用同步替换
- `i18n/locales/wealth*.ts`（12 件） → `i18n/locales/virtualCity*.ts`
- `i18n/types.ts` 中 `WealthDict` interface → `VirtualCityDict`
- `zh-CN.ts:1381` `'nav.wealth': '虚拟城市'` → `'nav.virtualCity': '虚拟城市'`
- `en.ts:1381` `'nav.wealth': 'Virtual City'` → `'nav.virtualCity': 'Virtual City'`
- `ja.ts:1380` `'nav.wealth': '仮想都市'` → `'nav.virtualCity': '仮想都市'`
- **批量改动风险**：1000+ key 全量替换，必须配合 `npx tsc --noEmit` 强类型校验（`WealthDict` interface key 改名后引用方会编译失败）

**验收**：
- 前端编译：`cd ClientWeb && npx tsc --noEmit && npm run build` 通过
- 浏览器访问 `/wealth` 应 404（旧的 URL 不再有效）；访问 `/virtual-city` 应正常进入大厅

### 阶段 5：测试入口 + 验证
**目标**：更新自动化测试入口；完整跑通虚拟城市建房链路 + 大厅 nav + 创建房间。
**文件清单**：
- `AutoTestAndDebug_Wealth.md` → `AutoTestAndDebug_VirtualCity.md`
- `AutoTestAndDebug_Wealth.sh` → `AutoTestAndDebug_VirtualCity.sh`
- 内文引用路径全部从 `/wealth/...` 改为 `/virtual-city/...`、组件名从 `Wealth*` 改为 `VirtualCity*`、WS 帧名 `game.wealth_*` → `game.virtual_city_*`、HTTP path `/api/games/wealth/...` → `/api/games/virtual_city/...`

**测试执行**：
- 删除已处理报告 `TestReport/*_虚拟城市自动化测试报告_*.md`（CLAUDE.md §22）
- 跑 `AutoTestAndDebug_VirtualCity.sh`（或 `claude` CLI 调度），按 §22 流程：登录 + 建 1 个房 + 观战 + 退出
- 验证弹窗默认 `resident_count = 10`、HTTP 200、WS `game.virtual_city_start` 帧收到
- 如有问题，按 §22 修复流程闭环

**§13 SubAgent 派遣**：
- 前端改 → `frontend-dev`（CLAUDE.md §13.1 职责线 1）
- 后端改 → `backend-dev`（CLAUDE.md §13.1 职责线 2）
- 跨端联调 → `integration-tester`（CLAUDE.md §13.1 职责线 5）
- lag_docs 改 → 主 Agent（CLAUDE.md §13.1 缺 §game-designer 派遣；本任务 §5.4 v2.0 由主 Agent 改）

---

## 3. 详细文件改动清单（按目录分）

### 3.1 后端 `ServerGo/`

#### 3.1.1 路由
- `router/router.go` L38-43, L286-296 —— 4 endpoints + 1 group + 2 个 API 构造签名
- `router/router_test.go`（如有） —— 同步改

#### 3.1.2 API
- `api/wealth_city_api.go` → `api/virtual_city_api.go`（重命名 + 类型 `WealthCityAPI` → `VirtualCityAPI`）
- `api/wealth_city_api_test.go` → `api/virtual_city_api_test.go`
- `api/wealth_survey_api.go` → `api/virtual_city_survey_api.go`
- `api/room_api.go` L44-65, L144, L152, L161-164, L173, L181, L256-259 —— `kind == "wealth"` 改 `kind == "virtual_city"`、`wealth` struct field 重命名
- `api/room_api_election_test.go`、`api/room_api_resident_test.go` —— 测试用例断言同步改
- `api/room_api_test.go`（如有） —— 同步

#### 3.1.3 Service
- `service/game_service.go` L46 —— lobby id + kind 改
- `service/room_service_crud.go` —— 16+ 处 `"wealth"` 替换
- `service/room_service_wealth_fill.go` / `_test.go` / `_full_agent_test.go` —— 10/12/19 处替换
- `service/room_service_*_test.go` —— 全部 wealth 命中同步改

#### 3.1.4 WS
- `ws/game_service.go` L706, L848-849, L903-904, L945-950, L1228, L1272, L1309 —— 30+ 处 `case "wealth":` 改 `case "virtual_city":`，4 处 `game.wealth_*` → `game.virtual_city_*`
- `ws/game_service_xiangqi.go` L38, L440, L634, L747 —— 4 处
- `ws/game_service_wealth.go` → 重命名 `ws/game_service_virtual_city.go`，函数 `GameServiceWealth*` → `GameServiceVirtualCity*`，log 字段 `"game_kind": "wealth"` → `"game_kind": "virtual_city"`
- `ws/game_service_wealth_bot.go` → `ws/game_service_virtual_city_bot.go`
- `ws/game_service_wealth_start_test.go` → `ws/game_service_virtual_city_start_test.go`

#### 3.1.5 Game Engine（最重）
- 整个 `game/wealth/` → `game/virtual_city/`，含 ~60 个 .go 文件，package `wealth` → `virtual_city`
- 大文件先拆：`view.go` → `view_state.go` + `view_lobby.go` + `view_action.go` + `view_residents.go`（按 §4 1800 行上限）
- `engine.go`、`manager.go`、`distribution.go` —— 先 `wc -l`，超 1800 行拆
- 子目录 `game/wealth/city/` → `game/virtual_city/city/`，`game/wealth/profession/` → `game/virtual_city/profession/`，`game/wealth/regulators/` → `game/virtual_city/regulators/`
- 测试文件同步重命名

#### 3.1.6 Agent
- `agent/wealthplayer/` → `agent/vcplayer/`，14 文件，package `wealthplayer` → `vcplayer`
- `agent/wealthtypes/` → `agent/vctypes/`，package `wealthtypes` → `vctypes`
- `agent/class_names.go` —— **保持** `AgentClassCityHuman`（已是城市向）

#### 3.1.7 Config
- `config/config.go` L33 —— `Wealth WealthConfig \`json:"wealth"\`` → `VirtualCity VirtualCityConfig \`json:"virtual_city"\``
- `config/config.go` L389 `CityDriverEnabled` 字段所在结构体重命名
- `config/config.go` L1237 默认值常驻结构体重命名
- `config/config.go` `applyWealthDefaults()` → `applyVirtualCityDefaults()`
- `LsmAgentGame.conf.example` L228 —— `"wealth": {...}` → `"virtual_city": {...}`，内层字段名保持（如 `month_ms`、`max_residents`）
- 真实 `LsmAgentGame.conf`（gitignore）—— **当前不含 wealth 段**，**无需改**（运维保持空配置即可，等价于应用默认值）

#### 3.1.8 错误码
- `errcode/errcode.go` 41 处 wealth —— 错误码数字不变（35010/35041 等），仅改 enum 名称和错误消息字面量

#### 3.1.9 静态资源
- `static/rules/wealth.md` → `static/rules/virtual-city.md`
- `static/rules/wealth_*.md`（如有） → `static/rules/virtual-city_*.md`

#### 3.1.10 main.go
- `main.go:457, 994, 1158, 1173` —— 注入点 `wealthResidentCounter` callback 重命名 `virtualCityResidentCounter`；import 路径同步

### 3.2 前端 `ClientWeb/src/`

#### 3.2.1 路由 & 页面
- `App.tsx:66-68` —— 3 条 Route path 改
- `pages/WealthLobbyPage.tsx` → `pages/VirtualCityLobbyPage.tsx`（重命名 + 11+ 处 `nav('/wealth/...')` 替换 + `list('wealth')` → `list('virtual_city')` + `roomService.create('wealth', ...)` → `roomService.create('virtual_city', ...)` + `useLobbyLiveUpdate({ gameKind: 'wealth' })` → `useLobbyLiveUpdate({ gameKind: 'virtual_city' })`）
- `pages/WealthGamePage.tsx` → `pages/VirtualCityGamePage.tsx`（重命名 + 16+ import 路径改 + WS frame payload `game_kind: 'wealth'` → `game_kind: 'virtual_city'`）

#### 3.2.2 布局 / 菜单
- `components/layout/AppSidebar.tsx:60` —— `to: '/wealth'` → `to: '/virtual-city'`，`labelKey: 'nav.wealth'` 保持

#### 3.2.3 大厅卡片
- `components/lobby/GameCard.tsx:19-37` —— `game.kind === 'virtual_city'` 分支映射 `/virtual-city`

#### 3.2.4 hooks / store / types / api / rules / session restore
- `hooks/useWealth.ts` → `hooks/useVirtualCity.ts`（18 行财富锚点：L155 log、L159 navigate、L174/L178/L190 game_kind、L196/L208/L214/L225 wsClient.send frame 名）
- `store/wealth.store.ts` → `store/virtualCity.store.ts`
- `types/wealth.ts` → `types/virtualCity.ts`（30+ 接口 `WealthXxx` → `VirtualCityXxx`，~15 utils 函数 `wealthXxx` → `virtualCityXxx`，3 常量 `WEALTH_*` → `VIRTUAL_CITY_*`，L519 game_kind 字面量，L1004 ws 帧名前缀，L1234 http path 注释）
- `types/api.ts:3` —— import 路径
- `api/wealth.ts` → `api/virtualCity.ts`（4 个 URL 改 `/api/games/virtual_city/rooms/...`）
- `rules/gameRules.ts:10, 13, 27-29` —— GameKind union + 路径正则 + rulesUrl 函数
- `hooks/useSessionRestore.ts` —— 4 处 `'wealth'` 替换

#### 3.2.5 components/wealth/
- 整个目录 → `components/virtualCity/`
- 7 个 `Wealth*` 前缀文件重命名（见 §1.4）
- 子目录 `civic/` `props/` 同步搬移，文件名前缀（如 `Pedestrian.tsx`、`Vehicle.tsx`）保持不变（命名中性）
- ~28 个非 `Wealth` 前缀的 .tsx 文件（如 `ActionPanel.tsx`、`AuctionModal.tsx`、`CityStatsPanel.tsx`）保留原名
- `wealth-batch20.css` → 改为 `virtualCity-batch20.css`，import 关系保持
- `Model.tsx` / `modelCache.ts` / `textureCache.ts` —— 保留原名，import 路径 `@/assets/images/virtualCity` 同步

#### 3.2.6 styles/
- `styles/wealth.css`（1738 行）先拆：
  - `styles/virtualCity.css`（核心，~800 行）
  - `styles/virtualCity-extras.css`（抽屉/弹层，~400 行）
  - `styles/virtualCity-economy.css`（经济图表，~400 行）
- `styles/wealth-p2.css` → `styles/virtualCity-p2.css`
- `styles/wealth-insurance.css` → `styles/virtualCity-insurance.css`
- `styles/wealth-residents.css` → `styles/virtualCity-residents.css`
- `styles/wealth-city3d.css` → `styles/virtualCity-city3d.css`
- `styles/wealth-batch20.css` → `styles/virtualCity-batch20.css`
- `styles/globals.css` `@import` 顺序**保持不变**（CLAUDE.md §2.1 第 4 条硬约束）
- `styles/buttons.css:53` 注释 `@import './wealth.css';` → `@import './virtualCity.css';`

#### 3.2.7 assets/
- `assets/images/wealth/` → `assets/images/virtualCity/`（10 个子目录 + banner.png）
- `assets/images/wealth/index.ts` → `assets/images/virtualCity/index.ts`，`WEALTH_BANNER` → `VIRTUAL_CITY_BANNER`
- `assets/models/` —— **不动**（CLAUDE.md §27 `category` 维度命名已是城市向）

#### 3.2.8 i18n（用户决策：全量改为 `virtualCity.*`）
- `i18n/wealthKeys.ts` → `i18n/virtualCityKeys.ts`，内文 1000+ key 全量改 `wealth.*` → `virtualCity.*`
- `i18n/locales/wealthBatch20-{en,ja,zh}.ts` → `i18n/locales/virtualCityBatch20-{en,ja,zh}.ts`
- `i18n/locales/wealthInsurance-{en,ja,zh}.ts` → `i18n/locales/virtualCityInsurance-{en,ja,zh}.ts`
- `i18n/locales/wealthP2-{en,ja,zh}.ts` → `i18n/locales/virtualCityP2-{en,ja,zh}.ts`
- `i18n/locales/wealthResidents-{en,ja,zh}.ts` → `i18n/locales/virtualCityResidents-{en,ja,zh}.ts`
- `i18n/locales/{zh-CN,en,ja}.ts` 中所有 `wealth.*` 引用替换为 `virtualCity.*`
- `i18n/locales/types.ts` `WealthDict` interface → `VirtualCityDict`
- AppSidebar.tsx 中 `labelKey: 'nav.wealth'` → `labelKey: 'nav.virtualCity'`

### 3.3 测试入口
- `AutoTestAndDebug_Wealth.md` → `AutoTestAndDebug_VirtualCity.md`
- `AutoTestAndDebug_Wealth.sh` → `AutoTestAndDebug_VirtualCity.sh`
- 内文引用全部虚拟化

### 3.4 文档
- `lag_docs/虚拟城市/虚拟城市定位声明-全Agent真实城市模拟器.md` —— v1.0 → v2.0，§5.4 解禁 + 新增 §5.5
- `lag_docs/通用功能/自动化测试与调试流程合并方案-20260915.md` —— AutoTestAndDebug_Wealth → AutoTestAndDebug_VirtualCity 引用同步

---

## 4. 风险点 + 验收清单

### 4.1 风险点

| 风险 | 缓解措施 |
|---|---|
| `ServerGo/game/wealth/view.go` 拆分引入 bug | 仅做纯代码搬移，不改逻辑；拆分前后 `go test ./...` 全绿 |
| `ClientWeb/src/styles/wealth.css` 拆分产生样式回归 | `globals.css` `@import` 顺序保持；拆分后 `npm run build` 产物 CSS 字节对比验证 |
| 数据库现存 `t_lsm_game_room.game_kind='wealth'` 行失效 | 阶段 5 跑 DB 迁移 SQL：`UPDATE t_lsm_game_room SET game_kind='virtual_city' WHERE game_kind='wealth';` |
| WS 帧 `game.wealth_*` → `game.virtual_city_*` 兼容性 | 保留 `case "game.wealth_action":` 作为 alias 转发到 `case "game.virtual_city_action":` 逻辑一段时间，灰度期后删除 |
| 错误码 35010/35041 数字不变但 enum 名改了 | 测试用例断言从 `ErrCodeRoomKindWealth` 改 `ErrCodeRoomKindVirtualCity`；IPC contract 兼容 |
| `AutoTestAndDebug_Wealth.sh` 文件名改了，CI/派遣脚本断链 | 同步改 `agent_cli_common.sh`、`auto_run_common.sh` 中 grep 锚点 |
| `python-generate-image-tool/` 子模块内 `generate_wealth_*.py` 文件已改 | 派遣 art-designer SubAgent 在子模块内 commit+push，主仓库再提交子模块指针更新 |
| `AutoTestScreenshots/wealth_*.png` 产物文件名 | gitignored，重命名后旧产物清理即可 |

### 4.2 验收清单

#### 阶段 1（定位声明）
- [ ] `lag_docs/虚拟城市/虚拟城市定位声明-全Agent真实城市模拟器.md` v2.0 §5.5 新增条目
- [ ] lag_docs 子模块内 commit + push

#### 阶段 2（后端 kind 字面量）
- [ ] `cd ServerGo && go build -o LsmAgentGame main.go` 通过
- [ ] `cd ServerGo && go test ./...` 全绿
- [ ] `grep -rn "case \"wealth\"\|game_kind.*wealth\|lobby-wealth\|kind == \"wealth\"\|kind != \"wealth\"" ServerGo/ --include="*.go"` 零命中（除 `agent/class_names.go` 中 `AgentClassCityHuman`）

#### 阶段 3（后端包/类型）
- [ ] `ServerGo/` 内 grep `WealthRoomOptions\|WealthConfig\|WealthCityAPI\|WealthSurveyAPI\|wealthplayer\|wealthtypes\|game/wealth` 零命中
- [ ] `go build` + `go test ./...` 全绿
- [ ] `errcode/errcode.go` 中所有错误消息字面量更新、enum 名同步

#### 阶段 4（前端 + 创建弹窗）
- [ ] `cd ClientWeb && npx tsc --noEmit` 通过
- [ ] `cd ClientWeb && npm run build` 通过（CSS 产物字节级对比无回归）
- [ ] `ClientWeb/src` 内 grep `/wealth\|WealthLobbyPage\|WealthGamePage\|game_kind.*'wealth'\|game.wealth_*` 零命中（除 i18n key `wealth.*` 命名空间）
- [ ] `VirtualCityCreateRoomModal.tsx` L56 `RESIDENT_DEFAULT = 10`
- [ ] `VirtualCityCreateRoomModal.tsx` L47-52 `RESIDENT_PRESETS = [{10,'10'}, ...]` 保留
- [ ] 浏览器访问 `https://127.0.0.1:39001/virtual-city` 进入虚拟城市大厅
- [ ] 浏览器访问 `https://127.0.0.1:39001/wealth` 404
- [ ] 左侧菜单栏"虚拟城市"入口点击进入 `/virtual-city`

#### 阶段 5（测试 + 提交）
- [ ] `AutoTestAndDebug_VirtualCity.{md,sh}` 文件名已改
- [ ] 测试通过：登录 + 建 1 个 `resident_count=10` 房间 + 观战
- [ ] HTTP `POST /api/games/virtual_city/rooms` body `resident_count=10` 返回 200
- [ ] WS `game.virtual_city_start` 帧正常收到
- [ ] 大厅列表 `RoomListTable.tsx` 🏙 badge 显示 `10`
- [ ] 删除已处理 `TestReport/*_虚拟城市自动化测试报告_*.md`
- [ ] 执行 `./rebuild_restart_app.sh` 重新编译
- [ ] 中文 git commit（feat/虚拟城市/批量21：URL + kind 全栈重命名为 virtual-city + 创建弹窗默认 10）
- [ ] git push origin main

---

## 5. SubAgent 派遣规划（按 CLAUDE.md §13.1）

| 阶段 | SubAgent 代号 | 工作面 | 预计时长 |
|---|---|---|---|
| 阶段 1 | **主 Agent**（CLAUDE.md §13.2 允许） | `lag_docs/虚拟城市/虚拟城市定位声明-全Agent真实城市模拟器.md` | 10 min |
| 阶段 2 + 3 | `backend-dev` | `ServerGo/`（除 `lag_docs/` 和 `static/rules/`） | 60-90 min |
| 阶段 4 前端 | `frontend-dev` | `ClientWeb/src/`（含 i18n 1000+ key 改名） | 90-120 min |
| 阶段 4 子模块 | `art-designer`（**新建派遣**，CLAUDE.md §13.1 职责线 4 扩展） | `python-generate-image-tool/`（git submodule 内） | 30-45 min |
| 阶段 5 | `integration-tester` | 跨端联调 + 自动化测试 | 30-45 min |

**说明**：
- 主 Agent 不直接修改 `ServerGo/`、`ClientWeb/`、`python-generate-image-tool/`、`go-web-debug-tool/`（CLAUDE.md §13.2 第 4 条）
- lag_docs 修改是文档类工作面，按 §13.1 缺 §game-designer 派遣；本任务由主 Agent 处理
- `art-designer` 派遣约束：因 `python-generate-image-tool/` 是独立 git 子模块（CLAUDE.md §9），art-designer SubAgent 必须在子模块内 commit + push 后，主仓库再提交子模块指针更新
- 跨阶段衔接：阶段 1 → 2 → 4 → 5 严格串行（每阶段验证通过后再启动下一阶段）；阶段 4 art-designer 与 frontend-dev 可并行

---

## 6. 不在本次范围

- `lag_docs/虚拟城市/已实现/` 目录内文件（定位声明 §4 禁改区）
- `lag_docs/虚拟城市/玩家职业设计/` 目录（人物卡池，MEMORY `caishangliu-card-edit-via-pipeline.md` 约束）
- `lag_docs/虚拟城市/其他类似游戏资料/` 目录（外部游戏描述）
- `AgentClassName = "LsmAgentGame-City-Human"` UA（CLAUDE.md §24 已城市向）

---

## 7. 时间线估算（端到端）

| 阶段 | 内容 | 预计耗时 |
|---|---|---|
| 阶段 1 | lag_docs 定位声明 v2.0 | 10 min |
| 阶段 2 | 后端 kind 字面量（不改包） | 30 min |
| 阶段 3 | 后端包/类型/文件名重构 | 60-90 min |
| 阶段 4 前端 | 前端 URL + i18n 1000+ key + 弹窗默认值 | 90-120 min |
| 阶段 4 子模块 | python-generate-image-tool 子模块重命名 | 30-45 min |
| 阶段 5 | 测试 + rebuild + commit + push | 30 min |
| **合计** | | **4.5-6 小时** |

---

（方案结束。）
