## 自动化 Web 游戏测试任务：狼人杀 13 人局 Agent 专项（`_Werewolf` 入口版）

> 本项目按 [MIT License](LICENSE) 开源。测试任务面向维护者，参与前请阅读 [CONTRIBUTING.md](CONTRIBUTING.md) 与 [AutoDebugTestReport.md](AutoDebugTestReport.md)。
> 本文件为狼人杀专用入口，**仅产出** `自动化测试报告_YYYYMMDD_HHMMSS.md` 前缀的报告；德州扑克请使用 [`AutoTestAndSaveReport_TexasPoker.md`](AutoTestAndSaveReport_TexasPoker.md)。

- **工作目录**: `/usr/local/LsmAgentGame/LsmAgentGame`
- **入口脚本**: `AutoTestAndSaveReport_Werewolf.sh`(随机选编程 Agent CLI → 跑测试 → git 提交狼人杀报告 → 接力自动修复)
- **产物路径(仅狼人杀,§20260820-01 起统一带「狼人杀」前缀 + 兼容旧文件)**:
  - 测试报告(新前缀):
    `TestReport/狼人杀自动化测试报告_YYYYMMDD_HHMMSS.md`
  - 测试报告(兼容旧):
    `TestReport/自动化测试报告_YYYYMMDD_HHMMSS.md`(历史已落盘文件,继续合法)
  - 进度文件(新):
    `AutoTestProgress/狼人杀自动化测试进度_YYYYMMDD_HHMMSS.md`
  - 子工程工具报告(新前缀):
    `go-web-debug-tool/UseReport/狼人杀测试工具使用报告_YYYYMMDD_HHMMSS.md`
  - 子工程工具报告(兼容旧):
    `go-web-debug-tool/UseReport/测试工具使用报告_YYYYMMDD_HHMMSS.md`
  - 协议抓包分析报告(新):
    `TestReport/狼人杀协议抓包分析报告_YYYYMMDD_HHMMSS.md`
  - 文件名时间戳统一 `YYYYMMDD_HHMMSS`,精度到秒
  - **Glob 单一事实源**: 所有扫描 glob 在 `auto_run_common.sh::GAME_GLOBS` 中声明,新增游戏仅改公共库一处,本文件不再内嵌 glob 列表。
- **执行模式**: 主 Agent 跑核心流程;遇代码读取/协议抓包/数据查询可按需委派 SubAgent,不阻塞主流程。
- **执行策略**: 模仿真实人类玩家(1 人 + 12 Agent 混合 13 人局),串行单房间,严守「每完成一步等待页面完全加载再走下一步」的拟人节奏;以最近 10 次非缺陷修复提交划定测试范围,新增/优化项优先。
- **对局模式(唯一)**: 1 名人类玩家 + 12 个 Agent,**不测试全 AI 模式**。
- **核心目标**: 验证 Web 端人类玩家可用的全部交互(房间创建、角色选择、技能/道具、投票、选择、发言、夜昼切换、遗言等)与 12 个 Agent 的拟人行为(策略表达、工具调用与发言解耦、说谎/叙事、观战适配)。
- **代码变更限制**: 业务代码只读;若发现 Agent 提示词/工具定义/业务逻辑缺陷,**只出报告与建议**,严禁自动改业务代码。

### 1. 核心测试工具(MCP)

主工具是 `go-web-debug-tool`(MCP 服务,默认 `http://localhost:28999`),接口定义以
`/usr/local/LsmAgentGame/LsmAgentGame/go-web-debug-tool/MCP_Proc_Def.md` 为唯一事实来源。提供两类能力:

1. **Web 端操作模拟**: 模拟真实人类点击/滚动/截图,问题即停。
2. **协议层诊断**: Agent 调 LLM 失败 ≥ 2 次时,**必须**走该接口下探协议抓包,再下结论。

可选辅助: `/usr/local/LsmHttpAgent/MCP_ChatAnalysisInterface_def.md`(协议抓包 SubAgent),仅在协议诊断时按需启用。

### 2. 单角色测试规则

- **每轮只测一个角色**: 单轮选定一个角色跑完整对局,不切角色连测。
- **轮换依据**: 从最近一份进度文件读取「已测 / 待测」清单,选**尚未测过**或**最久未测**的角色;无历史则从 `SELECTABLE_ROLES` 第 1 个开始。
- **进度落盘**: 本轮所选角色、结论、更新后清单,完整写入进度文件,作为下轮唯一依据。
- **角色选择界面也是测试对象**: 渲染完整性、选中/取消切换、非法选择拦截、**所选角色与发牌后实际身份必须严格一致**;不一致按终止条件 §4.2 整轮终止。

### 3. 终止条件

1. 阶段卡死 / 页面崩溃 / 规则自相矛盾 → 终止**当前用例**,记录后判断是否继续。
2. 游戏规则、胜负判定、**所选角色 ≠ 实际身份** 等核心逻辑错误 → 整轮终止。
3. MCP 工具严重异常无法支撑测试 → 整轮终止,并写子工程 `UseReport/` 工具报告。
4. Agent 调 LLM **连续 5 次失败**,协议诊断确认不可恢复(鉴权失败/协议不兼容/服务端持续故障)→ 整轮终止。
5. 无论正常结束或异常终止,均须按规范出报告 + 进度文件;中断的进度必须标「中断未完成」。

### 4. 启动前置

1. 读最近 10 次**非缺陷修复类**提交(`git log --oneline -10 -- ':!*fix' ':!*BUG'` 等过滤),梳理新增/优化项作为本轮重点。
2. 读 `AutoTestProgress/` 最新一份进度文件,沿用其「已测 / 待测」清单。
3. **读取「未覆盖项跟踪表」**(§11 多轮策略):从最新进度文件提取所有状态为 `❌ 未覆盖` 或 `⚠️` 的条目,按 §11 规则确定**本轮重点解决的 1-3 项**,写入进度草稿的「本轮重点突破项」。
4. 敲定**本轮唯一待测角色**并写入进度草稿;无历史则从 `SELECTABLE_ROLES` 第 1 个开始。
5. 已充分覆盖的模块可跳过;变更项必须覆盖。
6. 读 `go-web-debug-tool/MCP_Proc_Def.md` 确认 MCP 操作/抓包接口可用;启动 MCP 子 Agent 前确认 `/usr/local/LsmHttpAgent/MCP_ChatAnalysisInterface_def.md` 可连。
7. 准备 `test_account.json` 中的白名单账号(验证码自动旁路,见 `docs/通用功能/测试账号凭证.md`)。

### 5. 大模型 API 异常与协议层诊断

- **触发**: 任一 Agent 调 LLM 失败 ≥ 2 次(超时/报错/空响应/tool_use 解析失败等)即启动协议诊断。
- **方式**: 调 MCP「查看 Anthropic 协议抓包数据」接口(或委派 SubAgent),按 `MCP_Proc_Def.md` 规范读取。
- **重点排查**:
  - 请求侧: URL / 鉴权头 / `model` / `messages` 角色顺序 / `tools` `tool_use` `tool_result` 配对 / `max_tokens` 与上下文长度 / 请求体是否合法 JSON。
  - 响应侧: HTTP 状态码(400/401/429/5xx)/ `error.type|message` / `stop_reason` / SSE 是否截断 / `content` 中 `tool_use` 完整性。
  - 链路侧: TLS 握手 / 超时与连接重置 / 代理网关改写 / 多模型(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米/腾讯)协议适配差异。
- **结论落地**: 错误归因(请求构造/鉴权/限流/服务端/适配层/网络)写入协议抓包分析报告,并在测试报告里引用;若属可修复缺陷,出具体优化建议(**不自动改业务代码**)。
- **与终止条件联动**: 2 次失败启动诊断并继续;累计 5 次失败且确认不可恢复,按 §3.4 终止。

### 6. 测试步骤

#### 6.1 准备
- 读 `test_account.json` 取账号;新注册账号也回写,避免重复注册。
- 启 MCP 服务,确认操作模拟 + 抓包两类能力均可用。
- 验证码走 `test_01~04` 白名单旁路。
- 进度草稿记录本轮角色 + 房间创建时间。

#### 6.2 大厅与页面遍历
- 模拟人类玩家遍历大厅与游戏页所有可交互元素(按钮/链接/开关/输入框)。
- 结合最近 10 次提交判断是否有新/优功能:无则跑基础可用性,有则重点验证。
- 每次点击后**等待页面完全加载**(推荐 800~1500ms)再走下一步,符合真实人类节奏。
- 未达预期项详细记录,写入最终报告。

#### 6.3 房间功能与 Agent 专项(单房间串行)

**6.3.1 房间创建与角色选择**
- 校验房间创建弹窗各配置项渲染完整、可交互。
- **角色选择 UI 验证**: 全部可选角色展示、选中/取消切换、说明与图标、非法/冲突拦截提示。
- 按本轮唯一角色创建 1 人 + 12 AI 房间。
- 发牌后立即校验人类玩家实际身份 ≡ 所选角色(不一致按 §3.2 整轮终止)。
- 选角过程异常现象 + 身份一致性结论写入进度与报告。

**6.3.2 人类玩家操作与界面功能**
- **按钮完整性**: 准备/取消/确认/取消/技能/发言/退出 等所有按钮,验证点击响应、状态切换、无卡顿。
- **游戏环节界面**: 夜昼切换、发言、投票、遗言 等所有阶段的 Web 端窗口显示、计时器/玩家列表/状态图标/阶段提示,切换流畅。
- **技能/道具窗口**: 本轮角色的技能 UI(女巫解/毒、守卫守护、预言家查验、狼人刀人、猎人开枪、骑士决斗、猎魔人狩猎 等),目标选择 + 确认/取消 + 反馈提示。平民无主动技能则重点验证夜间等待态。
- **投票界面**: 投票窗口 + 选项渲染(含弃票) + 投票按钮 + 结果展示 + 状态变化。
- **选择界面**: 各类目标选择窗口(刀人/查验/救人/毒人/开枪目标 等),目标列表 + 确认/取消 + 结果生效与反馈。
- **窗口通用**: 所有弹窗/浮层/侧边栏在各类场景下显示正常,无遮挡/错位/溢出/不全。

**6.3.3 Agent 发言活跃度**
- 重点验证 12 Agent 沉默率显著下降,频次、策略表达、参与度提升;杜绝无意义灌水。
- 遇长时间沉默 Agent,先排查是否 API 失败(协议抓包确认请求/响应),**不得**误判为提示词问题。

**6.3.4 Agent 智能行为**
- **多模型驱动**: 12 Agent 接入不同 LLM(美团/豆包/DeepSeek/Kimi/GLM/Qwen/小米/腾讯 等),调度逻辑正常;某模型异常时,通过抓包定位是调度层还是协议适配层。
- **对话能力核心规则**: 所有策略表达 / 逻辑推演 / 身份伪装 / 立场传递**仅通过对话输出**,无独立于对话的「公开思考过程」。
- **工具调用与「心口不一」机制**: 真实操作仅走工具调用,公聊与实际工具操作可存在差异;验证工具返回值仅 Agent 自身可见、不入公共频道;评估是否需工具调用重构(调用时机隔离 / 入参脱敏 / 返回值私有);必要时核对抓包 `tool_use` / `tool_result` 实际传输,确认私有信息未混入公开通道。
- **说谎与叙事能力**: 悍跳预言家编完整查验逻辑 + 心路历程 / 倒钩虚构站边理由与底牌 / 抗推编织嫌疑证据链 / 被质疑输出自洽辩解 / 多轮策略欺骗与逻辑自洽。
- **游戏策略**: 工具调用执行动作(非闲聊);悍跳/倒钩/垫飞/划水 等语言策略可表达且服务于胜出。
- **核心逻辑**: 「胜出」为第一目标,能完成复杂逻辑表达与策略欺骗。
- **对人类玩家交互**: Agent 能感知并回应人类发言/跳身份/指认,响应及时逻辑合理,无忽略/错乱/规则违背。

**6.3.5 聊天(公聊 + 私聊)**
- 公聊/私聊界面、消息收发、发送后输入框自动清空。
- 私聊内容可与公聊形成差异化策略配合;Agent 在私聊场景下仍能合理伪装。

**6.3.6 完整对局**
- 以本轮角色的人类玩家跑完整对局至分出胜负,覆盖全部核心环节(夜昼/发言/投票/技能释放/遗言/结算)。
- 复盘 Agent 策略连贯性、伪装合理性、工具调用与发言匹配度。

#### 6.4 观战模式(独立用例)
- 主对局结束后另起一局,测试账号以**观战者**身份进入 Agent 对局,不占席位。
- **权限隔离**: 观战者无法查看 Agent 底牌、夜间行动等私有信息,仅公开信息 + Agent 公开发言。
- **聊天室交互**: 观战者能进公共聊天室与 Agent 收发消息。
- **交互逻辑**: 观战者公聊可被 Agent 正常感知并按设定回应。
- **提示词适配**: Agent 面对观战者发言不得泄露身份 / 违反规则 / 应对异常;若有缺陷,报告中给提示词优化方案。

### 7. 操作与覆盖要求

- **拟人操作**: 每步操作后**等待页面完全加载**(建议 800~1500ms)再走下一步,符合真实用户节奏;严禁「点击 + 立刻下一步」的机械节奏。
- **覆盖度**: 全面覆盖所有可交互元素,重点验证按钮/拖拽等游戏专属交互;针对狼人杀 13 人局,专项覆盖角色选择、技能/道具、投票、选择四大核心界面。
- **客观性**: 严格基于页面截图 / 日志 / 协议抓包等可观测事实,杜绝幻觉与臆断。

### 8. 报告与后处理

1. 测试结束首先生成测试报告,完整保存(含本轮使用角色)。报告必须包含 `未覆盖项继承与变化`节(对比上轮)和 `多轮收敛趋势`节(§11.6)。
2. 同步生成进度文件,必备字段:
   - `本轮测试角色`
   - `本轮角色测试结论`(`通过` / `存在缺陷(附摘要)` / `中断未完成`)
   - `已测角色清单` / `待测角色清单` / `下一轮建议角色`
   - `未覆盖项跟踪表`(继承上轮 + 本轮新增,含状态与积压标注)
   - `本轮重点突破项`(1-3 项) + `突破结果`
   - `积压升级标注`(连续 ≥ 2 轮未覆盖的条目,§11.1)
3. 两份文件确认落盘即可——**无需 Agent 手动执行** `AutoDebugTestReport.sh`：`AutoTestAndSaveReport.sh` 在 Claude 退出并完成 git 提交后，由 shell 层**确定性接力**启动自动修复流程（含待处理报告预检；不再依赖 Agent 自觉，避免「声明了却从不接线」式断链）。Agent 只需确保报告与进度文件落盘。
4. MCP 工具异常导致终止时,**额外**在 `go-web-debug-tool/UseReport/` 写工具报告,记录报错日志与异常点(参考 `MCP_Proc_Def.md` 规范)。
5. 触发协议层诊断时,**额外**写协议抓包分析报告(API Key/Token/Cookie 脱敏),并在测试报告里引用。
6. 中断未完成也要写进度,标 `中断未完成`,避免被后续轮次误判为已覆盖。

### 9. 注意事项

- **多 Agent 并发与 Git**: 测试期间留意其他开发 Agent;**仅做只读 Git 查询**,不提交/合并;Git 操作前先 `git status` 确认工作区。
- **单轮单角色**: 硬性约束,禁止「提高效率」连测多个角色;全角色覆盖靠多轮推进。
- **断线重连**: 验证服务器重启 / 客户端断网 场景下重连有效性,确认重连后游戏状态/角色/历史消息完整同步,无丢失 / 重复推送。
- **公平性与健壮性**: 校验 Agent 不得违规获取非公开信息(含人类玩家身份);网络波动重试 / 异常降级 / 重连;观战模式下 Agent 不得因人类观战者对话泄露私有信息。
- **大模型稳定性**: Agent 多次调 LLM 失败不得直接判业务缺陷,先协议层抓包定位再下结论。
- **数据安全**: 抓包数据严禁明文输出 API Key / Token / Cookie,必须脱敏。
- **优化建议**: 报告须给具体可执行建议(系统提示词迭代 / 游戏流程强化 / 工具调用重构 / 策略伪装训练 / 前端修复 / 协议重试策略),定位到根因(提示词 / 工具定义 / 业务逻辑 / 前端 / 协议适配)。

### 10. 主流程阻塞时的降级测试路径（避免全军覆没）

> 当 §6.3 主流程被阻塞（游戏状态同步超时 / Agent 全员静默 / 创建房间超时）时，**不要立即整轮终止**，按以下降级路径继续采集可观测数据，仍能产出有价值的报告。

#### 10.1 第一层：REST API 快照（不依赖 WS `game.state`）

当 `game.state` 无响应时，通过 HTTP REST 端点验证服务端状态：

- `GET /api/games/werewolf/rooms/:id` — 房间状态（`status`/`phase`/`current_count`）
- `GET /api/games/werewolf/state?room_id=:id&seat=:n` — 座位视角状态（含 `players[]`/`phase_extra`）
- `GET /api/games/werewolf/props` — 道具系统 REST（验证 §20260811-10 道具经济）
- `GET /api/llm/models` — 验证 10 个模型注册且 key 可用（排查 P0-2 Agent 静默根因）
- `GET /api/admin/llm/providers` — Provider 可用性（AES 解密是否成功）

**判定**：若 REST 端点返回的 `phase` 与预期不符（如始终 `filling` 未进入 `night_wolves`），则 Agent 唤醒路径有问题；若 `phase` 正常推进但 WS 不推送，则是 WS 分发层问题。

#### 10.2 第二层：服务端日志诊断（不依赖前端渲染）

通过 `journalctl` / 服务日志文件直接观察：

- `werewolf game started` 后是否有 `agent started` × 12
- 是否有 `LLM call` / `stream` 相关日志（排查 P0-2）
- `phase` 推进日志（`night_wolves` → `seer` → `witch` → ...）
- `judge` goroutine 启动日志

**判定**：若日志显示 phase 正常推进 + LLM 调用正常，但前端无响应 → 前端/WS 层问题；若日志也无推进 → 服务端 Agent 唤醒/Provider 问题。

#### 10.3 第三层：DB 直查（不依赖 API）

当 API 也不可用时，直查 DB 验证核心契约：

```sql
-- 验证 bot user + wallet 是否创建
SELECT COUNT(*) FROM t_lsm_game_user WHERE IsBot=1 AND username LIKE 'bot_%';
-- 验证 provider 是否注册且 key 非占位
SELECT id, model, provider_type, LENGTH(api_key_enc) FROM t_lsm_game_llm_provider;
-- 验证房间 DB 状态
SELECT id, status, phase, current_count FROM t_lsm_game_room WHERE id=:room_id;
```

#### 10.4 降级路径产出要求

即使主流程完全阻塞，降级路径采集的数据也必须写入报告：

- **REST 快照** → 填入报告「诊断数据与证据链」节
- **日志统计** → 填入报告「服务端日志统计」节
- **DB 直查** → 填入报告「诊断数据与证据链」节
- **结论**：明确「主流程阻塞，但通过降级路径验证了 X/Y/Z」或「降级路径也失败，问题在更底层」

**禁止**：主流程阻塞后直接终止、报告只写「全部未覆盖」而无诊断数据。

### 11. 多轮迭代策略与未覆盖项滚转

> 核心理念:每轮测试不可能覆盖所有功能,但**未覆盖项必须跨轮滚转、逐轮收敛**。
> 每轮重点解决 1-3 个高价值问题,而非试图一次性全覆盖。

#### 11.1 未覆盖项滚转规则

1. **每轮进度文件必须包含「未覆盖项跟踪表」**,格式:优先级 / 未覆盖项 / 状态 / 备注。
2. **状态三态**: `❌ 未覆盖` → `⚠️ 进行中` → `✅ 已覆盖`。
3. **上轮 `❌ 未覆盖` 自动继承到本轮**,除非有明确理由移除(如功能已下线)。
4. **连续 N 轮未覆盖的条目自动升级**:
   - 连续 2 轮 `❌` → 在备注中标注 `⬆️ 积压 2 轮`。
   - 连续 3 轮及以上 `❌` → 必须在本轮作为重点突破项,或给出「为何仍无法覆盖」的明确说明(如:依赖观战模式,当前仅测玩家模式)。
5. **新发现的未覆盖项**以 `NEW` 标记追加到表尾,不调整已有编号。

#### 11.2 每轮重点突破项选取规则

从「未覆盖项跟踪表」中,按以下优先级选取 **1-3 项**作为本轮重点:

1. **积压最久**(连续 `❌` 轮数最多)的条目优先。
2. **与本轮角色强相关的技能/交互未覆盖项**次优(如本轮测女巫 → 优先突破双药 UI)。
3. **依赖本轮已具备条件的条目**(如本轮可另起观战局 → 优先突破观战专属功能)。
4. **与最近提交变更相关的条目**最后。

选定后写入进度草稿的「本轮重点突破项」,并在测试过程中记录突破结果。

#### 11.3 角色依赖型未覆盖项的处理

当未覆盖项依赖特定角色(如女巫双药 UI、猎人开枪、守卫守护)但发牌未获得该角色时:

1. **不跳过**:记录为 `❌ 未覆盖(角色未获得)` 并继承到下轮。
2. **下轮策略调整**:若连续 2 轮未获得目标角色,可在启动前置时尝试多次创建房间直到牌组包含目标角色(最多 3 次),或调整测试策略优先覆盖其他未覆盖项。
3. **不因角色未获得而整轮无产出**:即使主打角色未获得,仍应利用本轮已发到的角色覆盖其他待测项。

#### 11.4 观战专属未覆盖项的处理

观战者专属功能(如:血迹图/胜率热力图/观战者紧凑底栏等)在玩家对局中无法覆盖:

1. 累积至 2 项及以上时,**必须在下一轮安排独立观战用例**(§6.4)。
2. 若本轮时间不足以起观战局,在进度文件中注明「下轮优先安排观战用例」。
3. 观战用例可与玩家用例在同一轮内串行执行(先玩家局,后观战局)。

#### 11.5 进度文件新增必备字段

在 §8.2 进度文件必备字段基础上,新增:

- `本轮重点突破项`(1-3 项,来自未覆盖项跟踪表)
- `突破结果`(每项: `✅ 已突破` / `⚠️ 部分突破(附说明)` / `❌ 未突破(附原因)`)
- `积压升级标注`(连续 ≥ 2 轮未覆盖的条目标注 `⬆️ 积压 N 轮`)

#### 11.6 报告新增必备字段

在 §8.1 测试报告中,新增:

- `未覆盖项继承与变化`节:对比上轮未覆盖项,列出本轮新增/已覆盖/仍继承的条目。
- `多轮收敛趋势`节:简述整体覆盖度进展(如「6 轮完成,已覆盖 X/Y 项,剩余 Z 项含 N 项积压 ≥ 2 轮」)。
